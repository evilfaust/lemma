# Changelog — История изменений

## [3.9.172] - 2026-09-08

### 🛠 Панель модерации учеников

Профиль ученика правился по кусочкам: имя — в составе группы, владелец — в
карточке, всё остальное не правилось вовсе. Логин посмотреть нельзя, пароль
сбросить нельзя (ученик забыл — заводит второй аккаунт, а учитель потом сливает
их), `telegram_id` не заполнить, хотя без него результаты «Решу ЕГЭ» ученику не
сопоставляются — сейчас он проставлен у 12 человек из 58.

**Единая карточка правки** (`StudentEditModal`) открывается из трёх мест —
списка «Ученики», карточки ученика и состава группы. В ней: имя, группа, класс,
Telegram ID, статус (учится / выпустился / выбыл) с учебным годом, логин, сброс
пароля, владелец-учитель. Сохраняются только реально изменённые поля, и в
сообщении видно, что именно поменялось.

**Подбор Telegram ID из журнала «Решу ЕГЭ».** Рядом с полем — поиск по внешнему
журналу: сначала совпавшие по имени (порядок слов и «ё» не мешают), затем
похожие по фамилии, затем свободные ID с именами и группами из журнала. Занятые
другими учениками не предлагаются.

**Сброс пароля** (`POST /api/students/set-password`). PocketBase не даёт менять
пароль auth-записи обычным update — нужен `oldPassword` или права суперюзера,
поэтому операция живёт в серверном хуке. Пароль генерируется без похожих
символов (0/O, 1/l/I) — его диктуют вслух, — показывается один раз и пишется в
журнал действий. Ученику «без аккаунта» (`external`) тут же можно выдать
полноценный доступ одной кнопкой.

**Массовые операции.** В списке учеников появились отметки: выбрал несколько —
и одним движением перевёл в группу, сменил статус или передал коллеге. Перед
применением показывается, кого именно затронет, а кто уже и так такой.

**Удаление пустых аккаунтов** (`POST /api/students/delete`, только суперадмин).
`students.deleteRule` закрыт наглухо намеренно, поэтому удаление тоже идёт через
хук и только для аккаунта без единой связи — ни попыток, ни программ, ни курсов,
ни отметок посещаемости, ни членств. Если данные есть, сервер отказывает и
перечисляет, что мешает: такие аккаунты объединяют или помечают «выбыл».

Список учеников заодно стал группироваться по учебной группе (сущности), а не по
строке класса, и получил колонку меток: «выпустился», «без аккаунта», «нет TG» —
с фильтрами по ним.

Хук проверен на копии боевой базы: смена пароля с реальным входом ученика,
отказ при удалении аккаунта с 15 попытками, удаление пустого.

## [3.9.171] - 2026-09-08

### 🎓 Перевод на новый учебный год

Сентябрь — ребята переходят в следующий класс, кто-то заканчивает школу, и до
сих пор всё это делалось руками: перепривязать каждого ученика к новой группе,
а выпускников просто отвязать. Хуже того, связь «ученик ↔ группа» была ОДНА
(`students.teaching_group`) — она хранила только «где он сейчас», поэтому после
перевода прошлогодняя группа оставалась пустой: ни журнала сдачи, ни ростера
посещаемости, ни ответа на вопрос «кто у меня был в 10-м классе».

**Журнал членства.** Появилась коллекция `group_memberships` — строка на
(ученик, группа) с учебным годом, датами и статусом (`active` / `transferred` /
`graduated` / `left`). Состав группы теперь читается из неё, а
`students.teaching_group` остался указателем «где сейчас». Группа прошлого года
показывает свой прошлогодний состав, даже когда все её ученики уже в следующем
классе. Существующие связи перенесены миграцией.

**Статус ученика.** `students.status` (+ `grad_year`): выпускник не удаляется и
не «теряется» — карточка, попытки, результаты «Решу ЕГЭ» и летние программы
целы, он лишь пропадает из пикеров и активных списков (тумблер «выпускники» в
«Учениках»). В карточке ученика — кнопка «Выпустить» / «Вернуть в активные» и
секция **«История обучения»**: год → группа → чем закончилось.

**Мастер «Новый учебный год»** (`/app/groups/rollover`, кнопка в «Классах и
группах»): выбрать год → решить по каждой группе (перевести с повышением класса,
выпустить, не трогать) → пофамильно (перевести / выпустить / выбыл / оставить) →
применить. 11-й класс по умолчанию выпускается, курсы в ежегодный перевод не
втягиваются, названия групп предлагаются с повышением номера («10 кл» → «11 кл»).
Новая группа помнит предшественника (`teaching_groups.prev_group`) — получается
цепочка лет. Повторный запуск безопасен: уже созданная группа переиспользуется.

**Учебный год как измерение.** В «Классах и группах» — селектор года (по
умолчанию текущий, есть «Все годы»), так что список не растёт с каждым
сентябрём. Формат года и правило «с августа — новый учебный год» собраны в одном
месте (`utils/academicYear.js`).

Результаты «Решу ЕГЭ» переводом не затрагиваются вообще: внешний журнал живёт
на именах, а не на связях, — каникулярные задания можно спокойно доразобрать уже
после перевода.

Миграции: `1784300000_create_group_memberships`, `1784310000_add_year_rollover_fields`.

## [3.9.170] - 2026-09-07

### 🔻 Логотип в свёрнутом меню

В компактном режиме бокового меню (кнопка ⊟ в шапке) полоса сужается до 64 px,
а логотипом там оставалась широкая надпись «LEMMA» с пропорциями 5,5 : 1 —
ограничения по ширине у неё не было, и она вылезала за меню поверх шапки.

Теперь в свёрнутом виде показывается квадратная эмблема (`icon-new.png`), а сам
блок логотипа не может переполнить полосу: `max-width: 100%` + `overflow:
hidden`. Заодно перестала выпирать и развёрнутая надпись — при ширине меню
220 px она была на 20 px шире доступного места.

Ссылка на исходный код на самом логотипе (требование §13 AGPL) сохранена в обоих
режимах.

## [3.9.169] - 2026-09-07

### ✏️ Правка готового графика в конструкторе

Кнопки «График» и «Векторы» раньше только вставляли новый чертёж — уже
вставленный правился руками в DSL. Теперь, если курсор стоит внутри готового
блока ` ```plot ` (или inline `` `plot: …` `` в ячейке таблицы), та же кнопка
открывает конструктор **на правке этого блока**: поля подняты из разметки,
заголовок «Правка», кнопка «Сохранить» переписывает блок на месте, не трогая
остальной текст. Работает и в условии/решении задачи, и в редакторе теории.

Появилась секция **«Подпись»** — текст у координаты без кружка. Именно так
подписаны точки пересечения в большинстве задач (`label 0 2 2`), и до сих пор
конструктор такие подписи не показывал. У них те же направление и расстояние,
что у подписи точки.

Разбор блока ничего не теряет: отрезки, засечки осей и посторонние строки
(например, комментарии `#`) переписываются как есть, даже если у них нет своего
поля в конструкторе.

### 📌 Чертежи вставляются по курсору, а не в конец

График, векторы, числовая прямая и заготовки таблиц теперь встают **на место
курсора** — раньше они всегда дописывались в конец поля, и вставленное
приходилось вырезать и переносить руками в нужную ячейку.

Причина была в фокусе: клик по кнопке тулбара уводит его из поля, и «живого»
выделения в момент вставки уже нет. Теперь поле сообщает последнюю позицию
каретки наружу и она переживает потерю фокуса; выделенный кусок сниппет
заменяет. Если позиции нет или она осталась от другого текста (поле успели
переписать кнопкой LLM-фикса), вставка идёт в конец — как раньше.

После вставки курсор встаёт за чертежом, поэтому второй чертёж подряд не ложится
поверх первого. В редакторе теории вставка по курсору работала и до этого.

## [3.9.168] - 2026-09-07

### 📍 Подписи точек на графике можно отодвинуть

В конструкторе «График функции» подпись точки всегда стояла справа сверху от
кружка — и если через точку проходил график, буква или число оказывались прямо
на линии. Теперь у каждой точки два новых поля: **направление** (восемь сторон:
↖ ↑ ↗ → ↘ ↓ ↙ ←) и **расстояние** (0,5–5, шаг 0,5). Пересечение двух прямых —
случай, где заняты все четыре диагонали: там подпись отводится дальше от точки.

В DSL это модификатор команды `label`:

```
label 0 2 A at nw       — слева сверху, вплотную
label -3 0 −3 at s 1,5  — снизу, в полтора раза дальше
```

Понимает и человеческие синонимы (`at left`, `at up-right`). Без модификатора
всё рисуется как раньше — старые задачи и листы теории не меняются.

## [3.9.167] - 2026-09-06

### 🗂 Смешанная работа устного счёта: 4 варианта на листе A4

В смешанной работе раскладку задавали два чекбокса — «2 варианта рядом» и
«2 варианта верх/низ», третьего варианта не было. Теперь там тот же
переключатель **«Вариантов на листе»**, что и в обычном устном счёте:
**1 · 2 ↔ · 2 ↕ · 4 · 6 · 8**. Четыре варианта встают сеткой 2×2 с линиями реза
— лист режется на четыре карточки.

Ячейки и стили общие с устным счётом, поэтому четверть листа выглядит одинаково
в обоих генераторах. Заголовки разделов («Устный счёт», «Дроби», «Уравнения»)
в мелких сетках ужимаются, а подсказка о вместимости учитывает их наравне с
заданиями — в четверть листа со свободным местом помещается около двадцати
строк на все разделы вместе.

Сохранённые листы со старой парой флагов читаются по-прежнему: раскладка
восстанавливается из `sideBySide` / `twoPerPage`.

## [3.9.166] - 2026-09-06

### 🔢 Надпись «Вариант N» на листе задач — тумблером

В «Генераторе», в «Оформлении» → «Лист», появился переключатель **«Номер
варианта»**. Раньше надпись жила по жёсткому правилу движка: есть несколько
вариантов — печатается, вариант один — нет, и повлиять на это было нельзя.

Теперь тумблер показывает фактическое состояние листа и переключает его в обе
стороны: добавить «Вариант 1» на одиночный лист или убрать нумерацию с
комплекта вариантов. Нетронутый тумблер сохраняет прежнее авто-поведение.

Действует сразу на шапку, колонтитул страниц 2+ и ключ учителя. В ключе при
нескольких вариантах заголовки остаются всегда — без них не разобрать, чьи это
ответы. Поле «Название варианта» показывается, только когда надпись включена.

Во входной контрольной работе ничего не изменилось: её собственный флаг
`alwaysShowVariant` работает как прежде.

## [3.9.165] - 2026-09-06

### 〰️ Числовая прямая без подписей координат

В конструкторе числовой прямой (режим «Неравенства (штриховка)») появился
переключатель **«Подписи координат»**. Выключенный — точки, лучи и штриховка
рисуются как обычно, но чисел под осью нет. Нужно для заданий вида «на рисунках
изображены решения квадратных неравенств — определите знаки коэффициентов»: по
подписанным границам ответ читается сразу, задача теряет смысл.

По умолчанию подписи включены — прежние прямые не меняются.

В DSL это команда `nolabels` (синонимы `labels off` / `labels on`), поэтому
работает и в блоке ```numline, и в inline-форме `numline: …` для ячеек таблиц.
Без подписей холст ниже на 12 px, ось остаётся на прежнем уровне — прямая с
подписями и без них в одной таблице стоят ровно.

### 〰️ Вся прямая целиком — и никаких точек из ниоткуда

Кнопка **«Вся прямая»** в списке фигур: штриховка от края до края, без точек —
случай «решение — любое число» (`x² − 4x + 9 > 0`). Обратный случай, «решений
нет», рисуется пустой прямой: достаточно удалить все фигуры. В DSL — команда
`all`.

Заодно исправлено: **точка за пределами диапазона оси больше не рисуется**.
Раньше её прижимало к краю, и на прямой появлялся кружок из ниоткуда — при
`domain 0 3` точка 7 садилась на конец оси. Штриховки это не касается: луч,
начинающийся за границей, по-прежнему заштрихован до самого края.

## [3.9.164] - 2026-09-06

### 🔗 Объединение аккаунтов учеников переносит всё, а не только попытки

Ученик забыл пароль, завёл второй аккаунт — старый механизм слияния
(`/api/students/merge`) переносил на новый аккаунт **только `attempts`**, а
удаление старого при этом срывалось: `study_programs.student` — обязательная
связь, PocketBase такую запись удалить не даёт. Попытки при этом уже были
перенесены (транзакции не было), и учитель получал полу-слитое состояние:
ошибка на экране, старый пустой аккаунт в базе, каникулярная программа
осталась на нём. Проверено на копии боевой БД — воспроизводится дословно.

Хук переписан:

- **Переносится всё, что ссылается на ученика** — `attempts`, `study_programs`
  (летние и каникулярные задания), `course_members`, `lesson_attendance`,
  `teacher_todos`. Записи с cascadeDelete (курсы, посещаемость) раньше просто
  удалялись вместе с аккаунтом.
- **Пустые поля профиля** целевого аккаунта дополняются донорскими: группа,
  класс, telegram, владелец. Самостоятельно заведённый аккаунт всегда без
  группы — без этого ученик выпадал из журнала и аналитики.
- **Одна транзакция** — при любой ошибке не меняется ничего.
- **Логин и пароль донора можно перенести** на оставшийся аккаунт
  (`keepCredentials`): сливаем новый аккаунт в старый (там группа, история и
  выданные программы), а ученик продолжает входить теми данными, которые
  помнит. Хеш пароля переносится SQL-запросом внутри транзакции.
- **Требуется авторизация учителя** — раньше ручка была открыта анониму.
  Свои ученики проверяются по `owner` (superadmin — все).
- `dryRun` возвращает сводку без изменений; результат пишется в `audit_log`.

В «Прогрессе учеников» модалка объединения показывает **живую сводку с сервера**
(что именно переедет и каким логином ученик будет входить), галочку «перенести
логин и пароль» и список **найденных дублей** — аккаунты с одинаковым именем,
где порядок слов не важен («Яна Сергеева» = «Сергеева Яна»), с кнопкой
подстановки пары.

### 📋 Результаты работы: режим «Все выдачи»

Летняя/каникулярная программа создаёт **персональную выдачу каждому ученику**,
поэтому у одной работы легко полтора десятка `work_sessions`, а вкладка
«Результаты» показывала попытки ровно одной из них. Ученик писал по своей
ссылке — учитель открывал работу и видел чужой результат или пустоту, хотя в
карточке ученика попытка была.

- В селекторе выдач появился пункт **«Все выдачи (N)»**, и он выбран по
  умолчанию, когда выдач больше одной.
- Выдачи подписаны адресатом — «Выдача 4 · Дрибинская Ксения» вместо
  неразличимых дат (связь берётся из `study_program_items`).
- В режиме «все выдачи» у таблицы появляется колонка «Из выдачи», а «выдать
  другой вариант» работает в той сессии, где ученик писал.

## [3.9.163] - 2026-09-04

### 📉 Генератор квадратных неравенств

Новый раздел меню «Уравнения → Квадратные неравенства»
(`/app/equations/quadratic-inequalities`). **38 категорий в семи блоках**:

| Блок | Про что | Примеры |
|---|---|---|
| 1. Простейшие (устно) | неполные, разложенные, сравнение с нулём — 10 типов | `x² > 9` · `x² − 7 ⩽ 0` · `x² > 0` · `x² > −4` · `3x² ⩾ 27` · `x² + 8x ⩽ 0` · `−2x² − 15x > 0` · `9 − x² < 0` · `(x − 3)² > 0` · `(x − 2)(x + 5) < 0` |
| 2. Приведённые (D > 0) | корни подбираются устно | `x² − 7x + 12 > 0` · `x² + 12x + 20 ⩾ 0` · `x² + x − 12 < 0` · `−x² + 5x − 6 > 0` |
| 3. Полные (a ≠ 1) | дробные границы, десятичные и дробные коэффициенты | `2x² − 7x + 3 ⩽ 0` · `6x² − 5x + 1 > 0` · `0,5x² − 2x + 1,5 < 0` |
| 4. Особые случаи (D ⩽ 0) | вся таблица вырождений | `x² − 16x + 64 > 0 → x ≠ 8` · `… ⩾ 0 → x ∈ ℝ` · `x² + 2x + 5 > 0` · `−x² + 2x − 5 < 0` |
| 5. Иррациональные границы | ответ с радикалом | `x² − 5x + 3 > 0` · `x² − 4x + 1 ⩽ 0` · `x² − 4√3·x + 9 ⩽ 0` |
| 6. Приведение к квадратному | скобки, обе части, биквадратные | `(x − 1)(x + 4) > 6` · `(x + 3)² ⩾ 9x + 15` · `3x² + 2x < x² + 8` · `x⁴ − 5x² + 4 < 0` |
| 7. Другие постановки | ответ — не промежуток | «сколько целых решений» · «наименьшее целое» · «наибольшее целое» · область определения `√(x² − 4x + 3)` |

**Ответ — множество промежутков, а не «случай».** У квадратного неравенства
ответом бывает интервал, объединение двух лучей, точка, вся прямая, пустое
множество и «всё, кроме точки». Все они описаны одним списком промежутков
(`utils/quadraticInequality.js`), поэтому биквадратное неравенство с четырьмя
границами ложится туда же — `x⁴ − 5x² + 4 ⩾ 0` честно отвечает
`(−∞; −2] ∪ [−1; 1] ∪ [2; +∞)`. Границы живут в поле `Q(√m)`, так что
`x² − 7 ⩽ 0` отвечает `[−√7; √7]`, а не десятичным приближением.

**Разворот знака — в одном месте.** Знак старшего коэффициента переворачивает
неравенство внутри решателя, а не в категориях: автор категории не может об
этом забыть. Дальше работает школьная таблица «парабола ветвями вверх», а для
нескольких корней — метод интервалов (знак чередуется через каждый корень).

**Проверка численная и независимая.** Перед выдачей задания ответ сверяется с
самим неравенством в пробных точках вокруг каждой границы: «точка принадлежит
ответу» обязано совпадать с «неравенство верно». Ошибка в знаке или в строгости
границы до листа не доезжает.

**Настройки листа**: «Числа» (лёгкий/средний/сложный — размах внутри того же
приёма), «Знаки» (любые / только строгие / только нестрогие), «Границы»
(любые / без радикалов / целые) и форма ответа — промежутками `[−3; 5]` или
неравенством `−3 ⩽ x ⩽ 5`.

Генератор поддержан общей обвязкой листов: план листа с чертой и перестановкой,
квоты типов, 1/2/4/6/8 вариантов на A4, лист ответов учителя, сохранение листа
(`generator_sheets`), тест A/B/C/D, раздел смешанной работы.

### ♻️ Общие заготовки квадратных заданий

Уровни сложности, выбор корней и сборка `A·x² + B·x + C` по корням переехали из
хука уравнений в `utils/quadraticForms.js` — форма задания от знака между
частями не зависит, и оба генератора теперь строят задания одним кодом.

Покрыто 28 юнит-тестами (всего 900): независимый разбор напечатанного
неравенства с проверкой в пробных точках, границы как настоящие корни, таблица
вырожденных случаев, метод интервалов на четырёх корнях, обе формы записи
ответа, сверка счётных вопросов с честным перебором целых.

## [3.9.162] - 2026-09-04

### 📐 Генератор квадратных уравнений

Новый раздел меню «Уравнения → Квадратные уравнения»
(`/app/equations/quadratic`). **40 категорий в шести блоках** по нарастанию
сложности — тема закрыта целиком, от устного счёта до теоремы Виета «без
решения»:

| Блок | Про что | Примеры |
|---|---|---|
| 1. Простейшие (устно) | неполные и свёрнутые квадраты, 11 типов | `x² = 49` · `7x² = 0` · `2x² = 18` · `x² = 7` · `x² = −7` · `x² + 2 = 0` · `3x² − 27 = 0` · `x² − 5x = 0` · `(x + 2)² = 4` · `(2x − 3)² = 0` · `x² + 2x + 1 = 0` |
| 2. Приведённые: Виета | корни подбираются устно | `x² − 5x + 6 = 0` · `x² + 7x + 10 = 0` · `x² + x − 12 = 0` · `x² − 23x + 132 = 0` |
| 3. Полные: дискриминант | 9 типов, включая D = 0 и D < 0 | `2x² − 7x + 3 = 0` · `6x² − 5x + 1 = 0` · `−x² + 5x − 6 = 0` · `0,5x² − 2x + 1,5 = 0` |
| 4. Иррациональности | корень в ответе и в коэффициентах | `x² − 4x + 1 = 0 → 2 ± √3` · `x² − 4√3·x + 9 = 0` · `√2·x² − 3x + √2 = 0` |
| 5. Приведение к квадратному | скобки, биквадратные, дробные | `(x − 1)(x + 4) = 6` · `(x + 3)² = 9x + 7` · `x⁴ − 5x² + 4 = 0` · `x + 6/x = 5` |
| 6. Виета без решения | ответ — не корни | `x₁ + x₂` · `x₁ · x₂` · `x₁² + x₂²` · `1/x₁ + 1/x₂` · составить уравнение по корням |

**Растяжка по сложности — два независимых регулятора.** Блоки задают приём
(что ученик должен уметь), переключатель «Числа: лёгкий / средний / сложный» —
размах корней и коэффициентов внутри того же приёма. Плюс «Корни: любые / без
корней √ / целые» — тот же лист можно выдать и восьмому классу, и одиннадцатому.

**Ответ не может разойтись с условием.** Уравнение почти нигде не собирается из
случайных коэффициентов: сначала выбираются корни, затем из них считаются
коэффициенты, и только потом дерево узлов печатается в LaTeX. Корни живут в
поле `Q(√m)` — числа вида `p + q√m` (новый модуль `utils/surd.js`), поэтому
одним механизмом описываются и «x² − 5x + 6 = 0 → 2; 3», и
«x² − 4√3·x + 9 = 0 → √3; 3√3». Категории, где интереснее именно случайные
коэффициенты («нет корней», иррациональный ответ), наоборот решаются честно
через дискриминант. Перед выдачей каждое задание проверяется подстановкой:
заявленный корень обязан обратить напечатанное уравнение в ноль.

**Оформление ответа**: `±7` для симметричных корней, `2 ± √3` и
`(5 ± √13)/2` — для сопряжённых одной записью, знак пустого множества — когда
корней нет; переключатель «−3; 5» / «x₁, x₂». Одинаковые уравнения на листе
отклоняются — при малом пуле корней они выпадали по два-три раза.

Генератор поддержан всей общей обвязкой листов: план листа с чертой и
перестановкой заданий, квоты типов, 1/2/4/6/8 вариантов на A4, лист ответов
учителя, сохранение листа (`generator_sheets`), тест A/B/C/D и «вписать ответ»,
раздел смешанной работы.

Покрыто 29 юнит-тестами (всего 871): независимый разбор напечатанного LaTeX с
подстановкой корней, сверка ответа с корнями, сканирование «нет корней» по
диапазону, арифметика `Q(√m)`, настройки и полнота реестра категорий.

## [3.9.161] - 2026-09-03

### 🗜 Шесть и восемь вариантов на листе A4 + подсказка вместимости

**Переключатель «Вариантов на листе» дополнен значениями 6 и 8** — та же сетка
в две колонки, но в три и четыре ряда. Ячейка выходит 105×99 мм и 105×74 мм.
Три колонки не делали намеренно: ячейка сузилась бы до 70 мм, и строка
«задание + место для ответа» перестала бы помещаться.

**Сколько реально влезает.** Замерено настоящей печатью (headless Chrome
`--print-to-pdf` по боевому CSS), интервал 1 / 1,5:

| Вариантов на листе | Ячейка | Заданий при интервале 1 | при 1,5 |
|---|---|---|---|
| 4 (2×2) | 105×148,5 мм | 20 | 12 |
| 6 (2×3) | 105×99 мм | 12 | 7 |
| 8 (2×4) | 105×74 мм | 9 | 5 |

То есть лист по 5 заданий спокойно печатается и вшестером, и ввосьмером —
ради этого всё и затевалось.

**Подсказка под переключателем** — «В ячейку влезает ~N заданий», а если
заданий в варианте больше — предупреждение красным. Раскладка переполнение не
режет (`overflow: hidden` прятал бы задания молча), поэтому лишние строки
выдавливают вариант на второй лист — теперь об этом видно заранее. Расчёт —
`utils/sheetCapacity.js`, чистая функция без DOM: геометрия ячейки минус шапка,
делённая на высоту строки. Её значения сверены с печатью и закреплены тестом.

### 🎚 Подписи ползунка интервала не вылезают за панель

«Плотно» и «Просторно» стоят на краях шкалы, а Ant центрирует подпись под
точкой (`translateX(-50%)`) — крайние слова торчали за блок настроек. Теперь
первая метка прижата к левому краю ползунка, последняя — к правому, шрифт
меток уменьшен, у блока появились боковые поля.

Покрыто 9 юнит-тестами (всего 842): значения переключателя и чтение старых
ключей, калибровка вместимости, рендер сеток 2×3 и 2×4 с переносом остатка на
второй лист, CSS-сторожи (ряды сетки, линии реза у последнего ряда) и сторож
сдвига крайних меток.

## [3.9.160] - 2026-09-03

### 💾 Сохранённые листы генераторов — новая сущность

Удачно сгенерированный лист теперь можно сохранить и вернуться к нему: раньше
он жил только до перезагрузки страницы, а «Сформировать» собирало другие числа.

**Что это за сущность.** Коллекция `generator_sheets` (миграция `1784200000`) —
**лист целиком одной записью**: тип генератора, настройки, порядок заданий и
снимок всех вариантов. В банк задач (`tasks`) при сохранении не пишется ничего:
задание генератора («2x = 18», «sin 30°») не самостоятельно — у него нет темы,
кода, разбора, статистики решаемости и вектора, и в каталоге экзаменационных
задач оно было бы мусором (так уже вышло с 10 тыс. задач `source='trig_generator'`
от тестов A/B/C/D). Хранится снимок, а не рецепт: генераторы не seeded, а ручные
правки заданий больше нигде не живут.

Обобщает прежние частные коллекции листов (`unit_circle_worksheets`,
`trig_values_worksheets` — там та же пара «настройки + задания», но по коллекции
на генератор); их не трогали, у них своё сохранение работает как раньше.

**Где кнопки.** В каждом из **18 листовых генераторов** (устный счёт ×6,
уравнения и оба неравенства, 7 тригонометрических, обе смешанные работы) в блоке
действий появились **«Сохранить лист»** и **«Загрузить лист»**. У сохранённого
листа кнопка становится «Обновить «название»» плюс «Сохранить как новый».
При сохранении задаются название, папка-ярлык, класс, заметка и закрепление —
как у работ. В окне загрузки виден список листов этого генератора, тумблер
«показать листы всех генераторов» уводит на нужную страницу.

**Где список.** В «Моих работах» — четвёртая вкладка **«Листы генераторов»**:
поиск, фильтры по генератору и папке, закрепление, правка названия/папки/заметки,
удаление. Клик открывает лист в том генераторе, который его сделал
(`?sheet=<id>` — ссылку можно положить в закладки).

**Правка заданий на месте.** У каждой позиции листа появилась кнопка ✏️
(в панели «Порядок заданий», а у тригонометрических — в новой панели «Задания»).
Она открывает окно с этим заданием **во всех вариантах сразу**: правится LaTeX
условия и ответа с живым превью, кнопкой 🎲 задание перебирается заново (той же
категории — иначе позиция разошлась бы с другими вариантами), «Заново во всех
вариантах» перебирает строку целиком, «Убрать с листа» удаляет позицию везде.
Рядом — «Своё задание»: вписанное руками встаёт в конец одинаковым во всех
вариантах. Правки видны на листе сразу и сохраняются вместе с ним.

**Грабли.** Правка меняет снимок заданий, а порядок листа пересчитывается на
каждый новый снимок — без переноса расстановки лист «распрямлялся» бы от каждой
опечатки. Поэтому правка и загрузка отдают порядок вместе с данными
(`useSheetLayout.apply`), а сохранённый порядок с устаревшими номерами чинится,
а не роняет лист. Удаление задания считает конечный порядок один раз: сложение
двух путей (`apply` + `removeTask`) срезало позицию дважды — поймано тестом.

Покрыто 21 юнит-тестом (всего 833): операции над снимком, перенос порядка при
загрузке и правке, перегенерация одного задания нужной категории, сторож
маршрутов реестра (пути продублированы строками, потому что `utils/` не может
импортировать `R` из `App.jsx`).

**Не делали намеренно:** онлайн-выдачи листа ученикам нет — для этого задания
пришлось бы материализовать в `tasks`; отложено отдельным заходом.

**Развёртывание.** Миграция `1784200000` (новая коллекция, существующие данные
не трогает) применена на ПРОД 03.09.2026 после внепланового бэкапа
`backup_2026-09-03_22-42-44`: файл в `/opt/pocketbase/pb_migrations/` +
`systemctl restart pocketbase-ege`; коллекция и правила доступа проверены.
**Деплой фронта выполнен 03.09.2026:** `./deploy-raspberry.sh --build`, в проде
`l.oipav.ru` версия `3.9.160-20260903194729` (health-check совпал).
`pdf-service-ege` не перезапускался — серверная часть фичи не нужна.

## [3.9.159] - 2026-09-03

### 🧩 Порядок заданий вручную, разделительная черта и 4 варианта на листе

Три изменения в генераторах листов устного счёта, уравнений и неравенств
(9 генераторов на общей раскладке).

**1. Задания одного типа стоят на одинаковых местах во всех вариантах.**
Раньше варианты набирались независимо и при отбраковке «съезжали»: в первом
варианте №5 — дробь, во втором — скобки. Теперь сначала строится **план листа**
(какая категория на какой позиции), и все варианты идут по нему. Лист можно
раздать по рядам и разбирать у доски по номерам, а столбец ответов у учителя
однороден. Логика — `utils/questionPlan.js`; на неё переведены все девять
генераторов, попутно ушло дублирование кода генерации в хуках.

**2. Панель «Порядок заданий» — перетаскивание и черта.** Появляется после
«Сформировать». Строку можно перетащить мышью или подвинуть стрелками ↑/↓, а
кнопка **«Черта»** вставляет горизонтальную линию — чтобы отделить задания
посложнее и увести их ниже. Порядок и черта общие для всех вариантов (типы
заданий синхронизированы), поэтому линия проходит через лист в одном месте.
Перестановка меняет только вид листа — задания не перегенерируются.
Компоненты: `hooks/useSheetLayout.js` + `trig/SheetOrderPanel.jsx`.

**3. Режим «4 варианта на листе A4»** — сетка 2×2 с пунктирными линиями реза,
уменьшенной шапкой и одной колонкой заданий. Квадрант — ячейка сетки (на экране
у него снята ширина листа 210 мм и рамка, иначе четверти расползались), строка
задания плотнее обычной: в 105×148,5 мм помещаются 20 заданий устного счёта при
интервале 1. Переполнение не прячется `overflow: hidden` — иначе лишние задания
исчезали бы с листа молча. В блоке «Печать и вид» два старых
чекбокса («2 рядом», «2 верх/низ») заменены переключателем **«Вариантов на
листе: 1 · 2 ↔ · 2 ↕ · 4»** (ключ `variantsPerPage`; старые ключи пишутся
следом, поэтому сохранённые настройки и смешанные работы не ломаются).

Покрыто 30 юнит-тестами (всего 812): план категорий и подстановка запасной
категории, синхронность типов по позициям в уравнениях и устном счёте,
операции плана листа (перенос, черта, сброс), печать по плану (черта не
сбивает нумерацию, порядок соблюдается) и сетка 2×2 — включая сторожей самого
CSS: ширина квадранта, две колонки на две строки, линии реза и отсутствие
`overflow: hidden` (jsdom импортированный CSS не применяет, поэтому геометрия
сторожится по исходнику — тот же приём, что в `entranceTest.test.jsx`).

**4. Место для ответа выключается.** В «Печать и вид» добавлен переключатель
**«Место для ответа в строке»**: с ним лист печатает только условия — без
«= ____», «x =» и «Ответ:», ученик решает в тетради. Работает во всех
раскладках, у тригонометрических это прежний режим `plain`. Лист ответов для
учителя по-прежнему отключается своим чекбоксом «Лист ответов (учитель)».

**5. Сколько заданий каждого типа.** У каждой категории в блоке появилось поле
количества: «пропорций — 3, остальное как выйдет». Пустое поле = прежнее
поведение (категории делят задания поровну). Типы при этом чередуются, а не
идут группой: план раскладывает их по кругу, пока у каждого не кончится своя
квота. Если сумма квот больше ползунка «Заданий в варианте», лист станет
длиннее — подпись у ползунка показывает фактическое число. Компонент
`trig/CategoryChecklist.jsx`, раскладка квот — `utils/questionPlan.js`.

**6. Починен межстрочный интервал.** Ползунок задавал строке только
`min-height`, а строка с дробью (`x/2 − x/3`) и так выше этого минимума —
поэтому на листах с дробями он почти ничего не менял, а «Плотно» не работало
вовсе. Теперь интервал добавляет зазор ПОВЕРХ высоты формулы
(`margin-bottom: calc((scale − 1) × 6mm)`, в квадранте 3,5 мм): шаг ползунка
0,25 = 1,5 мм на строку, при масштабе 1 добавка нулевая (лист выглядит как
раньше), при «Плотно» строки поджимаются. Исправлено во всех раскладках —
устный счёт, смешанная работа, тригонометрия.

**7. Шаг числа заданий — 1.** Ползунок «Заданий в варианте» в девяти
генераторах шёл с шагом 5 (5 · 10 · 15 …) — теперь 2…30 с шагом 1.

**Пока не охвачено:** тригонометрические генераторы (своя раскладка
`TrigExprPrintLayout`) и обе смешанные работы — там прежние настройки листа.

**Деплой выполнен 03.09.2026:** `./deploy-raspberry.sh --build`, в проде
`l.oipav.ru` версия `3.9.159-20260903143534` (health-check совпал). Этим же
деплоем уехали 3.9.157 и 3.9.158. Серверная часть и БД не менялись —
`pocketbase-ege` и `pdf-service-ege` не перезапускались, миграций нет.

## [3.9.158] - 2026-09-03

### ⩽⩽ Двойные неравенства: третий генератор раздела «Уравнения»

`/app/equations/double-inequalities` — задания вида `lo ⩽ f(x) < hi` с той же
линейкой усложнений, что на бумажном листе: от `−10 ⩽ 5x < 5` до
`−1 ⩽ (6 + 2x)/4 ⩽ 4`. **12 категорий в четырёх блоках:**

- **Блок 1. ax в середине** — целый коэффициент (`−10 ⩽ 5x < 5`), десятичные
  границы (`−12 ⩽ 3x ⩽ 3,6`), отрицательный коэффициент (`−18 ⩽ −6x ⩽ 12`).
- **Блок 2. Со сдвигом** — `−4 ⩽ x + 3 < 5` · `−7 ⩽ 2x + 1 ⩽ 5` ·
  минус перед переменной `−8 ⩽ 3 − 2x ⩽ 7`.
- **Блок 3. Дроби** — `0,5 < x/2 < 4,5` · `−1 ⩽ (6 + 2x)/4 ⩽ 4` ·
  дробный коэффициент `−2 ⩽ ⅔x ⩽ 4`.
- **Блок 4. Скобки** — `−6 ⩽ 2(x − 1) < 10` · `−5 < 3(x + 2) − 4 ⩽ 10` ·
  дробь с минусом `−3 ⩽ (5 − 2x)/3 < 1`.

Ответ — «`−2 ⩽ x < 1`» или промежуток «`[−2; 1)`»; настройки те же, что у
линейных неравенств (вид границы, только строгие знаки, только целые границы,
буквы переменной) плюс общие настройки листа. По умолчанию одна колонка на
листе — строки длиннее обычных.

**Как строится.** В отличие от линейных неравенств, задание строится не от
корня, а **от границ**: слева и справа стоят круглые числа, а решение уже может
быть дробным (`−17 ⩽ 2x` → `x ⩾ −8,5`) — как в тетради. Границы условия всегда
целые или короткие десятичные; часть заданий при этом подбирается от целых
решений, иначе на составных серединах (`3(x + 7) + 12`) почти всегда выходили
ответы вида −49/3. Деление на отрицательный коэффициент (перестановка границ
местами) делает общий решатель.

**Общее ядро неравенств** вынесено в `utils/inequalityCore.js` (знаки `OPS`,
`solveSimple`/`solveDouble`, запись ответа) — им пользуются оба генератора
неравенств. Категория `doubleIneq` в генераторе линейных неравенств осталась
как есть: там это одна из категорий блока «особые случаи», здесь — отдельный
лист с разбором по типам.

Покрыто 12 юнит-тестами (всего 782): множество решений сверяется с напечатанным
неравенством пробными точками, проверяется форма условия (три части, оба знака
«меньше»), запись ответа обеими формами и разворот при отрицательном
коэффициенте.

**Деплой:** уехало на прод в составе 3.9.159 (03.09.2026).
Серверная часть и БД не менялись.

## [3.9.157] - 2026-09-03

### 🖨 Настройки листа во всех генераторах: шапка, «Класс», межстрочный интервал

В каждом генераторе рабочих листов в блоке «Печать и вид» появились общие
переключатели того, что печатать в шапке, и ползунок межстрочного интервала:

- **Шапка: вариант, ФИО, дата** — убирает верхнюю строку целиком (лист без
  полей для подписи, например для работы в тетради).
- **Поле «Класс»** — убирает только «Класс: ______», остальная шапка остаётся
  (когда занимаетесь с одной группой, колонка лишняя).
- **Название листа** и **строка задания** («Вычислите:», «Решите неравенство:») —
  выключаются по отдельности.
- **Межстрочный интервал** — ползунок 0,75–2,5 («Плотно» … «Просторно»).
  Задаёт, сколько места остаётся ученику под каждым заданием: множитель уходит
  в CSS-переменную `--sheet-line-scale`, на которую завязаны отступы и высота
  строки задания в печати и в предпросмотре.

Настройки одинаковы во **всех 18 генераторах** — устный счёт (арифметика,
десятичные, дроби, степени и корни, логарифмы, степени и логарифмы), уравнения
и неравенства, тригонометрия (выражения, значения функций, обратные функции,
уравнения простейшие и f(kx+b)=a, формулы приведения и сложения, двойной
аргумент) и обе смешанные работы. У листа значений функций переключателя
инструкции и ползунка интервала нет — там таблица, а не строки заданий.

**Как устроено.** Общий модуль `components/trig/sheetOptions.jsx`:
`SHEET_DEFAULTS` + `sheetOptions(settings)` (отсутствующий ключ = прежнее
поведение, поэтому хуки генераторов и `reset()` не трогали) + UI-блок
`SheetLayoutOptions`. Поддержка добавлена в пять печатных раскладок:
`OralCountingPrintLayout`, `OralMixedPrintLayout`, `TrigExprPrintLayout`,
`TrigMixedPrintLayout`, `TrigValuesPrintLayout`. Новая раскладка → читает
настройки через `sheetOptions` и вешает `sheetSpacingStyle` на свой корень.

Покрыто 10 юнит-тестами (всего 770): дефолты и зажим интервала в границах,
а также рендер раскладок — что скрывается ровно то, что выключили, и что
множитель интервала доезжает до корня листа.

**Деплой:** уехало на прод в составе 3.9.159 (03.09.2026).
Серверная часть и БД не менялись.

## [3.9.156] - 2026-09-03

### 🧱 Уравнения — те же пять блоков, что и у неравенств

Генератор линейных уравнений перестроен по образцу неравенств: **пять блоков по
нарастанию сложности**, у каждого блока свой чекбокс (включается и выключается
целиком). Было 14 категорий в трёх группах — стало **20 в пяти блоках**:

- **Блок 1. Простейшие (устно)** — без изменений (7 категорий, `ax = b`).
- **Блок 2. Два шага и подобные** — подобные слагаемые, дробные подобные,
  `5x − 7 = 8`, переменная в обеих частях.
- **Блок 3. Скобки** — `2(x − 3) = 10` · **новое:** скобки с двух сторон
  `3(x − 1) = 2(x + 4)` · две скобки `2(x − 1) − 3(x + 2) = 4`.
- **Блок 4. Дроби** — `(x + 1)/3 = 2` · **новое:** разные знаменатели
  `x/2 − x/3 = 1` · дроби с двух сторон `(2x − 1)/4 = (x + 3)/2` · пропорция.
- **Блок 5. Особые случаи (новый)** — «нет корней» (`2(x + 6) = 2x − 5`) и
  «любое число» (`6(y − 8) = 6y − 48`). Ответы печатаются как `∅` и `x ∈ ℝ`.

По умолчанию включены блоки 1–3 (как в неравенствах). В листе учителя у особых
случаев не печатается префикс «x =» (иначе выходило бы «x = ∅»), а в листе
ученика подсказка у всех заданий одинаковая — иначе особый случай выдавал бы
себя до решения.

**Общие формы.** Выражения блоков «скобки», «дроби» и «особые случаи» вынесены
в `utils/linearForms.js`: форма строит пару сторон под заданный корень, а знак
между ними ставит вызывающий — «=» для уравнений, «<» / «⩽» для неравенств.
Оба генератора переведены на эти формы, так что одинаковые по виду задания в
уравнениях и неравенствах действительно одинаковые. Переключатель блока —
общий компонент `TrigBlockToggle` в `trig/TrigGeneratorLayout`.

Задание уравнения теперь несёт структуру решения `q.solution`
(`root | empty | all`) — как у неравенств; по ней строится ответ и её проверяют
тесты. Тестов стало 16 (всего 760): численная проверка корня пропускает
вырожденные случаи, а для них проверяется, что уравнение не выполняется ни при
каком значении (`empty`) либо выполняется при всех (`all`).

**Деплой выполнен 03.09.2026:** `./deploy-raspberry.sh --build`, в проде
`l.oipav.ru` версия `3.9.156-20260903102446` (health-check совпал). Этим же
деплоем на прод уехали 3.9.154 и 3.9.155. Серверная часть и БД не менялись —
`pocketbase-ege` и `pdf-service-ege` не перезапускались, миграций нет.

## [3.9.155] - 2026-09-03

### ⩽ Линейные неравенства: пять блоков по нарастанию сложности

В разделе «Уравнения» появился второй генератор — **«Линейные неравенства»**
(`/app/equations/inequalities`). Устный уровень тот же, что у уравнений, но
дальше идут блоки посложнее: скобки, дроби и особые случаи.

**18 категорий в пяти блоках** (по умолчанию включены блоки 1–3):

- **Блок 1. Простейшие (устно)** — `3x > 18` · `−4x > 12` (знак разворачивается)
  · `0,5x < 2,5` · `⅔x ⩽ 4` · `x + 5 > 12` · `18 < 3x`.
- **Блок 2. Два шага** — `3x − 7 > 8` · `0,4x + 0,6x ⩾ 2` · `5x − 3 > 2x + 9`.
- **Блок 3. Скобки** — `2(x − 3) > 10` · `3(x − 1) > 2(x + 4)` ·
  `2(x − 1) − 3(x + 2) ⩾ 4`.
- **Блок 4. Дроби** — `(x + 1)/3 ⩽ 2` · `x/2 − x/3 < 1` · `(2x − 1)/4 > (x + 3)/2`.
- **Блок 5. Особые случаи** — «нет решений» (`2(x + 1) > 2x + 5`), «любое число»
  (`3(x − 2) ⩽ 3x + 1`) и двойное неравенство (`−3 < 2x − 1 ⩽ 7`).

У каждого блока в панели свой чекбокс — блок включается и выключается целиком,
не по одной категории.

**Настройки:** форма ответа — «x > 5» или промежуток «(5; +∞)» (скобка круглая
у строгого знака, квадратная у нестрогого); вид границы (авто / обыкновенная
дробь / десятичная); «только строгие знаки < >»; «только целые границы»; буквы
переменной; число заданий и вариантов; печать как у остальных генераторов.
Ответы «нет решений» и «любое число» печатаются как `∅` и `x ∈ ℝ`.
Раздел добавлен и в «Смешанную работу» устного счёта.

**Общее ядро.** Рациональная арифметика и дерево выражения вынесены из
генератора уравнений в `utils/linearExpr.js` — уравнения и неравенства строят
условие из одного дерева, из него же считаются коэффициенты и ответ.
Разворот знака при делении на отрицательное делает решатель, а не автор
категории, поэтому ошибиться в нём в отдельной категории невозможно.

**Печать** (`OralCountingPrintLayout`, `OralMixedPrintLayout`) получила пропсы
`promptMode` (`eq` — «=», `var` — «x =», `answer` — «Ответ:») и `instruction`.
Прежнее поведение по `equationMode` сохранено.

**Код:** `hooks/useLinearInequalities.js`, `components/LinearInequalitiesGenerator.jsx`,
`utils/linearExpr.js`, запись в `hooks/oralMixedRegistry.js`. Покрыто 14
юнит-тестами (всего 758): множество решений сверяется с напечатанным
неравенством подстановкой пробных точек через независимый вычислитель LaTeX —
именно этот тест поймал двойной разворот знака в двойных неравенствах.

**Деплой:** уехало на прод в составе 3.9.156 (03.09.2026).
Серверная часть и БД не менялись.

## [3.9.154] - 2026-09-03

### ➗ Новый раздел «Уравнения» + генератор линейных уравнений

Появился отдельный раздел меню **«Уравнения»** (секция `equations`) — под
генераторы уравнений, которые не относятся ни к тригонометрии, ни к устному
счёту. Первый жилец — **«Линейные уравнения»** (`/app/equations/linear`).

Генератор собран по образцу листка из тетради (простейшие `ax = b`), но
расширен дробями, отрицательными числами и уравнениями в несколько шагов.
**14 категорий в трёх группах:**

- **Простейшие `ax = b`** — целый коэффициент (`3x = 18`), дробный корень
  (`2x = 5`), десятичный коэффициент (`1,2x = 2,4`), обыкновенная дробь как
  коэффициент (`2/3·x = 5`), дробь справа (`9y = 2/3`), число слева (`64 = 8x`),
  отрицательные числа (`-1,3y = 2,6`).
- **Приведение подобных** — `0,2x + 0,7x = 0,18`, дробный вариант `2/3·x + 1/6·x = 5`.
- **Усложнения** — два шага (`5x - 7 = 8`), переменная в обеих частях
  (`5x - 3 = 2x + 9`), скобки (`2(x - 3) = 10`), дробь с переменной в числителе
  (`(x + 1)/3 = 2`), пропорция (`x/6 = 5/3`). По умолчанию выключены.

**Настройки:** буквы переменной (`x` / `x, y` / разные), формат ответа
(авто — короткая десятичная, иначе обыкновенная дробь; всегда дробь; только
десятичный), «только целые корни», число заданий и вариантов, печать
(2 колонки, 2 варианта рядом или верх/низ, лист ответов, размер шрифта).
Печать и предпросмотр — общий `OralCountingPrintLayout` в режиме уравнений;
доступны «Тест A/B/C/D» и «Вписать ответ», как у остальных генераторов.
Раздел также добавлен в «Смешанную работу» устного счёта.

**Как устроено.** Уравнение строится не строкой, а маленьким деревом узлов
(число / переменная / скобка / дробь): по одному дереву считаются и LaTeX, и
коэффициенты приведённой формы `a*x + b = c*x + d`, откуда и берётся корень.
Поэтому напечатанное условие невозможно рассинхронить с напечатанным ответом.
Некрасивые уравнения (знаменатель корня > 12, |корень| > 100, длинная
десятичная дробь) отбраковываются и генерируются заново.

**Код:** `hooks/useLinearEquations.js` (ядро + чистая функция
`generateLinearEquationVariants`), `components/LinearEquationsGenerator.jsx`,
запись в `hooks/oralMixedRegistry.js`. Секция `equations` — в `ALL_SECTIONS`
(AuthContext) и `SECTION_LABELS` (UserManager). Печатные раскладки
(`OralCountingPrintLayout`, `OralMixedPrintLayout`) больше не считают, что
переменная обязательно `x` — берут `q.varLatex`. Покрыто 12 юнит-тестами
(`__tests__/linearEquations.test.js`, всего 744): корень проверяется
независимым вычислителем LaTeX подстановкой в обе части уравнения.

**Деплой:** уехало на прод в составе 3.9.156 (03.09.2026).
Серверная часть и БД не менялись.

## [3.9.153] - 2026-09-03

### 📥 Импорт работы целиком: листок коллеги одним файлом

Раньше готовая работа от коллеги заводилась в систему поштучно: перевести листок
в md+LaTeX, а потом загружать задачи по одной, каждый раз выбирая тему. Теперь
работа загружается **целиком**: задачи расходятся по своим темам, порядок и
варианты сохраняются, а сама работа появляется в «Мои работы».

**Формат.** Один markdown-файл — шапка работы в YAML, `## Вариант N`, задачи
`### 1` с метастроками (`тема:`, `ответ:`, `сложность:`, `теги:`, `баллы:`,
`часть:`, `решу:`). Метастроки стоят **до** условия — благодаря этому граница
условия однозначна, и внутри него разрешено всё: абзацы, таблицы, ```numline и
```plot, даже строка, начинающаяся со слова «Ответ:». Полная спецификация —
`WORK_IMPORT_FORMAT.md`. Формат двусторонний: `buildWorkMarkdown` собирает `.md`
из сохранённой работы, так что работами можно обмениваться текстом.

Поддержаны: блок `## Ответы` списком в конце (`1) 5`, `2. -3`, `3 — 12`),
написания вариантов «Вариант I», «II вариант», «Вариант Б», «Вариант №2»,
подсекции `#### Решение` / `#### Критерии`, плейсхолдеры чертежей `![](рис1)`.

**Мастер** — «Мои работы» → «Импорт работы» (или меню «Импорт работы»):
источник → проверка → темы → импорт.

- Своей ИИ-ручки нет намеренно: кнопка **«Промпт для ИИ»** собирает промпт с
  актуальным каталогом ваших тем — его копируют во внешнюю модель вместе с фото
  листка и возвращают готовый markdown. Темы модель берёт из вашей базы, а не
  выдумывает.
- Шаг «Проверка» рисует задачи через KaTeX, показывает ошибки и предупреждения
  разбора и просит приложить файлы к плейсхолдерам чертежей.
- Шаг «Темы» — таблица «задача → тема» с автоподбором (нечёткое сравнение:
  регистр, «ё», «№», пунктуация), созданием темы/подтемы на месте и массовой
  простановкой темы всем задачам без темы.
- **Поиск дублей**: задачи назначенных тем сравниваются с базой по
  нормализованному условию (те же ключи, что во вкладке дублей каталога).
  Найденная задача по умолчанию **берётся из базы**, а не копируется; по
  `решу:` дубль ищется точнее — по `sdamgia_id`.
- Чертежи: один на задачу уходит в legacy-поле `tasks.image` (именно оно рисуется
  в карточках и печати), два и больше — в `task_images` с переписыванием ссылок
  на локальные. Битый LaTeX помечается `latex_needs_review`.

**Оригинал остаётся при работе.** Фото/скан листка (до 6 файлов) прикрепляется к
работе — если ИИ переврала формулу, всегда есть с чем свериться.

**База:** миграция `1784100000` — `works.source`, `works.import_meta`,
`works.original_files`.

**Деплой выполнен 03.09.2026:** миграция `1784100000` применена на боевой БД
(поля появились в схеме `works`), `pocketbase-ege` перезапущен, фронт на
`l.oipav.ru` обновлён до 3.9.153. Бэкап перед миграцией —
`backup_2026-09-03_11-43-06.tar.gz`. Рестарт `pdf-service-ege` не требуется —
серверная часть не менялась.

**Код:** `utils/workImportFormat.js` (парсер/сериализатор/промпт),
`utils/workImportPlan.js` (план и дедуп), `utils/topicMatch.js` (общий подбор
тем — «Импорт задач» переведён на него), `hooks/useWorkImport.js`,
`components/work-import/`. API: `getTasksForDedup`, `uploadWorkOriginals`,
`getOrCreateTag`, `getOrCreateSubtopic`. Покрыто 84 юнит-тестами (всего 732).

## [3.9.152] - 2026-09-03

### 🧬 Параллельные варианты и векторный подбор: разбор и починка

Ревизия всего пути «похожие / непохожие работы на основе данной» — от модалки
«Параллельные варианты» до режимов подбора в «Генераторе». Замеры делались на
копии боевого индекса (22 477 векторов).

**Подбор параллелей больше не смотрит в узкую щель.** KNN брал `count*6+15`
ближайших соседей (при 2 параллелях — 27), и уже по ним применялись фильтр темы
и полоса похожести. Для пресета «Разные» (cos 0.70–0.90) это отсекало почти всё:
на выборке из 200 задач пустой пул получался у 24% позиций против 5% при
глубоком поиске. Теперь кандидаты берутся из ВСЕЙ темы: KNN в sqlite-vec стоит
~45 мс независимо от `k` (это полный скан индекса), а пул темы с кэшем — единицы
миллисекунд.

**Варианты стали равноценными.** Жадный проход «вариант за вариантом» отдавал
первой параллели всегда ближайшего кандидата, второй — следующего: комплекты
выходили разной трудности, а повторное «Подобрать» давало ровно тот же результат.
Теперь кандидаты раздаются по позициям — окно лучших тасуется и расходится по
вариантам в случайном порядке. Средняя похожесть параллелей сошлась (0.81 / 0.82
/ 0.81 вместо систематического убывания), а каждый прогон даёт новый набор.

**Параллели больше не пересекаются с другими вариантами работы.** Модалка брала
базой первый вариант, но задачи вариантов 2..N ничем не исключались — «дубль»
мог повторить то, что класс уже видел в соседнем варианте оригинала.

**Учитываются ответ, известные дубли и решаемость.** Кандидат с тем же ответом,
что у образца, уходит в конец очереди и помечается в карточке («тот же ответ» —
по такой паре списывание не отличить); задачи из одного dedup-кластера с
образцом не предлагаются вовсе; при наличии статистики с обеих сторон
отбрасываются кандидаты, расходящиеся с образцом по решаемости больше чем на
30 п.п. Решаемость видна на карточке.

**Ползунок «похожести» в режиме «По образцу» стал honest.** Он двигал границу
окна по РАНГУ соседей, и левые 80% хода ничего не меняли: 0% давало cos 0.699,
50% — 0.762, тогда как случайная задача той же темы — 0.708. Теперь позиция
ползунка переводится в целевой косинус (0.70 → 0.98), и подбираются задачи
вокруг него.

**Векторные режимы «Генератора» перестали игнорировать фильтры.** Сложность,
теги, год и источник теперь действуют и в режимах «по образцу» / «разные сюжеты»
/ «анти-дубль»: фронт считает белый список по снимку каталога (без запросов) и
передаёт его подбору. «Анти-дубль» принимает несколько работ сразу (плюс кнопки
«Последние 5 / 10»), а не одну.

**Нарезка вариантов по кругу.** В векторных режимах набор резался подряд, а он
отсортирован по похожести — вариант 1 получал самые близкие задачи, вариант N —
самые далёкие. Теперь раскладка round-robin.

**Скорость и наблюдаемость.** Пулы векторов по темам кэшируются (LRU с бюджетом
12 000 векторов), между загрузками тем отпускается event loop — раньше подбор
для варианта ЕГЭ занимал ~0,9 с сплошной блокировки backend-helper, теперь
1,3 с на холодном кэше и 6 мс на тёплом. «Дополнить похожими» ходит одной ручкой
`/similar-batch` вместо 20 запросов подряд (27 мс против ~900 мс), запись
семейства идёт пачками по 8. Появилась ручка `/vec-stats`: интерфейс показывает,
сколько задач каталога в индексе и когда он обновлялся — «нет подходящей замены»
больше не выглядит поломкой.

**Мелочи модалки.** Исходная работа не переименовывается задним числом; дубли
наследуют класс оригинала; при обрыве создания семейство откатывается целиком;
ручная замена открывает каталог сразу на теме образца и запоминается — эта пара
больше не предлагается (миграция `1784000000`, значение `rejected_parallel`);
если для набора уже создавались параллели, модалка сообщает об этом и по
умолчанию не предлагает те же задачи.

**Структурные признаки перед косинусом.** Косинус bge-m3 сам по себе не
отделяет «параллель» от «просто про то же»: на ручной разметке у похожих пар
p10 = 0.671, у непохожих p90 = 0.804 — классы перекрываются. Теперь перед
сравнением векторов работает дешёвая проверка структуры: класс ответа
(числовой / выражение / «доказать» / пусто), знак, наличие таблицы, формул и
чертежа, объём условия (допускается расхождение на одну ступень). Правило
подобрано по разметке: теряет 10% настоящих параллелей, отсекает 65% ложных;
на пороге cos 0.85 точность подбора поднимается с 90% до 100% при той же
полноте. Числовые формы ответа считаются взаимозаменяемыми — ответ 0,25 вместо
5 параллель не ломает. Структурно иные кандидаты не выбрасываются, а уходят в
хвост очереди: если совместимых не нашлось, ячейка заполняется таким кандидатом
с пометкой «иная структура» — это лучше, чем «нет подходящей замены».

Чистая логика подбора вынесена в `pocketbase/vec-lib.mjs` (без SQLite) и
покрыта тестами: 33 теста на раздачу параллелей, ползунок, структурную подпись,
MMR, кластеры и новизну, плюс 11 на фильтрацию по снимку каталога.

**Деплой выполнен 03.09.2026:** миграция `1784000000` применена (в
`task_families.type` появилось `rejected_parallel`), `vec-lib.mjs` /
`vec-search.js` / `pdf-service.js` залиты в `/opt/pocketbase/pdf-service/`,
оба сервиса перезапущены, фронт на `l.oipav.ru` обновлён. Бэкап перед
миграцией — `backup_2026-09-03_11-01-21.tar.gz`.

**⚠️ Индекс отстал:** свежая ручка `/vec-stats` показала 3 032 задачи каталога
вне векторного индекса (25 509 задач, в индексе 22 477, последняя индексация
27.06.2026). Пока не прогнан `cd vector-benchmark && npm run index`, эти задачи
не участвуют ни в подборе параллелей, ни в «похожих», ни в режимах Генератора.

## [3.9.151] - 2026-09-02

### 🖨 Печать листа задач шла в 62% масштаба

Лист печатался уменьшенным и прижатым к левому верхнему углу, хотя в превью
выглядел правильно.

Причина — не в самом листе. Портальные слои Ant Design (`.ant-tooltip` и
компания) рендерятся прямо в `<body>`, вне `.no-print`, и хранят ЭКРАННЫЕ
координаты: тултип от кнопок правки задачи стоял на `left ≈ 1050px`.
`visibility: hidden` его прячет, но из потока не убирает — Chrome считает
ширину документа по нему (~1300px вместо 210 мм) и включает shrink-to-fit,
ужимая весь лист. Во входной контрольной этого не было: там печатный лист
намеренно без кнопок, наводить не на что.

Чинится в `@media print` двумя правилами: порталы Ant получают `display: none`,
а корню документа задаётся жёсткая ширина листа с `overflow: hidden` — чтобы ни
один будущий «хвост» шире 210 мм больше не ронял масштаб печати. Лист это не
задевает: он `position: absolute` от начала печатной области, и такой предок
его не клипует.

Воспроизведено и проверено печатью в headless Chrome (`--print-to-pdf`) на
стенде с реальным CSS: до фикса — 62% и прижато влево-вверх, после — лист во
всю страницу, многостраничность цела. Сторожевой тест читает `printSheet.css`
с диска и следит за обоими правилами.

**Грабля на будущее:** тот же дефект возможен в остальных печатных корнях
(WorkPrintPreview, WorksheetGridPrint, КИМ, ТДФ, марафон) — записано в BACKLOG.

## [3.9.150] - 2026-09-02

### 📐 Лист задач: две колонки, узкие поля, редактор шапки, управление чертежами

Продолжение 3.9.149 — четыре настройки поверх нового печатного движка.

**Две колонки.** Раскладку считает пагинация (`paginateIntoColumns`), а не
`column-count`: браузерные колонки режут задачу посередине, и превью перестаёт
совпадать с печатью. Колонка набивается сверху вниз до конца листа, потом
начинается следующая. Остаток на последней странице раскладывается поровну —
иначе четыре коротких задачи уезжали бы в левую колонку, а правая оставалась
пустой. 🚨 Задачи в этом режиме меряются **шириной колонки** (measure-зона
сужается), иначе высоты занижены вдвое и колонки переполняются; шапка при этом
меряется шириной листа — она печатается во всю ширину.

**Узкие поля** — 8 мм по бокам вместо 14 и 8/6 мм сверху/снизу (пресеты
`MARGIN_PRESETS`). Для «Листа задач» это дефолт, входная контрольная осталась
на обычных полях. Поля раздаются CSS-переменными, пагинация считает по тем же
числам.

**Редактор шапки.** Заголовок (по умолчанию = название работы), подзаголовок,
класс, время, дата, подпись в подвале; отдельными тумблерами — поле «Класс» и
строка «N заданий» в подзаголовке (на листе с длинными условиями она только
занимает место — задания и так пронумерованы). В полной шапке — ещё
надзаголовок, инструкция и произвольный блок с заголовком.

**Чертежи и графики — один рычаг размера на три источника:**

- внешняя картинка задачи (`tasks.image`);
- картинка markdown прямо в условии (`![](…)`);
- встроенные чертежи — числовая прямая ```numline и график ```plot/```vectors,
  которые рисуем мы сами в SVG, **в обеих формах записи**: блочной и inline
  (`numline: …` одиночными бэктиками) — последняя живёт в ячейках таблиц, из
  неё собраны задачи «на каком рисунке изображено множество решений».

У чертежа в ячейке таблицы своя база размера — ширина **ячейки**, а не колонки
текста (`--ps-fig-cell-w`: 50/70/85/100%). С общей шкалой галерея из четырёх
прямых съёживалась бы до 48% ячейки.

Размер S/M/L/XL раздаётся CSS-переменными `--ps-fig-w/--ps-fig-h` (шкала общая
с `kimImageSize`), у отдельной задачи переопределяется переключателем в правом
верхнем углу самой задачи на листе и живёт в `task.kimImageSize`. Панель правки
задачи (ручка, ✏️, 🔁, размер чертежа) теперь **видна всегда** — приглушённой,
и проявляется при наведении: полностью скрытая по `opacity: 0` она просто не
находилась. Тумблер «Показывать» убирает с листа все
три вида разом. Инлайн-стилями это не сделать: у наших SVG уже есть свой
`style`, а картинки в тексте рендерит react-markdown.

**Грабли:**

1. Правило масштабирования SVG повешено **только** на `.mr-figure` и на сами
   `.numline`/`.coordplot`. Общее `.ps-task-text svg { max-width }` схлопывает
   радикал KaTeX — он рисуется инлайновым `<svg width="400em">`, и знак корня
   пропадает с печати (сторожевой тест читает CSS с диска).
2. Inline-чертежи в ячейках надо цеплять и по `td`, и по `th`: GFM всегда
   делает первую строку таблицы шапкой, а в галерее варианты часто именно
   в ней. `display` у них не трогаем — рядом стоит подпись «1)», блочная
   раскладка сбросила бы её на отдельную строку.
3. «N на лист» и две колонки несовместимы: остаток высоты делить не на что.
   Пункт скрывается из списка, а если он был выбран — сбрасывается явно.
4. Балансировка колонок считает зазоры и в цели, и в накопленной высоте. Без
   этого страница из задач одинаковой высоты схлопывалась в одну колонку.

Тесты: +22 (колоночная пагинация и балансировка, геометрия полей, переменные
чертежей и шкала для ячеек, чертёж из ячейки таблицы на листе, личный размер
задачи, редактируемая шапка, тумблер числа заданий) — всего 603.

## [3.9.149] - 2026-09-02

### 🖨 «Генератор» печатает лист тем же движком, что входная контрольная

Раньше в «Генераторе» лист задач верстался по старому CSS: пагинации не было
вообще — задачи сыпались потоком, а разбиение на страницы отдавалось браузеру
через `break-inside: avoid`. Отсюда полупустые листы, съехавшие поля (`@page`
задавался с полями поверх padding'ов) и колонки, которые в печати разъезжались.

Печатный движок входной контрольной вынесен в общий модуль
`components/print-sheet/` — теперь на нём работают оба раздела:

- честная пагинация: задачи меряются в скрытой зоне шириной с лист и
  раскладываются по страницам по реальным высотам, с перемером после загрузки
  шрифтов KaTeX и чертежей;
- монохромная типографика, колонтитул со сквозной нумерацией, лист ответов
  для учителя последней страницей;
- A4, поля держит сам лист (`@page { margin: 0 }`).

**Компактность.** Шапка переключается: «компактная» — название, вариант и
строки ФИО/даты в две строки (вместо ~45 мм полной шапки с инструкцией это
~14 мм, то есть плюс 2–4 задачи на первой странице); «полная» — как во входной
контрольной, с надзаголовком, классом, временем и инструкцией. Место для
решения регулируется отдельно: нет / S / M / L / XL. По умолчанию — компактный
лист без зоны решения, только строка «Ответ: ______».

**Режим «N на лист»** заменил отдельную страницу «Рабочий лист»: страницы
набиваются ровно по N заданий, а весь остаток высоты делится между ними как
зона решения (клетка/линейка/пусто). Отличие от старого «Рабочего листа» —
высота блока считается из реальной высоты условия, а не по формуле «270 мм / N»,
поэтому длинные условия больше не выдавливают клетку за лист.

**Что сохранилось:** шифровка по ответам (теперь монохромный блок в конце
варианта, участвует в пагинации наравне с задачами), лист ответов для учителя,
правка прямо в превью — перетаскивание задач, ✏️ и 🔁 (кнопки позиционированы
абсолютно и в печать не идут).

**Что убрано:** формат A5 (движок считает геометрию A4), вёрстка в 2–3 колонки,
старый тумблер «компактный режим» (его заменяют «шапка: компактная» +
«место для решения: нет»), кнопка PDF на листе — html2pdf растрирует
миллиметровую вёрстку, вместо неё подпись «PDF — через „Печать“».

**Грабли:**

1. Кнопки правки на задаче обязаны быть `position: absolute` — в потоке они
   попадают в измеренную высоту задачи, и пагинация считает лист короче, чем он
   печатается. В зону измерения они не рендерятся вовсе.
2. Зона решения не меряется в DOM: к измеренной высоте «голой» задачи
   прибавляется её высота в мм. Иначе на каждое переключение S/M/L шёл лишний
   цикл перемера.
3. Неполная последняя страница в режиме «N на лист» считается по N слотам, а не
   по числу задач на ней — иначе две задачи на хвосте растягивались бы на
   пол-листа каждая.

Тесты: 19 новых (`worksheetSheet.test.jsx`) — пагинация «N на лист», шапки,
поле ответа, зона решения, кнопки правки, монохромность листа; тесты входной
контрольной (27) переведены на новые классы и проходят на общем движке.

## [3.9.148] - 2026-09-02

### 🔎 Поиск задачи по номеру с «Решу ЕГЭ/ОГЭ»

Номер задачи с решу (`311151`) лежит в отдельном поле `tasks.sdamgia_id` и в
тексте условия не встречается — обычный поиск по каталогу его не находил.
Теперь строка поиска разбирается отдельно и понимает три способа ввода:

- ссылку целиком — `https://math-oge.sdamgia.ru/problem?id=311151`;
- подпись со страницы — «Задание 6 № 311151»;
- голый номер — `311151`.

Ссылка и «№ …» ищут строго по номеру (искать такое по тексту нечего), голое
число добавляет номер к обычному поиску по коду и условию — число могло быть и
куском условия. Числа короче трёх цифр номером не считаются (это скорее номер
задания в варианте). Работает во всех трёх местах, где ищут задачи: каталог,
выбор задач в работу, «Генератор».

Миграция `1783900000` — индекс `tasks(sdamgia_id)`. По нему ходит не только
поиск, но и дедуп при импорте с решу и мост problem_id → задача для внешнего
журнала; без индекса каждый такой запрос был полным сканом 25 тыс. строк.

**Грабля:** PocketBase заводит индекс необязательного поля частичным —
`CREATE INDEX … WHERE sdamgia_id != ''`, — а SQLite берёт такой индекс, только
если ровно это условие есть и в самом запросе. `sdamgia_id = "311151"` шёл
сканом (26 мс), `sdamgia_id != "" && sdamgia_id = "311151"` идёт по индексу
(0 мс). Условие непустоты добавлено во все точечные запросы по номеру решу:
поиск, дедуп импорта, мост внешнего журнала, скрипты импорта каталогов.

## [3.9.147] - 2026-08-29

### 🔍 Поиск по каталогу: находит независимо от регистра и не дёргает сервер на каждую букву

**Поиск не находил слова с заглавной.** SQLite приводит регистр только для
латиницы: `abc` находит «ABC», а «Треугольник» не находит «треугольник».
На боевой базе это 50 задач против 2801 — то есть поиск с заглавной буквы
показывал полтора процента того, что должен. Теперь запрос перебирает
написания (как ввели · строчными · с заглавной) — то же самое сделано в
поиске по каталогу геометрии, где ищется ещё и по названию, ответу, источнику.

**Запрос уходил на каждое нажатие клавиши.** Слово из шести букв — шесть
полных поисков по 25 тыс. условий, причём ответы могли прийти вразнобой и
перетереть друг друга (автоотмена запросов в SDK выключена). Теперь: пауза
400 мс после набора, минимум два символа, применяется результат только
последнего запроса. Очистка поля срабатывает сразу, без ожидания.

**Индексы для каталога** (миграция `1783800000`): `tasks(code)` под сортировку
и постраничную выборку, `tasks(topic)` под фильтр по теме. Замер на копии
боевой базы: листание 25 мс → 0, поиск «треугольн» 62 мс → 1 мс, редкое слово
→ 14 мс, фильтр по теме → 1 мс. Полный ответ API изнутри сервера — 110 мс,
остаток держит `COUNT(*)` для числа страниц (это цена пагинации).

## [3.9.146] - 2026-08-29

### 🐛 «Мои работы» падали с ошибкой CORS + сохранение работы шло 7–10 секунд

**Статистика работ не грузилась.** Список выдач запрашивался одним фильтром
`work = "…" || work = "…"` по всем работам учителя — при 258 работах это
9,5 КБ в адресной строке. PocketBase отдаёт 400 примерно от 130 условий,
nginx рубит запрос ещё раньше (414 Request-URI Too Large), а так как на 414
нет CORS-заголовков, в консоли это выглядело как «blocked by CORS policy».
Теперь такие запросы режутся на куски по 50 значений и идут параллельно —
общий хелпер `getFullListByOr` (`shared/services/pb/chunked.js`), переведены
все места со списками id: выдачи, попытки, ответы, задачи по id, аналитика
MC-теста, ученики курса, пересечения задач между работами.

**Сохранение работы.** Сервер отвечал за 2–7 мс, ждал браузер: пока учитель
правил состав, три виджета редактора молотили запросы на каждое движение
задачи. Бейдж новизны на каждую правку тянул весь список работ и варианты
пяти последних работ по одному, с раскрытыми задачами; предупреждение о
повторах внутри вариантов перезапускалось вообще на каждый рендер (зависимость
по ссылке на массив). Всё это забивало шесть HTTP-соединений браузера, и
сохранение стояло в очереди.

- бейдж новизны: дебаунс, отмена устаревшего расчёта, лёгкие запросы
  (`getRecentWorkIds` + один `getVariantsByWorks` без раскрытия задач)
  вместо шести тяжёлых;
- предупреждения о повторах и о пересечениях с другими работами — дебаунс
  и зависимость по составу, а не по ссылке;
- само сохранение: варианты пишутся разом (`Promise.all`), список
  существующих запрашивается без раскрытия задач;
- кнопки «Сохранить» / «Копия» блокируются на время записи — повторный клик
  по «зависшей» кнопке отправлял второй комплект запросов;
- снимок каталога (~25 тыс. задач, полсотни запросов) больше не
  перезагружается в фоне, если кэш моложе двух минут.

## [3.9.145] - 2026-08-28

### 🔧 Входная контрольная: поле «Класс» отключается отдельно

Раньше поля для заполнения учеником включались одним тумблером — «Фамилия,
имя · Класс · Дата». В школе с одной параллелью графа «Класс» только мешает,
а коллегам с несколькими она нужна, поэтому теперь у неё свой переключатель:
«Поле "Класс"» рядом с «Поля "Фамилия, имя" и "Дата"». Выключается независимо,
блокируется вместе с основным тумблером; оставшиеся поля растягиваются на
освободившееся место сами.

## [3.9.144] - 2026-08-28

### ✨ Модификаторы таблиц можно писать отдельными строками

Комбинировать виды таблиц можно было только перечислением через запятую —
`{галерея, без линий}`. Естественная запись двумя строками

    {галерея}
    {без линий}
    | А) … | Б) … |

не работала: плагин смотрел лишь на один абзац перед таблицей, поэтому нижняя
директива применялась, а верхняя оставалась висеть на листе текстом.

Теперь директивы перед таблицей собираются все подряд — обе записи дают
одинаковый результат, порядок не важен.

## [3.9.143] - 2026-08-28

### ✨ Галерея из одной строки

Ряд чертежей «А) … | Б) … | В) … | Г) …» без второй строки markdown не считал
таблицей вовсе: у pipe-таблицы обязана быть строка-разделитель, а её нельзя
поставить, когда тела нет. Строка печаталась как текст с палками, вместе с
самой директивой `{галерея}`.

Теперь разделитель подставляется и для таблицы в одну строку — но только если
перед ней стоит директива: намерение объявлено явно. Без директивы любая
строка с палками превращалась бы в таблицу. Такая однострочная таблица
закрывается пустой строкой, иначе GFM утаскивал следующий абзац («Запишите в
ответ цифры…») в тело таблицы.

В меню «Таблица ▾» — заготовка «Ряд рисунков (одна строка)» с четырьмя
параболами.

### 🩹 Числовая прямая: дробь слипалась с точкой

Подпись-дробь под выколотой точкой начиналась на 3 px ниже оси, а кружок точки
(радиус 3,4 плюс обводка) уходит вниз на 4 — числитель налезал на кружок, и
«¼» читалась как приклеенная к точке.

Дробь опущена: теперь от оси до верха числителя 8,5 px — чуть больше, чем у
обычной подписи (там около 7), потому что верхняя цифра дроби стоит прямо под
кружком. Поле под осью и высота холста для прямых с дробями увеличены
соответственно (58 → 64 px), положение самой оси не изменилось — верхняя часть
рисунка с штриховкой осталась прежней.

## [3.9.142] - 2026-08-28

### ✨ Таблицы-галереи: чертежи в клетках без шапки

Задания вида «на каком рисунке изображено множество решений» — это сетка 2 × 2
с числовыми прямыми. Собрать её markdown-таблицей мешало то, что GFM обязан
считать первую строку заголовком: два верхних варианта уезжали на серую плашку,
по центру и полужирным, а нижние два выглядели иначе.

Новый модификатор — `{галерея}` (синонимы `{рисунки}`, `{варианты}`):

    {галерея}
    | 1. `numline: domain -6 6; ray left -4 open` | 3. `numline: domain -6 6; ray right -4 open` |
    | --- | --- |
    | 2. `numline: domain -6 6; ray left 4 open`  | 4. `numline: domain -6 6; ray right 4 open`  |

Первая строка снова выглядит обычной клеткой: прозрачный фон, выравнивание по
левому краю, нормальное начертание. Колонки равной ширины (`table-layout:
fixed`), содержимое по вертикали центрировано, широкий чертёж ужимается, а не
ломает раскладку. При печати клетка не разрывается между страницами.

Как и остальные модификаторы, комбинируется через запятую — `{галерея, без
линий}` даёт ту же сетку без рамок.

В меню «Таблица ▾» редактора задач добавлена заготовка «Рисунки в клетках
(2 × 2)» — вставляет готовый каркас с четырьмя числовыми прямыми.

### 🩹 Модификаторы таблиц не работали в разделе «Теория»

Классы `md-table--*` конвейер теории проставлял, а стили к ним не подключал:
`markdownTables.css` импортировался только в `MathRenderer`, и если на странице
не было ни одной задачи, чанк со стилями не загружался. Теперь css импортирует
и `useMarkdownProcessor`.

## [3.9.141] - 2026-08-28

### 🩹 Входная контрольная: с листа пропадали задания с чертежами

После того как в задачи добавили чертежи, печать поехала: у задания №9
обрезалась строка «Ответ», а задания №15–17 исчезли с листа целиком —
страница прыгала с №14 сразу на №18.

Причина — асинхронная загрузка картинок. Высоты задач снимаются в
`useLayoutEffect` сразу после рендера, а `kimImageImgStyle` задаёт чертежу
только `max-height`, поэтому незагруженный `<img>` в этот момент имеет высоту
0 — задача с чертежом мерилась на 3–8 см короче, чем окажется. Пагинация
набивала страницу «лишними» заданиями, картинки догружались, содержимое
вылезало за границу листа, а `overflow: hidden` его срезал.

Две правки:

- **Ждём чертежи.** Компонент подписывается на `load`/`error` каждой ещё не
  загруженной картинки в зоне измерения и перемеряет страницы. Тот же приём,
  что уже применялся для `document.fonts.ready`.
- **Лист больше не режет содержимое.** `height` + `overflow: hidden` заменены
  на `min-height`: если пагинация всё же промахнётся, лист вырастет и лишнее
  уедет на следующую физическую страницу — некрасиво, но видно. Молча терять
  задания печатный лист не должен.

Тесты: чертёж присутствует и в зоне измерения, и на листе; сторож на
`.et-page` — `min-height` без `overflow: hidden`.

## [3.9.140] - 2026-08-28

### 🩹 Входная контрольная: на печати пропадал знак корня

В задании вида «Найдите значение выражения $-4\sqrt{3}\sin(-780^\circ)$» на
листе печаталось «−4  3 sin(−780°)» — радикал и черта над подкоренным
выражением исчезали. На экране, в списке состава работы, та же задача
рисовалась правильно.

Виновато было собственное правило `.et-task-text svg { max-width: 100% }`,
добавленное в 3.9.137 ради встроенных чертежей ```numline / ```plot. KaTeX
рисует знак корня инлайновым `<svg width="400em">` внутри `.hide-tail`
(`overflow: hidden`) — ограничение ширины схлопывало его в ноль. Правило
удалено: чертежам оно и не требовалось, `numberLineSvg` / `coordPlotSvg` сами
ставят `style="max-width:100%;height:auto"` на свой `<svg>`.

Регрессия закрыта двумя тестами: рендер `\sqrt` на печатном листе и сторож на
исходник CSS (jsdom стили не применяет, `?raw` на .css в vitest отдаёт пустую
строку — файл читается с диска).

## [3.9.139] - 2026-08-27

### 📋 Таблицы в условиях задач: без линий, с местом для ответа

Три давние проблемы markdown-таблиц в условиях:

**1. Таблицы без линий.** Задания на соответствие («ВЕЛИЧИНЫ / ЗНАЧЕНИЯ»,
«ПЕРИОДЫ ВРЕМЕНИ / ХАРАКТЕРИСТИКИ») в оригинальном КИМ — это два столбца
*без единой рамки*, а у нас рисовалась сетка с серой шапкой. Теперь вид
таблицы задаётся **строкой-директивой перед ней**:

```
{без линий}
| ВЕЛИЧИНЫ | ЗНАЧЕНИЯ |
| --- | --- |
| А) длительность урока | 1) 17,6 секунды |
```

Модификаторы (можно через запятую — `{без линий, без шапки}`):
`{без линий}` · `{без шапки}` · `{бланк}` · `{компактная}` · `{линии}`
(последний отключает автоподбор). Есть английские синонимы: `{plain}`,
`{noheader}`, `{answers}`, `{compact}`, `{grid}`.

**2. В таблицу-бланк нельзя было ничего вписать.** Строка вида
`|   |   |   |   |` схлопывалась в нулевую высоту — на печати ученику
физически некуда было писать. Теперь целиком пустая строка таблицы получает
высоту под рукописный ответ (одиночные пустые ячейки в заполненных
таблицах не раздуваются — это «нет данных», а не поле).

**3. Ответ = заполнение таблицы** (задания вида «в таблице под каждой буквой
укажите соответствующий номер»). Шапка «А Б В Г» с пустой строкой под ней
теперь распознаётся как **бланк ответа**: узкая таблица по центру, ровные
клетки 10 мм, без серой заливки шапки.

Разметку старых задач править не нужно: **вид подбирается автоматически** —
таблица с пустым телом → бланк ответа, два столбца вида «А) …» | «1) …» →
таблица без линий. Директива всегда сильнее автоподбора, `{линии}` возвращает
обычную сетку.

В редакторе задачи (условие и решение) появилась кнопка **«Таблица ▾»** с
готовыми заготовками: «Соответствие + бланк ответа», «Бланк ответа (А Б В Г)»,
«Таблица без линий», «Обычная таблица с линиями».

Механика общая для всего приложения — remark-плагин
`utils/remarkTableModifiers.js` + стили `shared/components/markdownTables.css`
подключены к обоим конвейерам markdown (`MathRenderer` и `useMarkdownProcessor`),
поэтому работает везде: рабочие листы, КИМ-варианты, входная контрольная,
тесты ученика, теория. 13 новых юнит-тестов.

## [3.9.138] - 2026-08-27

### 🔧 Входная контрольная: тумблер нижнего колонтитула

В «Оформлении листа» появился переключатель «Нижний колонтитул». Выключенный
убирает подпись и номер страницы со всех листов, включая лист ответов, —
и **отдаёт задачам освободившиеся 8 мм**: ёмкость страницы в пагинации
считается от того, печатается подвал или нет, поэтому на лист влезает больше,
а не остаётся пустая полоса. Поле «Подпись в подвале листа» при выключенном
подвале блокируется с пояснением.

## [3.9.137] - 2026-08-27

### ✨ Входная контрольная работа — новый генератор

Школьная «входная» диагностика в начале года получила свой раздел:
**Рабочие листы → Входная контрольная**. Раньше её собирали в «Контрольных
работах» и печатали типовым листом — теперь у неё свой бланк.

Лист **монохромный по замыслу**: цвета нет вообще, потому что печатается он на
ч/б принтере и любой акцент превращается в серую кашу. Иерархию держат кегль,
насыщенность и четыре толщины линеек — 0.5 мм рамка шапки, 0.35 мм линии для
заполнения, 0.25 мм рамки блоков, волосяные 0.2 pt на разделителях и клетке.
Ничего декоративного: шапка занимает ~50 мм вместо трети листа, задачи идут
плотным столбцом с волосяными разделителями, чертежи — блоком по центру с
управляемым размером.

- **Два макета** (переключатель вверху страницы):
  - **Набор задач** — номер в тонком квадрате, условие, строка «Ответ:».
  - **Рабочая тетрадь** — то же плюс зона решения (высота S/M/L/XL, разлиновка
    «пусто / линейка / клетка 5 мм»).
- **Шапка листа**: надзаголовок, название, подзаголовок, бокс варианта,
  метаданные одной строкой («10 класс · 45 мин · 10 заданий · 1 сентября»),
  поля «Фамилия, имя · Класс · Дата», хайрлайн-бокс с инструкцией и
  **произвольной дополнительной информацией** (свой заголовок + текст) —
  обе врезкой, «ИНСТРУКЦИЯ. текст…». Плюс подпись в подвале листа.
- **Шаблоны** под школьные сценарии: «9 → 10 класс», «10 → 11 база»,
  «10 → 11 профиль», «Свой шаблон». Шаблон подставляет шапку и инструкцию, а
  кнопка «Заполнить темами экзамена» собирает структуру по заданиям (№1, №2, …).
- **Типографика**: шрифт условий гротеск или антиква, кегль 10/11/12 pt.
  Тумблеры строки ответа, кода задачи, типовых вступлений и листа ответов.
- **Чертежи**: размер каждого (S/M/L/XL) выставляется в списке задач под
  настройками — чтобы рисунок стоял на своём месте, а не плавал по листу.
- **Печать** — постраничная: жадная пагинация по измеренным высотам задач,
  колонтитул и сквозная нумерация на листах 2+, лист ответов отдельной
  страницей (растёт на несколько листов, если вариантов много).
  PDF — через «Печать» → «Сохранить как PDF».
- Состав работы правится списком под настройками: ↑/↓, ✏️ и 🔁 у каждой задачи.

Новое: `components/EntranceTestGenerator.jsx`, `components/entrance-test/`
(`EntranceTestPrint.jsx` + `.css`, `EntranceTaskListEditor.jsx`, `presets.js`),
маршрут `/app/worksheets/entrance-test`. 21 тест в
`__tests__/entranceTest.test.jsx`.

## [3.9.136] - 2026-08-27

### 🩹 Импорт: только что созданная тема пропадала из списка

Продолжение 3.9.135. Тема создавалась в базе, но импорт падал на всех задачах
с «У темы не указан номер ЕГЭ (ege_number)».

- Причина — синхронизация локального списка тем с контекстом «по разной длине»
  (`TaskImporter`): после добавления новой темы длины списков расходились, и
  условие честно возвращало список контекста, **выбрасывая только что созданную
  тему**. `topicId` при этом оставался, поиск `topics.find(...)` давал
  `undefined` → сообщение про отсутствующий `ege_number` (номер был, темы в
  списке не было).
- Локальные копии заменены на merge: списки контекста + созданные в этой сессии
  записи (дедуп по id, пропадают сами после `reloadData()`).
- Страховка в `useTaskImport.handleImport`: если темы нет в списке, запись
  дочитывается из БД (`api.getTopic`), а не роняет импорт.

## [3.9.135] - 2026-08-27

### 🩹 Импорт задач: тема из файла больше не теряется

Лемма читала `topic:` из YAML, но при показе «Привязки к базе» молча оставляла
селектор темы пустым, если такой темы в базе нет, — а создать её из импортёра
было почти невозможно.

- **Сопоставление темы нормализуется** (`normalizeTopicTitle` в `utils/normalize.js`):
  регистр, `ё/е`, «№» и пунктуация больше не мешают. «ЕГЭ-База №14 Вычисления»
  из файла теперь находит «ЕГЭ-База. №14 Вычисления» в базе (раньше сравнение
  было побайтовым, и точка ломала матч). Частичное совпадение по коротким
  названиям (< 8 символов) отключено — «Планиметрия» внутри длинного заголовка
  давала ложный автовыбор темы.
- **Тема не найдена — это видно**: на шаге предпросмотра появляется плашка
  «Тема «…» не найдена в базе» с кнопкой «Создать тему».
- **Создание темы из файла в один клик**: название, тип экзамена и номер
  подставляются из метаданных. Если в заголовке нет «№N», тема считается
  вненомерной — номер 0 и тип «Прочее», чтобы она не лезла лишней строкой в
  генераторы вариантов ЕГЭ.
- **Разрешён номер 0** в модалке создания темы (раньше `min=1`, а дубль-проверка
  по номеру блокировала второй «нулевой» предмет). Так уже живут «Марафон»,
  «Летнее ДЗ», Мордкович — темы вне нумерации ЕГЭ.
- Кнопка «+» у подтемы на шаге предпросмотра подставляет `subtopic:` из файла.

## [3.9.134] - 2026-08-27

### 📈 Графики функций и векторы на клетчатой плоскости (SVG-DSL ```plot)

Два новых конструктора рядом с числовой прямой — для условий/решений задач и
для теории. **Без новых библиотек**: чистый inline-SVG, как в `numberLine.js`.

- **Ядро `utils/coordPlot.js`** — единый источник истины для ОБОИХ конвейеров
  рендера: `parseCoordPlot(spec)` → модель, `coordPlotSvg(model)` → строка
  `<svg>`. Собирается **без `<defs>/<marker>/<pattern>`** (стрелки осей и
  векторов — `<path>`, клетка — обычные `<line>`), поэтому разметка проходит
  DOMPurify и печатается вектором. Клетка всегда квадратная: масштаб по X и Y
  один, иначе векторы и углы врут.
- **Свой калькулятор `f(x)` без `eval`** — токенизация → рекурсивный спуск →
  AST. Неявное умножение (`2x`, `3(x+1)`), правоассоциативная степень,
  десятичная запятая (`0,5x`), функции `sqrt/abs/sin/cos/tg/ln/lg/log(b,x)/…`,
  константы `pi`, `e`. Незнакомое имя или посторонний код (`alert(1)`) — внятная
  ошибка в конструкторе, а не молчаливо пустой график.
- **График рвётся там, где надо**: вне области определения (`sqrt(x-1)`) и на
  асимптотах (`1/x`) путь разбивается на подпути, выход за окно по Y обрезается
  аналитически по точке пересечения с границей.
- **DSL**: `x -4 4` · `y -3 5` · `grid 1|off` · `size 280` · `axis t s` ·
  `units off` · `f x^2-4 color orange from -2 to 2 dash` ·
  `vec a 1 4 3 1` (или `vec b 2 3` — из начала координат) · `point 1 3 fill` ·
  `seg 0 0 2 3 dash` · `label 2 3 A` · `xtick -5` / `ytick 3`.
  Подпись вектора рисуется буквой курсивом со стрелочкой сверху (аналог
  `\vec a`) — со стороны, заданной `side left|right`.
- **Стиль по умолчанию — строгий монохром** (как у числовой прямой,
  печатается в Ч/Б); цвет включается явно: `color orange|blue|green|red|violet`.
- **Две формы синтаксиса**, как у `numline`: блок ` ```plot ` (алиас
  ` ```vectors `) и inline `` `plot: x -3 3; f x^2` `` для ячеек
  markdown-таблиц (в ячейке fenced-блок не работает — там только inline).
- **Конструктор `PlotModal`** с переключателем «График функции / Векторы»:
  живое превью, окно и шаг клетки, размер S/M/L, буквы осей, шаблоны формул
  (прямая, парабола, гипербола, показательная, логарифм, корень, модуль,
  синус), список кривых/векторов/точек, формат вставки «блоком / в строку».
  Кнопки «График» и «Векторы» — в `TaskEditModal` (условие и решение) и в
  тулбаре редактора теории.

Подключение — те же две ветки, что у числовой прямой: `MathRenderer`
(переопределения `pre`/`code`) для задач и `useMarkdownProcessor` (два regex в
`postprocess` + расширенный whitelist DOMPurify) для теории.

Тесты: `coordPlot.test.js` (30), `PlotModal.test.jsx` (5),
`plotPipelines.test.jsx` (7 — обе ветки рендера сквозь DOMPurify).

## [3.9.133] - 2026-08-27

### ➖ Устный счёт → Арифметика: категория «Минусы и скобки»

Одиннадцатая категория в генераторе арифметики (`useOralCounting.js`) —
задания с цепочками знаков, где ошибаются чаще всего:

- `-\dfrac{7}{8} : \left(-\dfrac{14}{8}\right)` — частное/произведение двух
  отрицательных дробей (общий знаменатель, вторая дробь несокращённая);
- `-\dfrac{3}{4} - (-2{,}25)` — вычитание отрицательного, дробь + десятичная;
- `-\left(-\dfrac{7}{8} : \left(-\dfrac{14}{8}\right)\right)` — внешний
  унарный минус перед всей скобкой (добавляется к любому подвыражению с
  вероятностью 0,4);
- цепочки на целых: `-(-(-7))`, `-(-4) \cdot (-9)`, `-(-11) - (-2{,}5)`.

Минимум два минуса в задании, обычно три-четыре. Значение считается на
рациональных числах (`ratAdd/ratSub/ratMul/ratDiv`), поэтому ответ точный;
десятичные операнды дают десятичный ответ, дробные — смешанное число.
Отбрасываются вырожденные дроби (`\dfrac{2}{2}`, `\dfrac{6}{2}`).
Категория работает и в смешанной работе (`oralMixedRegistry`), и с флагом
«только целые/десятичные ответы».

Тесты: `src/__tests__/oralNegSigns.test.js` (4 теста) — ответ сверяется с
независимым вычислением выражения, проверяется рендер KaTeX и число минусов.

## [3.9.132] - 2026-08-25

### 🧪 Лаборатория: Grist (таблицы) и HedgeDoc (совместные заметки) на малине

Два self-hosted сервиса рядом с учительским фронтом, встроены в раздел
«Лаборатория» через iframe-обёртку.

- **Малина** (Pi 4, 2 ГБ): отдельный стек `/opt/docker/lab/docker-compose.yml`
  (`lab-grist`, `lab-hedgedoc`, `lab-pg`) — намеренно НЕ в основном
  `docker-compose.yml`, чтобы `up -d` не задевал боевой nginx. Сеть —
  внешняя `docker_default`, наружу порты не публикуются. `mem_limit`
  800/500/256 МБ. Расход в покое: Grist 195 МБ, HedgeDoc 117 МБ, PG 57 МБ.
- **zram-swap 1,1 ГБ** (`zram-tools`, PERCENT=60) — раньше swap не было вовсе,
  и OOM-killer при нехватке памяти мог прибить nginx вместе с l.oipav.ru.
- **Grist**: аутентификации у OSS-версии нет, поэтому личность приходит из
  заголовка — `GRIST_FORWARD_AUTH_HEADER=x-forwarded-user` + обязательный
  `GRIST_IGNORE_SESSION=true` (без него Grist держит анонимную сессию и не
  пускает даже после `/auth/login`). Заголовок проставляет nginx из логина
  basic-auth и всегда перезаписывает — иначе личность подделывается запросом.
  Песочница — `pyodide`: gvisor на arm64 недоступен.
- **HedgeDoc** 1.12 на Postgres 16, свои учётки (`bin/manage_users`),
  саморегистрация закрыта. `CMD_CSP_ALLOW_FRAMING=true` для встраивания,
  домен фрейминга ограничен в nginx (`frame-ancestors 'self' https://l.oipav.ru`).
- **nginx**: `proxy_set_header Authorization ""` в grist-локации — обязательно.
  Иначе Grist принимает заголовок basic-auth за свой API-ключ и отвечает
  401 «unsupported Authorization scheme, expected 'Bearer'», хотя nginx
  аутентификацию уже прошёл. Сертификаты Let's Encrypt выпущены на оба
  домена (`enable-tls.sh`), фрейминг ограничен `frame-ancestors 'self'
  https://l.oipav.ru`.
- **Бэкап** (`/opt/docker/lab/lab-backup.sh`, cron 02:40): Grist-базы через
  `sqlite3 .backup` (открыты сервисом), `pg_dump` HedgeDoc, uploads через
  `docker cp` (0700 под контейнерным uid), `.env`+htpasswd. Локально 7,
  S3 `tws3:math-lemma/lab` 14. До этого данные малины offsite не уезжали.
- **Фронт**: `components/lab/LabEmbed.jsx` — обёртка с «во весь экран»,
  «в новой вкладке» и перезагрузкой; `GristSection` / `HedgeDocSection` как
  lazy-роуты `/app/lab/grist` и `/app/lab/pad`. Адреса — через
  `VITE_LAB_GRIST_URL` / `VITE_LAB_PAD_URL`.

### 🎛 Сворачиваемое левое меню

- `Sider` стал `collapsible` (220 ⇄ 64 px), триггер — кнопка в шапке рядом с
  хлебными крошками; встроенный нижний триггер не годится, меню слишком
  длинное и он перекрывал бы пункты. Состояние живёт в localStorage.
- В свёрнутом виде `openKeys` передаётся пустым (иначе antd ругается на
  inline-collapsed) и прячутся подписи. Ссылка на исходники по §13 AGPL
  остаётся доступной — переезжает на сам логотип (href + title).

## [3.9.131] - 2026-08-23

### 🐛 Главная ученического: футер уехал вбок

- **Проблема.** В v3.9.130 копирайт-строку AGPL добавили вторым дочерним
  элементом `.student-home`, а этот контейнер — flex-строка
  (`display:flex; align-items:center; justify-content:center`). Карточка входа
  и футер встали *рядом*: карточка сместилась влево от центра, а текст
  «Lemma © 2026 … AGPL-3.0, исходный код» повис справа от неё по вертикали
  посередине экрана.
- **Фикс** (`StudentApp.css`): контейнеру добавлено `flex-direction: column` —
  футер вернулся под карточку, карточка снова по центру. Карточке и футеру
  задан `flex-shrink: 0` (в колонке главная ось вертикальная, иначе на низких
  экранах контент сжимался бы по высоте), футеру — `width:100%; max-width:420px`
  под ширину карточки.
- **Контраст футера.** Копирайт-ссылка была почти невидима: `#a0a4b8` на светлом
  фоне ≈ 2,3:1, `#5c5c5c` на тёмном ≈ 2,8:1 — вдвое ниже WCAG AA. Стало
  `#666b7e` / `#787d91` (≈ 4,7:1 в обеих темах): ссылка на исходники по §13 AGPL
  должна читаться, вид остаётся приглушённым.

## [3.9.130] - 2026-08-23

### ⚖️ Лицензия: AGPL-3.0 вместо «все права защищены»

- **Проблема.** Репозиторий публичный, лендинг заявлял «open source // free», а
  файла лицензии не было вообще — в README стояло «Приватный проект. Все права
  защищены.» Юридически это означало, что использовать код нельзя никому и ни
  в каком виде, а авторство при этом нигде явно не фиксировалось.
- **`LICENSE`** — канонический текст GNU Affero General Public License v3
  (взят с gnu.org, 34 523 байта). Выбор AGPL, а не MIT/Apache: платформа
  работает как сетевой сервис, и §13 обязывает всякого, кто запускает Lemma
  или производную работу для других людей, открыть исходники своей версии.
  Закрыть код и продавать как проприетарный продукт нельзя.
- **`NOTICE`** — копирайт + явный перечень того, на что лицензия **не**
  распространяется: учебный контент («Решу ЕГЭ», банк МЦНМО, листки Гордина,
  ФИПИ — в репозитории лежат только скрипты импорта, не сами задачи),
  изображения достижений (`public/achievements/`, собраны из открытых
  источников), название и логотип «Lemma» (§7(e) сохраняет права на
  товарные знаки — форки берут своё имя), сторонние библиотеки. Там же —
  контакт для коммерческого лицензирования: автор единственный
  правообладатель и вправе лицензировать код на иных условиях.
- **Упоминание автора в интерфейсе** (§13 требует давать сетевым пользователям
  доступ к исходникам): футер лендинга (`FooterV2`) — «Lemma © 2026 Oleg
  Pavlyuchenko» + ссылка на LICENSE; сайдбар учительского приложения
  (`App.jsx`) — строка «© Oleg Pavlyuchenko · AGPL-3.0» под номером версии;
  главная ученического приложения (`StudentApp.jsx` + `.student-home-legal`
  в CSS, есть тёмная тема).
- **`package.json`** (корневой и `ege-tasks/`) — поля `author`, `license:
  AGPL-3.0-or-later`, `repository`. Бейдж лицензии и развёрнутая секция
  «Лицензия» в README.
- Проверено: зависимости конфликта не дают (701 MIT, 56 ISC, 14 Apache-2.0,
  ни одного чистого GPL). Автор кода один — смена лицензии ничьего согласия
  не требует. Сборка и 436 тестов зелёные.

## [3.9.129] - 2026-08-23

### ✅ Видно, какая работа сдана, на странице конкретного ученика

- **Проблема.** Сводка по кампании (v3.9.128) показывала, кто отстаёт, но при
  переходе в само задание ученика страница про факт выполнения не знала ничего:
  список работ по неделям выглядел одинаково — и сданные, и нетронутые.
- **Факт по каждой выдаче.** Новая чистая функция `sessionFacts(attempts)` в
  `campaignProgress.js` (та же логика лучшей попытки, что в сводке класса):
  сессия → `{ tries, done, inProgress, score, total, quality, at }`. 3 юнит-теста.
- **Загрузка факта** в `StudentProgramEditor` переехала с
  `getAttemptsByStudentAll(studentId)` на `getAttemptsBySessions(...)` по выдачам
  плана — прежний запрос терял решения без входа в аккаунт (аноним по ссылке),
  из-за чего ✅ в памятке и в PDF не появлялись даже у сдавших.
- **UI страницы ученика:**
  - карточка-сводка сверху: «Сдано работ N из M», «Решает сейчас», решаемость,
    последняя активность, чип темпа («отстаёт на 6») и прогресс-бар — тот же
    язык, что в ростере кампании;
  - колонка «Статус» у каждой работы недели: `сдано 8/10 80%` / `решает` /
    `не приступал` / `без выдачи` (файл, ссылка), с датой сдачи в подсказке;
  - в шапке недели — чип «сдано 2 из 4» (и у блока «Вне недель»);
  - новая секция «Общее задание класса»: блоки кампании со статусом именно
    этого ученика (сессия общая, поэтому матч по `attempt.student`; решение без
    входа в аккаунт сюда не попадает — так и подписано в подсказке).
- Миграции не потребовались.

## [3.9.128] - 2026-08-10

### 📊 Прогресс учеников на странице каникулярного задания

- **Проблема.** Блок статистики на странице кампании считался по полям самой
  программы (`study_programs.status`, `reviewed_at`), а `status` после выдачи
  никто не двигает — «Сдано» всегда показывало 0. О том, кто приступил, кто
  отстаёт и когда ученик последний раз что-то решал, страница не знала ничего.
- **Источник факта.** Попытки (`attempts`) по выдачам элементов плана. Выдача
  каждого элемента индивидуальна (`buildProgram.js`), поэтому попытка
  связывается с учеником **через сессию**, а не через `attempt.student` — верно
  считаются и те, кто решал анонимно, без входа в аккаунт.
- **Темп относительно плана.** У летних выдач нет дедлайна, роль срока играет
  `params.week`: неделя плана + `config.startDate` дают «ожидалось к сегодня».
  Отсюда статусы «в графике / отстаёт на N / не приступил / всё сдано».
- **Расчёт** — `components/workspace/summer/campaignProgress.js` (чистая функция
  `computeCampaignProgress`, 10 юнит-тестов). Считает только элементы с выдачей:
  файлы, ссылки и устный счёт факта выполнения не оставляют и в знаменатель не
  идут (подписано в UI).
- **UI** (`VacationCampaignDetail`): сводка — «Приступили X из Y», «Сдано работ
  N из M», решаемость класса, чипы распределения по темпу и кликабельный список
  не приступивших; в ростере — колонки «Выполнено» (прогресс-бар), «Темп»,
  «Решаемость», «Активность» («3 дн. назад»), все с сортировкой.
- **Данные** грузятся вторым этапом (страница не ждёт): новый
  `api.getCampaignProgramItems(programIds)` + `getAttemptsBySessions` с
  необязательным параметром `fields` (нужны `status`/`submitted_at`).
  Миграции не потребовались — правила чтения `attempts` и `study_program_items`
  учителя пускают.

## [3.9.127] - 2026-08-03

### 🔤 Переименование работы доезжает до плана программы и до выдачи

- **Проблема.** Название работы хранится копиями в трёх местах: `works.title`
  (редактор работ), `study_program_items.title` (план каникулярной программы —
  страница ученика, редактор программы, PDF) и `work_sessions.student_title`
  (заголовок при входе в тест). Копии проставлялись один раз, при добавлении
  работы в неделю (`buildProgram.js`), обратной связи не было → после
  переименования работы ученик продолжал видеть старое название.
- **Фикс в коде.** `api.updateWork` (`shared/services/pb/works.js`) при смене
  названия вызывает `syncWorkTitleSnapshots(workId, prevTitle, nextTitle)` —
  обновляет те копии в `study_program_items` и `work_sessions`, которые
  совпадали со старым названием (т.е. были снимком). Свои метки, заданные
  учителем вручную, не затираются. Ошибка синхронизации не роняет само
  переименование (лог в консоль).
- **Разовая синхронизация на проде** (03.08.2026, бэкап
  `backup_2026-08-03_20-59-00.tar.gz`): 117 элементов плана + 117 выдач у 13
  учеников — работы «Вычислительные навыки (N неделя)» → «(N неделя) 1/2».
  Тематические элементы (алгебра/геометрия) не трогали: там короткая метка
  «№21 — ЕГЭ-База…» вместо длинного названия работы — так и задумано.

## [3.9.126] - 2026-07-24

### 🛡 Устойчивость и безопасность (Горизонт 1 из аудита приложения)

- **Границы ошибок (ErrorBoundary).** Одна битая формула/задача/чертёж больше не
  роняет весь интерфейс белым экраном. `components/ErrorBoundary.jsx` подключён на
  двух уровнях: корень обоих приложений (`teacher/main.jsx`, `student/main.jsx`) и
  вокруг `<Outlet/>` в `AppLayout` (`resetKeys={[location.pathname]}` → при переходе
  на другой роут фолбэк сам гаснет). Фолбэк — дружелюбный экран с кнопками
  «Попробовать снова» / «Обновить страницу».
- **Глобальный учёт ошибок.** `shared/reportError.js` + `installGlobalErrorHandlers()`
  ловит `window.onerror` и `unhandledrejection`. Единая точка для будущего
  мониторинга (Sentry/бэкенд) — подключать туда.
- **Санитайзинг SVG (закрыт вектор stored-XSS).** `utils/sanitizeSvg.js` на DOMPurify
  (профиль svg) вырезает `<script>`, обработчики событий и `<foreignObject>`,
  сохраняя геометрию. Применён ко всем недоверенным SVG-инжектам (банк МЦНМО,
  SVG-редактор, GeoGebra→SVG): `GeometryTaskList`, `GeometryWorksheetPrint`,
  `geometry/SvgEditor`, `geometry/TabDrawing`. Покрыт тестом `__tests__/sanitizeSvg.test.js`.
- **Зависимости.** `npm audit fix` (без `--force`): 22 → 15 уязвимостей, все
  критичные устранены (jspdf, vitest, ws) + прямые high (react-router-dom, js-yaml);
  изменён только `package-lock.json`. Остаток — за major-апгрейдами, которые не
  делаем (BlockNote 0.52 требует React 19, vite 8, excalidraw).
- **Убран сторонний скрипт из прода.** Из `index.html` удалён случайный лефтовер
  `mcp.figma.com/.../capture.js` (грузился на боевой учительской странице).

## [3.9.125] - 2026-07-13

### 🧠 Поиск геометрических задач по смыслу (NL) + миграция индекса на te3

- **Поиск на естественном языке**: кнопка «🧠 Поиск по смыслу» в каталоге
  геометрии → `GeometrySemanticSearchModal` («биссектриса пересекает описанную
  окружность» → релевантные задачи банка/свои, фильтр Все/Банк/Мои, клик →
  быстрый просмотр). Ручка `POST /geo/search` (за ИИ-гейтом учителя): эмбеддинг
  запроса на сервере → KNN по vec_geometry; LRU-кэш запросов.
- **Индекс геометрии переведён с bge-m3 на `openai/text-embedding-3-large`
  @1024d** (та же размерность — схема vec_geometry не тронута). Эмбеддинги
  считает САМ VPS через Timeweb AI Gateway → Mac/Ollama для геометрии больше
  не нужны. Полный пересчёт 17 823 задач: ~55 мин, 0 ошибок, ~2.4 млн токенов
  (≈25–60 ₽ разово; поисковый запрос — доли копейки).
- **Серверная переиндексация**: `POST /geo/reindex` 🔒 (фоновая job, инкремент
  по text_hash+model) + `GET /geo/reindex/status` 🔒. Запуск с Mac:
  `npm run index:geo` (= `geo-reindex.mjs`, триггерит VPS и ждёт прогресс);
  старый путь `index.mjs --geometry` заблокирован (пуш bge-m3 отравил бы
  te3-индекс). Банк tasks (vec_tasks) остаётся на bge-m3/Mac как был.
- **⚠️ Грабля шлюза**: батч-ответы `/embeddings` Timeweb приходят с битым
  полем `index` (input из 3 → индексы [0,1,1]) — вектора терялись/путались.
  Фикс: только одиночные запросы (`embedRemoteOne`), 8 параллельных воркеров,
  единичный отказ не валит прогон (failed-счётчик, доедет инкрементом).
- **Перекалибровка под te3** (косинусы сжаты: почти-дубль ≈0.90, несвязанные
  ≈0.5): `toPctGeo` floor 0.50 / ceil 0.90 для «Похожих» и «Дублей с банком»;
  пороги дублей Строго 0.86 / Средне 0.82 / Широко 0.78 (дефолт 0.82 → 62 пары).

## [3.9.124] - 2026-07-12

### 🔎 Дедуп «мои ↔ банк МЦНМО» (векторный поиск геометрии, фаза 2)

- **Кнопка «Дубли с банком»** в списке геометрических задач →
  `GeometryBankDuplicatesModal`: пары «моя задача ↔ похожая в банке МЦНМО»
  бок о бок с бейджем похожести (%), меткой «ответы совпадают», переходом в
  быстрый просмотр обеих задач (👁) и отметкой «скрыть/просмотрено»
  (localStorage — данные не трогаются). Порог: Строго 0.92 / Средне 0.87 /
  Широко 0.82 (мягче дедупа банка tasks: у банка в тексте эмбеддинга есть
  фасетные теги, у своих задач нет → точный дубль даёт cos < 1).
- **Сервер**: `POST /geo/duplicates { min_cos?, per_task? }` в pdf-service —
  для каждой проиндексированной своей задачи ближайшие соседи из банка
  (`findGeometryBankDuplicates`). ~190 KNN по 17.8k векторов ≈ 20 с →
  in-memory кэш (TTL 10 мин, сброс при записи/прунинге векторов) + уступка
  event loop между пачками KNN (не блокирует /latex-fix и /similar).
- На первом прогоне по каталогу: 189 из 249 своих задач сравнимы (60 —
  «чертёж без текста»), найдено 40 пар на пороге 0.87, топ — реальные дубли
  (куб/призма/выбор точек на сторонах, 85–97%).

## [3.9.123] - 2026-07-12

### 🔎 Векторный поиск для геометрических задач (фаза 1)

- **Похожие геометрические задачи**: в «Быстром просмотре» задачи геометрии
  (`GeometryTaskList` → 👁) новый Collapse «🔎 Похожие задачи (вектор)» —
  панель `geometry/SimilarGeometryPanel` с бейджами похожести (%), чипом
  происхождения («Банк»/«Моя») и фильтром «Все / Банк МЦНМО / Мои»; клик по
  соседу переключает просмотр на него. Работает поверх текстов банка МЦНМО
  (17.6k задач, условия + фасетные теги объект/метод/факт).
- **Сервер (pdf-service)**: отдельная таблица `vec_geometry` в vec.db (bge-m3,
  1024d, cosine — как у банка задач, сущности не смешиваются) + ручки
  `POST /geo/similar`, `POST /geo/index-vectors` 🔒, `POST /geo/prune-vectors` 🔒
  (🔒 = X-Index-Token); `/similar/health` показывает `geometry_vectors`.
  В `vec-search.js` запись/прунинг обобщены (`indexVectorsInto`/`pruneVectorsFrom`).
- **Индексатор** (`vector-benchmark`): флаг `--geometry` (= `npm run index:geo`,
  полный пересчёт `index:geo:full`) — источник `geometry_tasks` (банк + свои),
  текст эмбеддинга `buildGeoText` = тема + фасетные теги МЦНМО + заголовок +
  условие; задачи «чертёж без текста» (сигнал < 30 симв.) не индексируются и
  вычищаются прунингом; `--full` теперь дропает только таблицы своего профиля
  (не весь vec.db); дедуп для геометрии не считается.
- Инкремент после добавления геометрических задач — вручную:
  `cd vector-benchmark && npm run index:geo` (как у банка задач).

## [3.9.122] - 2026-07-06

### 🖥 Онлайн-доска курса + новое вечернее расписание

- **Онлайн-доска курса**: у группы-курса новое поле `board_url` (миграция
  `1783700000`) — постоянная ссылка на онлайн-доску (Miro/Excalidraw и т.п.)
  для всего курса, рядом с комнатой конференции. Задаётся в форме группы
  (GroupManager), показана в «Кабинете курса» (GroupDetail). У ученика
  (StudentCoursePortal): кнопка «Доска» в шапке курса и в карточке ближайшего
  занятия рядом с «Подключиться».
- **Вечерние пары сдвинуты** (`lessonTime.js`): Вечерняя 1 = 18:00–19:30
  (45+45 без перерыва), Вечерняя 2 = 19:40–21:10 (было 19:00–21:00 /
  21:10–22:40). Ученический портал курсов считает время из того же PAIRS —
  при смене расписания деплоить и student app.

## [3.9.121] - 2026-07-03

### 🔄 Передача работы другому учителю

- Кнопка ⇄ на карточке в «Мои работы» (владелец/superadmin): Select коллеги →
  работа уходит ему **вместе со всеми выдачами и результатами** (варианты и
  попытки привязаны к работе/выдаче — едут автоматически). После передачи
  бывший владелец работу не видит (superadmin видит всё). API
  `transferWork()` в `pb/works.js` (+audit). Миграций не потребовалось.
- Разовая передача на проде: «Биатлон для 8 класса» и «Биатлон для 8 класса
  Тамила» (+их выдачи) переданы аккаунту vzmax — коллеги снова видят
  результаты каникулярного задания.

## [3.9.120] - 2026-07-02

### 🔄 Передача учеников между учителями + аккаунты от учителя (мультиучительство, этап 5)

- **Создание аккаунтов учителем**: в карточке группы кнопка «Аккаунты» —
  по имени на строку создаются полноценные ученические аккаунты (логин/пароль
  генерируются), сразу привязанные к группе и учителю. Логины и пароли
  показываются один раз таблицей + «Скопировать список» (в БД только хэш).
  API `createStudentAccount()` в `pb/students.js`.
- **Передача ученика**: в карточке ученика кнопка «Передать учителю» (владелец
  или superadmin) — Select коллеги → ученик со всеми результатами уходит в его
  списки; «ничьего» ученика можно привязать («Привязать к учителю», в т.ч. к
  себе). Бейдж «не привязан к учителю» в шапке карточки. API `transferStudent()`
  (+audit). После передачи чужому карточка закрывается (ученик больше не виден).
- **Миграция 1783600000**: `teachers.listRule` ослаблен до любого залогиненного
  учителя — нужен список коллег для Select'а передачи (create/update/delete
  остались строгими: superadmin / своя запись).

## [3.9.119] - 2026-07-02

### 👨‍🎓 Привязка учеников к учителю + скоуп аналитики (мультиучительство, этап 4)

- **Модель привязки**: ученик, созданный учителем («Вписать вручную»), сразу
  принадлежит ему; ученик из саморегистрации — «ничей» и виден всем учителям,
  пока первый учитель не добавит его в свою группу — в этот момент ученик
  «забирается» (claim в `setStudentGroup`, пишется в audit_log). Отвязка от
  группы владельца не сбрасывает.
- **Правила `students`** (миграция 1783500000): раньше список учеников был
  ПУБЛИЧНЫМ (аноним мог выкачать ФИО) — теперь list только учителям в скоупе
  «свои + ничьи» (superadmin — все), view — то же или сам ученик, update —
  editor+ в скоупе или сам ученик. Саморегистрация (create) не тронута.
- **Скоуп аналитики**: «Прогресс учеников» считает попытки только своих
  (+ничьих) учеников — фильтр `student.owner` в
  `getAttemptsForRegisteredStudents` (хелпер `andRelOwnerOrFree`). Тепловая
  карта уже скоупилась автоматически (идёт от работ), журнал — от групп,
  «Аналитика» банка задач остаётся общей осознанно.

## [3.9.118] - 2026-07-02

### 🤝 Шаринг работ (мультиучительство, этап 3)

Работу можно сделать общей: коллеги видят её в отдельной вкладке и могут
клонировать себе, но не могут править оригинал.

- **`works.visibility`**: пусто/`private` = личная (по умолчанию), `shared` =
  общая (миграция 1783400000). `works.listRule` расширен: учитель видит свои +
  общие; править/удалять может только владелец (superadmin — всё).
- **«Мои работы»**: кнопка ⤴ «Поделиться с коллегами» на карточке (владелец),
  зелёный бейдж «Общая» у расшаренных; повторный клик — снова личная.
- **Вкладка «Общие работы»**: список общих работ всех учителей с автором и
  темой; кнопка «Клонировать себе» — копия работы со всеми вариантами (задачи
  и порядок) падает в «Мои работы»; выдачи/попытки/папки не копируются.
- API `pb/works.js`: `getSharedWorks()`, `setWorkVisibility()`, `cloneWork()`
  (+audit). `teachers.viewRule` ослаблен до «любой залогиненный учитель» —
  для отображения автора общей работы (list остаётся superadmin-only).

## [3.9.117] - 2026-07-02

### 👥 Мультиучительство, этап 1: владелец у личных сущностей

Подготовка к открытию платформы для других учителей: всё «личное» получило
владельца-учителя, банк задач/теории/ТДФ остаётся общим.

- **Миграция 1783100000** (применена на ПРОД): поле `owner→teachers` добавлено в
  15 коллекций — `works`, `work_sessions`, `cards`, `mc_tests`, `marathons`,
  `qr_worksheets`, `pixel_art_worksheets`, `unit_circle_worksheets`,
  `trig_values_worksheets`, `cryptograms`, `route_sheets`, `formula_sheets`,
  `geometry_print_tests`, `pixel_art_team_sets`, `students`. Все существующие
  записи бэкфиллом получили `owner = evilfaust` (суперадмин).
- **API-слой**: хелперы `withOwner`/`andOwner`/`andOwnerOrFree` в `pb/client.js`;
  owner проставляется при создании, списки учительских разделов фильтруются
  «только моё». **Superadmin видит всё** (фильтр не накладывается). В ученическом
  приложении teacher-auth нет → хелперы прозрачны, student-фло не тронуто.
- Включена owner-фильтрация и в готовых workspace-коллекциях (группы, уроки,
  заметки, дела, КТП, курсы, программы, кампании).
- Ученики: списки «мои + ничьи» (саморегистрация владельца не имеет) — до
  отдельной модели привязки учеников к учителю.
- НЕ тронуто: `ext_journal_*` (owner = sync-аккаунт, входит в ключ
  идемпотентности), `listok_sheets` (своя модель шаринга published/cloned_from).

### 🔐 Мультиучительство, этап 2: серверные правила PB + ИИ-тумблер

- **Миграция 1783300000 — настоящие правила доступа** (~35 коллекций; раньше
  защита была UI-only): банк задач/теории/ТДФ/геометрии читается как раньше,
  но писать могут только учителя editor+ (аноним и viewer — нет); личные
  материалы (работы, выдачи, MC-тесты, рабочие листы, марафоны…) — списки и
  запись скоупятся владельцем, superadmin видит и правит всё; «Моё пространство»
  (уроки, заметки, дела, КТП, посещаемость) — строго владелец. Ученические пути
  не тронуты: `attempts`/`attempt_answers`/`variants`/`students` без изменений,
  `works`/`work_sessions`/`mc_tests` доступны ученику по id (view публичный),
  студенческие клаузы кампаний/программ/курсов сохранены дословно. Старый
  задеплоенный фронт продолжает работать (create не требует owner). Откат —
  down() восстанавливает снапшот правил прода от 02.07.2026.
- **Миграция 1783200000 — `teachers.ai_enabled`** (бэкфилл: всем включено) +
  колонка-переключатель «ИИ» в UserManager (суперадмин).
- **ИИ-гейт в UI**: при выключенном тумблере скрываются LLM-кнопки — «🤖
  Починить LaTeX» и «→ Таблица (AI)» (TaskEditModal), LLM-чекбокс перепарсинга
  (RefreshFromSdamgiaModal), 🤖-контролы импорта (TaskImporter), 📷 сканирование
  бланков (WorkManager), «Сгенерировать определение (AI)» (CryptogramGenerator).
- **ИИ-гейт на сервере**: `/latex-fix` и `/scan-blank` на pdf-service проверяют
  токен учителя через PB (`auth-refresh`, кэш 5 мин) и флаг `ai_enabled`; фронт
  шлёт токен (`aiHeaders()` из `pb/client.js`). Включается на VPS переменной
  `REQUIRE_TEACHER_AI_AUTH=1` **строго после деплоя фронта** (старый фронт токен
  не шлёт); без неё поведение прежнее.

## [3.9.116] - 2026-07-02

### 📷 Автопроверка бумажных бланков ответов №1 (сканирование фото)

Ученики решают распечатанный вариант и заполняют бланк ответов №1 как на
настоящем экзамене — учитель фотографирует бланк, ответы распознаются
автоматически и записываются как обычная попытка (вся статистика, журнал и
«работа над ошибками» работают из коробки).

- **«Мои работы» → кнопка 📷 «Проверить бумажные бланки»** на карточке работы:
  выбор варианта/ученика/выдачи → фото с камеры или из галереи (сжимается на
  клиенте до 1600px) → распознавание → **экран верификации**: таблица
  «распознано (редактируемое) ↔ эталон ↔ ✓/✗», подсветка неуверенных полей,
  бейдж «замена» для исправлений из зоны «Замена ошибочных ответов». После
  «Записать» модал готов к следующему бланку той же работы (пакетная проверка).
- Результат = `attempts` со `source='scan'` + `attempt_answers` через тот же
  `checkAnswer`, что и в ученическом интерфейсе; фото бланка сохраняется в
  `attempts.blank_photo` для спорных случаев (миграция 1783000000).
- **pdf-service: `POST /scan-blank`** — vision-LLM `gemini/gemini-2.5-flash`
  через Timeweb AI Gateway (модель выбрана сравнительным тестом 6 моделей на
  синтетическом бланке: единственная стабильно правильно читает зону замены;
  <0,5 ₽/бланк). Пост-фильтр полей за пределами числа задач варианта,
  замены применяются на сервере, `uncertain` — честные сомнения модели.
- Распознавание всегда идёт через ручную верификацию — автоматической записи
  без подтверждения учителя нет.

## [3.9.115] - 2026-07-01

### Курсы (онлайн-интенсивы) + кабинет курса для ученика

Новый тип учебной группы — **курс** (онлайн-интенсив в малой группе) с отдельным
кабинетом для учеников. Учитель ведёт занятия в календаре как обычно, а ученики
курса видят у себя расписание, ссылку на конференцию, материалы и ДЗ.

**Учитель:**
- В группе появился тип **Класс / Курс** и поле «Ссылка на конференцию (комната
  курса)» ([GroupManager]). Карточка курса помечена бейджем «курс».
- В карточке курса ([GroupDetail] → `CourseMembersSection`) — управление
  **участниками курса** (независимо от обычного класса: ученик может состоять
  и там, и там) + сводка кабинета (комната, число опубликованных занятий).
- В карточке урока курса ([LessonModal]) — блок **«Кабинет ученика курса»**:
  переключатель «Показывать ученикам», переопределение ссылки на конференцию,
  на каждом файле — тумблеры 👁 «виден ученикам» и «Классн./ДЗ», добавление
  **ДЗ-ссылок на уже выданные сессии** тестов и текстовых заданий.

**Ученик:** новый раздел **«Курсы»** (`StudentCoursePortal`) в личном кабинете —
расписание занятий с датой/временем, кнопка «Подключиться», материалы и блок
домашнего задания по каждому занятию.

**Модель (аддитивные миграции):**
- `teaching_groups`: `kind` (class|course) + `conference_url` (миграция 1782950010).
- `lessons.conference_url` — переопределение комнаты на занятии (1782950020).
- `course_members` — членство в курсе, ученик читает только свои строки (1782950030).
- `lesson_publications` — **витрина-проекция урока** для ученика: сюда попадают
  только видимые материалы/ДЗ/ссылка; приватная заметка урока и скрытые материалы
  физически не копируются (1782950040). Пересборка витрины — `api.syncLessonPublication`
  после сохранения урока курса.

## [3.9.114] - 2026-06-29

### Листки: правка задач + массовое изменение атрибутов

В просмотре листка ([ListokView]) для учителя добавлен **режим правки задач**
(кнопка «Править задачи»). Задачи листков живут в общем банке `tasks`
(`source='listok_gordin'`), но после парсера часть нуждается в доработке и не
имеет темы/подтемы — теперь это правится прямо из листка.

- **Иконка редактора задачи** ✏️ у каждой задачи → полноценный `TaskEditModal`
  (условие/решение, LaTeX-fix, перепарс с Решу и т.д.). После сохранения лист и
  справочные данные перечитываются.
- **Массовое изменение атрибутов** — чекбоксы выбора + drawer
  (`ListokTaskBulkDrawer`): перенос в каталог **Контекст → Тема → Подтема**
  (контекст фильтрует список тем), теги (добавить/заменить), сложность 1–5.
- **Попадание в общий каталог:** контекст задачи определяется темой
  (`topic.exam_type`), поэтому проставленная тема/подтема сразу делает задачи
  листка находимыми по фильтрам каталога. Задачи без темы помечены бейджем
  «без темы».

## [3.9.113] - 2026-06-29

### Листки по алгебре Р. К. Гордина (57 школа)

К планиметрии и стереометрии добавлен третий курс листков Гордина — **алгебра**
(`zadachi.mccme.ru/listki.alg`): 33 части, **817 задач** в общий банк `tasks`
(`source='listok_gordin'`).

- **Парсер расширен** под форматы алгебры: компактные списки задач (`div.parp`
  с ведущим `div.indent`), вложенные пункты (`div.pu1` — несколько задач в одном
  блоке), нормализация LaTeX `\sovok` (совокупность → `\left[\begin{array}…`),
  `\syst` со сбалансированными скобками, `\Ln`→`\ln`, `\defis`, гильеметы в
  математике. Все 10 770 формул трёх курсов компилируются в KaTeX (0 ошибок).
- **БД:** в select `listok_sheets.course` добавлено значение `algebra`
  (миграция `1779000037`).
- **UI:** в библиотеке «Листки» — фильтр/группа «Алгебра» (тон amber),
  метка курса в просмотре листа.

## [3.9.112] - 2026-06-28

### Каталог геометрии — двухколоночная панель фильтров

Длинные названия объектов/методов/фактов (и тем/подтем) не влезали в узкие
горизонтальные селекторы — а это главный инструмент поиска. Панель фильтров
`GeometryTaskList` переразбита на две колонки:

- **Слева (широкая):** фильтры-категории друг под другом во всю ширину —
  объект/метод/факт (банк) либо тема/подтема/источник («Мои»). Теперь длинные
  названия видны целиком; на опциях — `title` (нативный тултип).
- **Справа (узкая, 240px):** сложность, кнопка «Теги», «Сбросить» + «Обновить».

## [3.9.111] - 2026-06-28

### Банк МЦНМО — ленивая загрузка, чистка и редактирование тегов

Доработки каталога геометрии после интеграции банка (17.6k задач):

- **Ленивая загрузка банка.** Переключение на «Банк МЦНМО» больше НЕ тянет все
  17 634 задачи (тормозило, куча запросов). Теперь в режиме банка задачи
  грузятся только при выборе фасета (объект/метод/факт), поиске или сложности;
  без выбора — подсказка + кнопка **«Загрузить все 17 634»** (для тех, кому надо).
- **Чистка имён тегов.** Артефакты парсера в названиях тегов (`&laquo;`,
  `<f>n</f>`, `---`) почищены — 148 тегов нормализованы (сущности→юникод,
  убраны теги, `---`→«—»).
- **Редактор тегов.** Кнопка «Теги» в режиме банка → `GeometryTagsModal`:
  таблица всех объектов/методов/фактов с фильтром по типу и поиском, правка
  названия на месте и удаление (под `canEdit`). Backend `updateGeometryTag` /
  `deleteGeometryTag`.

## [3.9.110] - 2026-06-28

### Интеграция банка «Задачи по геометрии» (МЦНМО) — 17 634 задачи

В коллекцию `geometry_tasks` втащен весь открытый банк zadachi.mccme.ru
(Прасолов, Шарыгин, Атанасян, олимпиады) ради педагогической/методической
ценности — для 7–11 классов и углублённой подготовки, не только ЕГЭ.

- **Заливка:** **17 634 задачи** (`origin='mccme'`), 15 367 решений, 5440
  подсказок, **776 фасетных тегов**, 2514 чертежей. Старые 249 задач учителя
  не тронуты (`origin` пуст = свои). Ошибок при заливке 0 (6 задач с
  «ответом-простынёй» — длинный ответ перенесён в начало решения).
- **🔑 Архитектура — фасеты, не дерево.** Теги МЦНМО трёхмерны и пересекаются
  (одна задача = неск. объектов + методов + фактов). Поэтому помимо дерева
  `topic/subtopic` добавлен независимый фасетный слой `geometry_tags`
  (kind: object/method/fact, many-to-many через relation `tags`). Факты несут
  текст теоремы (`description`) — задел под «Теорию».
- **Конвертер формул** (`scripts/import-mccme/`): нестандартная разметка
  `<f>`/`<F>`/`$` МЦНМО → KaTeX. На 166 467 формулах — **0% битых** (словарь
  макросов `\mbox`/`\ctg`/`\syst`/`\eqno`), LLM-fix не нужен. Multi-чертежи
  (478 задач) склеиваются вертикально (`sharp`) в одно файловое поле.
  Идемпотентность по `mccme_id` (повторный прогон безопасен).
- **БД:** миграция `1782659636` — `geometry_tags` + поля
  `geometry_tasks.{origin,mccme_id,tags}` + `geometry_subtopics.parent`
  (задел под вложенные подтемы). На ПРОД 28.06.2026.
- **UI:** в каталоге геометрии (`GeometryTaskList`) — переключатель
  **«Мои задачи / Банк МЦНМО»**; в режиме банка вместо Темы/Подтемы/Источника
  три фасетных мультиселектора **объект / метод / факт** (AND-фильтрация).
  Backend `getGeometryTasks` += фильтры `origin`/`tags`, новый `getGeometryTags()`.
- **Чертёж = решение** (миграция `1782676504`, поле `geometry_tasks.image_role`).
  В банке МЦНМО **все чертежи относятся к решению**, а условие — чисто текстовое
  (в исходнике `pic[]` рендерится внутри блока решения). У всех 2518 задач банка
  с чертежом проставлено `image_role='solution'`. Рендер: чертёж больше НЕ
  показывается в условии-раздатке (`GeometryPreviewCard`, `GeometryWorksheetPrint`)
  — иначе ученик видел бы чертёж решения; вместо этого показывается в блоке
  решения (`TabSolution` редактора). Наши 249 задач (`image_role` пуст) — без
  изменений, чертёж как был в условии.

## [3.9.109] - 2026-06-28

### Новая фича «Листки» — листки Р. К. Гордина (57 школа) + свои листки учителя

Забраны в Лемму листки по планиметрии и стереометрии Гордина с
`zadachi.mccme.ru` и добавлен раздел для сборки собственных листков.

- **Импорт данных:** 41 официальный лист (30 планиметрия + 11 стереометрия),
  **1641 задача** в общий банк `tasks` (`source='listok_gordin'`, без темы —
  изолированы от каталога ЕГЭ/ОГЭ), 1650 элементов. Формулы конвертированы из
  LitMath-разметки в KaTeX (все 7413 валидны). Парсер/импортёр —
  `scripts/import-listki/` (`parse.mjs`/`recover.mjs`/`import.mjs`,
  идемпотентно по `code`/`slug`).
- **БД:** новые коллекции `listok_sheets` (kind official|teacher) +
  `listok_items` (task|heading) — миграции `1779000035`/`1779000036`.
- **UI (раздел «Листки», секция `listki`):**
  - Библиотека: 41 лист Гордина по курсам + «Мои листки» (`ListokLibrary`).
  - Просмотр листа (`ListokView`): теория части + задачи с бейджами сложности
    (°базовая / ∗трудная) и разделами «Дополнительные задачи»; печать-раздатка
    (`ListokPrint`, A4, 1/2 колонки, scoped print); «Выдать ученику» (создаёт
    работу → флоу «Мои работы»); «В свои листки» (клон официального).
  - Редактор своих листков (`ListokEditor`, editOnly): название, вводная
    теория, добавление задач из банка (`TaskSelectModal`) и заголовков-разделов,
    перестановка ↑/↓, удаление.
- API-модуль `shared/services/pb/listki.js` (+ barrel). Рендер — `MathRenderer`.

## [3.9.108] - 2026-06-26

### Редизайн раздела «Календарь» — три типа событий на одной сетке

Реализован дизайн-хэндофф `design_handoff_calendar/`. **react-big-calendar
оставлен** и кастомизирован (drag-перенос уроков/дедлайнов сохранён).

- **Третий тип событий — «Дела» (`teacher_todos`)** теперь на сетке наравне с
  уроками и дедлайнами (по `due_date`). Белый чип с интерактивным чекбоксом
  (цвет = группа дела или teal), флажок приоритета, зачёркивание выполненных.
  Чекбокс кликается прямо на сетке (`completeTodo`/`updateTodo`, оптимистично).
- **Кастомный вид «Неделя» по парам** (`WeekByPairs`) — сетка строк по `PAIRS`
  школы вместо почасовой шкалы; полоса «весь день» для дедлайнов/дел; клик по
  пустой ячейке пары → быстрое создание урока.
- **Правый рабочий рейл 330px** (`RightRail`, тумблер `showRail`): сводка недели
  (уроки/дедлайны/дела/«требует внимания»), фильтры типов + легенда групп,
  «Дела на сегодня».
- **Боковой инспектор события** (`EventInspector`, slide-over 380px) — заменяет
  всплывашку: контент адаптируется под тип (урок с материалами/посещаемостью ·
  дедлайн с переходом к работе · дело с чекбоксом и привязками).
- **Единое создание из сетки** (`CreateEventModal`) — табы Урок/Дедлайн/Дело.
  Дедлайн = выбор существующей работы → `createSession({ work, deadline })`.
- Кастомный тулбар (Сегодня/‹/› + период + Месяц/Неделя/День/Список), тумблер
  плотности сетки, статус «идёт сейчас» (обводка + пульс).
- Новые файлы — `ege-tasks/src/components/workspace/calendar/`. Шрифты
  Geist/Geist Mono (уже подключены). API-слой не менялся.

## [3.9.107] - 2026-06-26

### Общее задание класса теперь видно ученику в его кабинете

- **`StudentSummerProgram`** (кабинет ученика) загружает кампанию по
  `study_programs.campaign` и рисует общие блоки класса **над** индивидуальным
  планом: фиолетовые карточки с описанием, кнопкой «Решать» (если приложена
  работа), внешней ссылкой и файлами. Раньше блоки кампании показывались только
  в учительском предпросмотре/печати — в самом кабинете ученика их не было.
- **Миграция 1779000034** — открывает чтение `vacation_campaigns` залогиненному
  ученику (list/view). Запись — по-прежнему только учитель. Аддитивно, в духе
  UI-only безопасности платформы (как открыты `study_programs` в 1779000021).
- CSS-стиль группы кампании в кабинете (`student-summer-group--campaign`,
  фиолетовый акцент + тёмная тема).

## [3.9.106] - 2026-06-26

### Фикс: умный выбор программы при входе через кампанию

- `StudentProgramEditor` теперь загружает **обе** версии программы параллельно —
  по кампании и по сезону/году. Если кампания вернула пустой черновик, а по
  сезону нашлась выданная программа с заданиями — используется она. При этом
  программа автоматически привязывается к кампании (`campaign` поле) для
  единообразия в будущем.
- Исправляет ситуацию, когда `handleBulkCreate` создал черновики до фикса
  v3.9.105 (с `season: campaign.season` вместо `'summer'`), а «Построить»
  создал отдельную программу без привязки к кампании.

## [3.9.105] - 2026-06-26

### Фикс: задание ученика пропадало после возврата из кампании

- `assembleSummerProgram` теперь принимает `campaignId` и ищет программу через
  `getStudyProgramForCampaign` (а не `getStudyProgramForStudent`). Это исправляло
  ситуацию, когда «Построить» создавал **вторую** программу без привязки к кампании,
  и при повторном входе через `?campaign=XXX` находился пустой черновик.
- `handleBulkCreate` в `VacationCampaignDetail` теперь сохраняет `season: 'summer'`
  (а не `campaign.season`), чтобы программа находилась как обычным поиском, так и
  поиском по кампании.

## [3.9.104] - 2026-06-26

### Общее задание кампании отображается у ученика

- **`SummerProgramPrintView`** теперь принимает `campaignBlocks` — блоки общего
  задания класса рендерятся перед индивидуальными неделями. Каждый блок оформлен
  как секция с заголовком, описанием, ссылкой на работу (с QR-кодом) и файлами.
- **`StudentPreview`** (Drawer «Глазами ученика» в редакторе программы) тоже
  показывает блоки кампании фиолетовыми карточками над неделями.
- **`StudentProgramEditor`** загружает кампанию параллельно с программой ученика
  (когда передан `?campaign=XXX`) и пробрасывает `campaignBlocks` в оба места.

## [3.9.103] - 2026-06-26

### Иерархия каникулярных заданий — кампании + общее задание для класса

- **Новая сущность `vacation_campaigns`** (миграция 1779000032): кампания объединяет
  каникулярные задания для одного класса. Поля: название, сезон (свободный текст —
  «Лето», «Зима», «Майские праздники»), год, группа, дедлайн, инструкция, режим
  (`individual` / `identical`), статус (`draft` / `issued` / `archived`).
- **`study_programs` расширена** (миграция 1779000033): поля `campaign` (привязка к
  кампании, nullable) и `reviewed_at` (дата проверки учителем).
- **Страница `/app/summer`** теперь показывает список всех кампаний по годам.
  Кнопка «Новая кампания» открывает модал создания (название, сезон, группа, дедлайн,
  режим, инструкция). Старый индивидуальный список — `/app/summer/individual`.
- **Страница кампании `/app/summer/campaign/:id`** — два раздела:
  - **«Общее задание для класса»** — блоки с описанием, прикреплённой работой из
    «Моих работ», внешней ссылкой и файлами из библиотеки материалов. Несколько блоков
    можно добавлять и редактировать; сохранение автоматическое (debounce 800 мс).
  - **«Индивидуальные задания»** — ростер учеников класса со статусом программы,
    переключателем «Проверено» и кнопками «Открыть» / «Создать». Кнопка
    «Создать черновики для всех» массово создаёт `study_programs` для всех учеников
    без программы одним кликом.
- **`StudentProgramEditor`** принимает `?campaign=XXX` — при создании новой программы
  автоматически привязывает её к кампании.
- **API `pb/campaigns.js`** (`campaignsApi`): `getCampaigns`, `getCampaign`,
  `createCampaign`, `updateCampaign`, `deleteCampaign`, `getCampaignPrograms`,
  `markReviewed`. Подключён в barrel `pocketbase.js`.
- **Миграция данных**: существующие 12 летних программ 10 класса привязаны к новой
  кампании «Летнее задание · 10 класс · 2026» (группа «10 кл»).

## [3.9.102] - 2026-06-24

### Редизайн раздела «Моё пространство» — карточный язык + дашборд «Сегодня»

- **«Сегодня» (`TodayDashboard`) переписан в дашборд одного окна** по дизайн-хэндоффу
  (`design_handoff_today_dashboard/`). Новое: навигатор недели (точки-индикаторы
  уроков, выбор дня, ±неделя), **живой hero «Идёт сейчас»** (тёмная карточка, пульс,
  прогресс-бар урока — обновляется раз в минуту, авто-переключение на «Далее в HH:MM»),
  KPI 2×2 (уроков сегодня / к подготовке / дедлайнов / просрочено), расписание дня с
  чипом материалов и кнопкой «Подготовить», бэклог «К подготовке на неделе»,
  «Требует внимания» (дедлайны) и «Заметки». Кнопка «отметить проведённым» сохранена.
  Подкомпоненты — `workspace/today/` (`WeekNavigator`, `NowHero`, `KpiCluster`, `today.css`).
- **Логика времени уроков вынесена в общий `workspace/lessonTime.js`** (`PAIRS`,
  `slotRangeFromCode`, `endForLesson` + новые `lessonStartEnd`/`lessonProgress`) —
  единый источник истины для календаря и дашборда (дубль из `TeacherCalendar` убран).
- **Общий UI-слой расширен** (`workspace/ui/`): `SectionCard` + классы `.ws-card`
  (карточка-секция), `.ws-grid`/`.ws-tile` (сетка карточек-сущностей), `.ws-roster`
  (ростер людей) — единый «карточный» язык раздела.
- **Страницы раздела переведены в новый стиль** (визуальный апгрейд, логика та же):
  - `GroupManager` / `KtpList` — таблицы → сетки карточек (цветной значок по тону группы,
    мета, действия на hover);
  - `GroupDetail` — `Card`/`Descriptions`/`List` → `SectionCard` + ростер учеников
    с аватарами-инициалами;
  - `GradeJournal` — таблица в `.ws-card`, аватар ученика в первой колонке;
  - `KtpEditor` — таблица в `.ws-card`;
  - `MaterialsLibrary` — область загрузки, папки и файлы → `.ws-card`/`.ws-tile`.
- **Заметки (`NotesWorkspace`)** — панели подняты до нового стиля (скругление 12px +
  тень). **Фикс «мёртвой» пустоты снизу**: маршрут помечен `noMargin`, страница стала
  flex-колонкой на всю высоту контента — скроллятся только внутренние панели, внешнего
  скролла больше нет (мобильная ветка скроллит страницу целиком).
- **Цвет папок в Библиотеке** — значок папки красится детерминированным оттенком из
  5-палитры по имени/id (как цвет групп), стабильный для каждой папки.

## [3.9.101] - 2026-06-24

### Поиск по всей базе + чертежи в модалке добавления задачи

- **Поиск задач при добавлении в вариант — по всему каталогу** (`TaskSelectModal`).
  Раньше поле поиска фильтровало только уже подгруженные страницы (клиентски), и
  нужная задача часто «не находилась», если не попала в загруженное. Теперь запрос
  уходит на сервер (`code ~` / `statement_md ~`), с дебаунсом 300 мс и серверной
  пагинацией — один запрос на ввод, без подвисаний.
- **Поиск сужается выбранным контекстом/темой** — если контекст/тема/сложность/теги
  заданы, ищем внутри них; если ничего не выбрано — по всей базе. Контекст-каскад
  больше **не обязателен** перед поиском: непустой запрос сам запускает загрузку.
- **Чертежи рендерятся в карточках модалки** — для ориентира. Legacy-поле
  `tasks.image` (большинство импортированных ОГЭ/база) показывается как превью
  (220×180, клик = зум), inline-картинки `![](url)` из условия ограничены по ширине.
  **Без дополнительных запросов** — все поля уже приходят в выдаче `getTasksPage`.
- ⚠️ Кириллический поиск через SQLite `LIKE` регистрозависим (латиница/код — нет);
  поиск по середине слова обычно совпадает, при промахах по регистру — доработаем.

## [3.9.100] - 2026-06-24

### Простая печать работы — стиль «рабочего листа» без клетки

- **Переписан простой режим печати работ** (`WorkPrintPreview`) под стилистику
  «Рабочего листа»: чистые A4-листы с серифом, шапка, поля ФИО/Дата, лист ответов
  для учителя — но **без клетки** (вместо сетки пустое поле для решения,
  опционально с тонкой рамкой). Раньше режим рендерил `VariantRenderer` +
  `AnswersPage` (стили `TaskWorksheet.css`) с синим Ant-акцентом.
- **Компоновка по числу задач на листе** — селектор «Задач на листе» (1–12),
  каждый вариант пагинируется по этому числу, блоки равномерно заполняют лист.
- **Тумблер слова «Вариант»** — печатать/не печатать подпись «Вариант N»
  (в шапке листа и в листе ответов учителя).
- **Только чёрный** — заголовки, бейджи `№N`, разделители, линии полей и лист
  ответов учителя — всё `#000`, без синего/красного (лучше при ч/б печати).
- **Переключатель начертания** «Прямой / Курсив» для текста условия.
- **Узкие поля + фикс печати** — поля листа `6mm` (ответы `8mm`); устранён «съезд
  вправо» при печати/PDF: в `@media print` лист тянется по ширине печатной области
  (`width:100%`, размер задаёт `@page { size:A4; margin:0 }`) — фиксированные
  `210mm` + `margin:auto` внутри абсолютного print-root вылезали за границу.
- Контролы из рабочего листа сохранены: размер текста S/M/L, чертежи + размер
  S/M/L/XL, ФИО/Дата, ответы учителя, «без префиксов», редактируемый заголовок;
  кросс-ссылка «Рабочий лист» (клетка) и кнопки Печать / PDF на месте.

## [3.9.99] - 2026-06-23

### Markdown в доп. задании + автосохранение текстов памятки

- **Markdown в «Дополнительном задании»** каникулярной программы. Редактор —
  `LatexField` (CodeMirror с подсветкой markdown + KaTeX-линтер, как в карточке
  задачи) с живым предпросмотром. Текст рендерится через `MathRenderer` в печатном
  PDF, «Глазами ученика» и кабинете ученика: `[текст](url)` и голые ссылки
  становятся настоящими кликабельными ссылками (удобно давать долги по тестам),
  работают `**жирный**`, списки, таблицы, формулы `$x^2$`.
- **Ссылки markdown открываются в новой вкладке** — в общий `MathRenderer`
  добавлен рендер `<a target="_blank" rel="noopener noreferrer">` (по всему
  приложению; в PDF кликабельны через `enableLinks`).
- **Автосохранение текстов памятки** — «Напутствие» и «Текст в памятке» теперь
  сохраняются и при закрытии модалки «Памятка ученику» (крестик/«Отмена»/клик вне),
  а не только по кнопкам «Скопировать»/«PDF». Пишется в БД только при изменении.

## [3.9.98] - 2026-06-23

### Каникулярное задание — красивый PDF + порядок классов

- **Памятка ученику → красивый PDF/печать** (`SummerProgramPrintView`). Вместо
  простого «скопировать текст» и грубой печати — оформленный лист A4: обложка с
  именем ученика, периодом и счётчиками (недель/работ), напутствие учителя,
  свободный текст учителя, карточки недель с заданиями, чекбоксами и **QR-кодом
  на каждую работу**. 5 акцентных цветов на выбор. Печать (`window.print` с
  инъекцией `@page`) и клиентский PDF (`html2pdf`, `enableLinks`).
  - **Кликабельные ссылки** — короткая ссылка под заданием и сам QR обёрнуты в
    `<a href>`: открыв PDF на телефоне, ученик тапает и сразу попадает в тест.
  - **Дополнительное задание** — секция с текстом учителя, работами вне недель
    (QR/ссылка) и внешними ссылками/файлами.
  - В модалке «Памятка ученику» добавлено поле «Текст в памятке» (`config.printNote`),
    кнопка «Красивый PDF / печать» открывает полноэкранный предпросмотр.
- **Порядок классов/групп** — в разделе «Классы и группы» кнопки ↑/↓ у каждой
  строки меняют порядок (поле `teaching_groups.sort_order`, миграция
  `1779000027`). Сортировка `sort_order,-created`; пока порядок не трогали —
  список как раньше (новые сверху). API `reorderTeachingGroups`.

## [3.9.97] - 2026-06-20

### Переименование учеников в карточке группы

- В `GroupDetail` у каждого ученика — действие «✏️» → модалка «Переименовать»
  (правка поля `name` через `api.updateStudent`). Удобно дописать фамилию тем,
  кого вписали вручную только по имени. Работает и для обычных учеников.

## [3.9.96] - 2026-06-20

### Геометрия — размер текста карточек + компактная печать «Просмотра»

- **Размер текста карточек (S/M/L)** — единый стиль с «Размер текста» рабочих
  листов. Контрол в тулбаре «Просмотра» (`GeometryTaskPreview`) и в модалке
  «Быстрый просмотр» (`GeometryTaskList`). M = прежний размер, S — мельче (для
  задач с большим объёмом текста, которые не влезали на карточку), L — крупнее.
  Множитель `TEXT_SIZE_SCALE` поверх per-task `fontScale` (ручная правка макета
  по-прежнему работает).
- **Компактная печать** — поля листа «Просмотра» сужены 5мм → 3мм, поля ячеек и
  шапки листа уменьшены, бейдж номера компактнее. Свободное пространство листа
  используется рациональнее.
- **Убран синий** — бейдж номера задачи теперь чёрный (`#1f1f1f`), а не синий:
  при ч/б печати синий выходил бледным, чёрный — плотным. Панель шапки листа и
  подсказка-тег перетаскивания — нейтрально-серые.

## [3.9.95] - 2026-06-20

### Геометрия — вложения-фото к решению + 5 уровней сложности

Для геометрических задач часто проще сфотографировать решение с бумаги, чем
набирать его в LaTeX. Добавлена возможность прикреплять файлы к решению через
наш файловый сервер.

- **Вложения к решению** — поле `geometry_tasks.solution_files` (json-массив
  ссылок `{id, title, url, mime}`, миграция `1779000026`, применена на ПРОД
  20.06.2026, аддитивно). Файлы физически живут в хранилище **pb-files**
  (`files.l.oipav.ru`), в задаче хранятся только ссылки.
- **Компонент `geometry/SolutionAttachments.jsx`** на вкладке «Решение»:
  загрузка фото/PDF (drag-drop или кнопкой) на pb-files через `materialsApi`,
  список с превью (`FilePreviewModal`), открепление. Требует подключённого
  хранилища (логин в «Библиотеке материалов») — иначе подсказка. Ссылки
  копируются при дублировании задачи (`duplicateGeometryTask`).
- **Сложность 1→5** — `geometry/TabCondition.jsx`: селектор уровня сложности
  доведён до канона системы (1 Базовый / 2 Средний / 3 Повышенный / 4 Высокий /
  5 Олимпиадный). Раньше в редакторе геометрии было только 3 уровня, причём с
  расхождением подписи («3 — Сложный»); фильтр списка уже был на 5.

## [3.9.94] - 2026-06-19

### Ученики «без аккаунта» — ручное вписывание для разовых занятий

Для разовых/внешних занятий (продвинутые ученики чужих преподавателей) —
возможность вписать ученика по имени, чтобы фиксировать продвижение в заметках,
без тестов и ДЗ.

- **Флаг `students.external`** (миграция `1779000025`, применена на ПРОД
  19.06.2026, аддитивно). Помечает учеников, вписанных вручную.
- **API `createManualStudent({ name, groupId })`** — создаёт `students`-запись
  с именем, привязкой к группе, `external=true` и авто-логином/паролем-заглушкой
  (createRule students открыт). `getStudents` отдаёт `external`.
- **Карточка группы** (`GroupDetail`): кнопка «Вписать вручную» → модалка (имена
  по строкам). Бейдж «без аккаунта», ссылка «профиль» у каждого ученика, действие
  «убрать» (отвязка) для внешних.
- **Переиспользование**: внешние ученики попадают в ростер посещаемости и в
  «Ученики группы» заметки урока, получают карточку с заметками/посещаемостью.
- **Скрыты из тестовых разделов**: Журнал сдачи (`getGroupJournal`), Прогресс
  учеников (`StudentProgressDashboard`), Летняя программа (`SummerProgramList`).
  Карточка ученика (`StudentDetailPage`) работает и для них (бейдж «без аккаунта»).

## [3.9.93] - 2026-06-19

### Заметки — хронологический список вместо группировки по классам

- На странице «Заметки» убрана группировка списка по классам/группам (и
  секция «Инбокс» в общем виде). Теперь: «Закреплённые» сверху → дальше все
  заметки единым хронологическим потоком (по `-updated`). Группа по-прежнему
  видна чипом на каждой заметке; фильтр по группе и вкладки Все/Инбокс/Архив
  сохранены. `NotesWorkspace.jsx` — упрощён `sections`.

## [3.9.92] - 2026-06-19

### Ученики в заметке урока — индивидуальные траектории

Вставка учеников группы в заметку урока + связь заметки с карточкой ученика.

- **Кнопка «Ученики группы»** в редакторе заметки (`NoteEditor` →
  `InsertStudentsModal`): модалка со списком учеников группы заметки (все
  отмечены, снимаешь ненужных) → на каждого вставляется раздел
  (заголовок-имя + место под траекторию). Свободный текст BlockNote (формулы,
  списки и т.д.). Доступна, когда у заметки указана группа.
- **Тег ученика в `teacher_notes.links`** (`{type:'student', id, name}`) — без
  миграции БД, переиспользует существующее json-поле.
- **Секция «Заметки уроков» в карточке ученика** (`StudentDetailPage`): список
  заметок, где ученик отмечен, с датой урока, группой и кнопкой «Открыть»
  (→ `/app/notes?note=`). API `pb/notes.js::getNotesByStudent` (фильтр по `links`
  + уточнение в JS).

## [3.9.91] - 2026-06-19

### Посещаемость на уроках + лента в карточке ученика

Учёт присутствия учеников прямо в карточке урока календаря.

- **Новая коллекция `lesson_attendance`** (миграция `1779000024`, применена на
  ПРОД 19.06.2026): по строке на `(урок, ученик)`, статусы
  `present|late|excused|absent`, `cascadeDelete` на урок и ученика, уникальный
  индекс `(lesson, student)`. Строки создаются **лениво** — только при отметке.
- **Список учеников НЕ дублируется** — берётся из группы урока
  (`lessons.group → teaching_groups`, `students.teaching_group`).
- **Ростер в `LessonModal`** (`AttendanceRoster.jsx`): ученики группы + Segmented
  на 4 статуса в строке, кнопка снять отметку, «Все были», сводка «был N из M».
  Для нового (несохранённого) урока — подсказка сохранить сперва; без группы —
  подсказка указать группу.
- **Лента в карточке ученика** (`StudentDetailPage`): секция «Посещаемость» —
  сводка по статусам + список по датам уроков (новые сверху).
- API `pb/attendance.js`: `getLessonAttendance`, `setAttendance` (upsert, пустой
  статус = удалить строку), `getAttendanceByStudent`.

## [3.9.90] - 2026-06-19

### Колонка «Успеваемость» — честный показатель решаемости (база/профиль/ОГЭ)

Переработка показателя в генераторах вариантов ЕГЭ-база/профиль/ОГЭ.

- **Источник вместо фейкового `tasks.success_rate`** (там ~10.7к нулей по
  умолчанию, тянувших все темы в красное) — реальные ответы учеников
  (`attempt_answers`). Поле `success_rate` больше не читается.
- **Пулинг по теме** `Σверных/Σвсего` (а не среднее по средним) + **нижняя
  граница Уилсона** для цвета (чтобы «верно 2 из 2» не горело зелёным) + число
  наблюдений `n`.
- **Внешние данные Решу** (`ext_journal_task_results` через мост
  `problem_id→sdamgia_id`) подмешиваются к показателю: бейдж «Р» и разбивка
  «наши ученики vs Решу» в тултипе.
- **Оценка-усадка для тем без/с малыми прямыми данными**: вместо «нет данных»
  показывается «≈ N%» — усадка к среднему по каталогу (помечена как оценка,
  бледно). Валидация (leave-one-out) показала, что усадка даёт ту же точность,
  что и векторный перенос, без какого-либо пайплайна.
- Логика вынесена в `utils/successStats.js` (Уилсон, пулинг, усадка) + общий
  компонент `worksheet/SuccessRateCell.jsx`; данные — хук `useTaskStats`
  (кэш на сессию) + `answersApi.getInternalTaskStats` /
  `extJournalApi.getExternalTaskStatsByProblem`.

## [3.9.89] - 2026-06-17

### Лист ответов для учителя в режиме КИМ (база/профиль/ОГЭ)

- Рядом с «Распечатать КИМ» — новая кнопка **«Лист ответов (учителю)»**: печатает
  отдельно от ученического буклета только ответы, на **A4 с полями** (буклет
  остаётся A5 без полей). Для ручной проверки бумажных работ.
- Изоляция печати: класс `body.printing-kim-answers` + инъекция `@page A4`
  (перебивает постоянный КИМ-овский A5/margin:0); на время печати буклет
  скрывается, показывается лист ответов. После печати всё откатывается.
  Помощник — `utils/printKimAnswers.js`.
- Содержимое переиспользует существующие компоненты ответов: база — сетка
  `№ → ответ`; профиль/ОГЭ — краткие ответы части 1 + **развёрнутые решения
  части 2** (`solution_md`) с баллами.
- **Заголовок листа = название работы** (`currentWork.title`), а не «Вариант N» —
  осмысленное имя сразу показывает, к какой работе ответы. «Вариант N» в
  подзаголовках остаётся только при нескольких вариантах. Если работа не
  сохранена — дефолтный заголовок «Ответы». На экране лист скрыт (печатный
  артефакт).

## [3.9.88] - 2026-06-17

### Фикс печати КИМ — пропадали последние задачи (база/профиль/ОГЭ)

- В базовом варианте ЕГЭ при печати в стиле КИМ **пропадали задачи №18–21**, а
  футер обложки уезжал на пустой 2-й лист, сдвигая всю вёрстку. Причина — не
  числовые прямые (данные/рендер были ок), а **изоляция печати**: буклет не был
  «прибит» к началу листа, и любой видимый при печати элемент выше по потоку
  (отступы layout/Card) сдвигал обложку (206mm) за край листа → каскад → хвост
  обрезался.
- **Фиксы:** (1) `.kim-booklet` в печати теперь `position: absolute; top: 0;
  left: 0; width: 100%` — канонический паттерн многостраничной печати проекта;
  (2) постоянная инъекция `@page { size: A5 portrait; margin: 0 }` в `<head>`,
  пока активен режим КИМ (перебивает глобальный `@page { margin: 10mm 8mm }` из
  `TaskWorksheet.css` при любом способе печати — кнопкой и Cmd+P; как в
  `OgeVariantGenerator`); (3) высота печатной страницы 210→206mm + пагинация под
  неё — запас под футер от округлений печати.

### Размер чертежей в КИМ — переключатель S/M/L/XL per-задача

- У задач **с чертежом** в редактируемом виде варианта (режим КИМ) появился
  переключатель размера картинки `S / M / L / XL` рядом с кнопками
  редактирования. `M` = прежний размер (старые работы не меняются).
- Размеры (высота / макс. ширина блока): S — 24мм/35%, M — 35мм/48%,
  L — 55мм/70%, XL — 85мм/100%. Удобно укрупнить мелкие графики (напр. №3 базы).
- Работает во **всех КИМ-генераторах** (базовый, профильный, ОГЭ) через общий
  `VariantRenderer` + util `utils/kimImageSize.js`. Размер применяется и в фазе
  измерения, и в печати — пагинация учитывает увеличенную картинку.
- **Сохраняется в работе** без миграции БД — в json-поле `variants.order`
  (`imageSize`), восстанавливается при открытии работы.

## [3.9.87] - 2026-06-17

### Числовые прямые со штриховкой (условия задач + теория)

- Новый синтаксис — fenced-блок ` ```numline ` с компактным DSL (`domain`,
  `ray`, `seg`, `point`, `tick`), который **одинаково работает в обоих
  конвейерах рендеринга**: в условиях/решениях задач (react-markdown →
  `MathRenderer`) и в теории (`useMarkdownProcessor` → HTML-строка).
- Рендер — чистый inline-SVG со штриховкой интервалов, выколотыми ○ /
  закрашенными ● точками и стрелкой оси. SVG собран без `<defs>/<pattern>/
  <marker>` (стрелка — `<path>`, штриховка — аналитически обрезанные диагонали),
  чтобы надёжно проходить DOMPurify и корректно печататься в Chrome. Единый
  источник истины — `utils/numberLine.js` (`numberLineSvg`), один и тот же для
  React-компонента и строкового конвейера.
- **Inline-форма для ячеек таблиц**: `` `numline: domain 0 2; ray left 1 open` ``
  (код-спан, команды через `;`). Fenced-блок не работает внутри markdown-таблиц
  (содержимое ячейки — только inline), поэтому для задач-на-соответствие
  используется компактный inline-вариант. Ловится в обоих конвейерах (`code`-
  override в `MathRenderer`, regex `<code>numline:…` в `useMarkdownProcessor`).
- **Визуальный конструктор** `NumberLineModal` с живым превью: диапазон оси +
  список фигур (луч/отрезок/точка, выбор open/fill) + переключатель формата
  (блок / в строку). Кнопка в тулбаре редактора теории (`EditorToolbar`) и в
  `TaskEditModal` для полей «условие» и «решение» (там формат по умолчанию —
  inline, т.к. прямые обычно в таблице соответствия).
- **Второй тип — «точки на прямой»** (задачи «отметьте точки A, B, C, D»):
  команды `scale FROM TO [STEP]` (линейка с целыми засечками и числами) и
  `mark LABEL X` (помеченная точка над осью). В конструкторе — переключатель
  типа «Неравенства (штриховка) / Точки на прямой».
- **Буква оси настраивается** (`axis y` в DSL, поле «Буква оси» в конструкторе) —
  раньше всегда было `x`.
- **Фикс**: луч влево больше не рисует вторую (левую) стрелку — у оси всегда одна
  стрелка вправо, уход в −∞ показывает штриховка до края (как в ФИПИ).
- **Дробные значения**: координату можно вводить дробью (`1/2`, `-3/4`) — точка
  встаёт в нужную позицию, а подпись рисуется настоящей дробью (числитель/черта/
  знаменатель) нативным SVG, без KaTeX. Холст авто-увеличивается под дробь.
  В конструкторе координаты теперь текстовые (принимают и дроби, и десятичные).
- **Строгий монохромный стиль** (по просьбе, не под решуЕГЭ): вся графика
  чернильная (`COLORS`: axis `#1f2937`, hatch `#4b5563`, tick `#374151`), тонкие
  линии, плотная штриховка — чисто печатается в Ч/Б.
- Покрыто 26 юнит-тестами (`numberLine.test.js`).

### Авто-починка markdown-таблиц

- `utils/markdownTables.js::autofixTableDelimiters` — если у pipe-таблицы
  пропущена обязательная строка-разделитель `|---|---|`, GFM не считает блок
  таблицей и рендерит сырой текст с палками (частая беда при правках в ячейках).
  Теперь рендер сам вставляет разделитель по числу столбцов заголовка.
  Подключено в `MathRenderer` (условия задач) и `useMarkdownProcessor` (теория).
  Консервативно: трогает только заголовок в начале блока, у которого следующая
  строка — тоже pipe-строка, но не разделитель; корректные таблицы не меняет.
  7 юнит-тестов (`markdownTables.test.js`). **Числовая прямая таблицу не ломает**
  (проверено remark-gfm) — ломало именно отсутствие разделителя.

## [3.9.86] - 2026-06-17

### Редизайн кнопок ученического интерфейса

- Единая кнопочная система на CSS-переменных (`--st-brand` и др.) в `.student-app`:
  убран устаревший «глянцевый» сине-фиолетовый градиент (`#4361ee → #3a0ca3`) и
  тяжёлые тени. Primary-кнопки теперь — плоский брендовый индиго с мягкой тенью,
  аккуратными hover/active-состояниями. Контурные кнопки (`default`) — спокойная
  заливка вместо разнобоя. Стартовая кнопка входа в тест приведена к тому же языку.
- **Страница «Каникулярное задание»**: карточки недель получили мягкую тень,
  разделитель шапки и hover-подсветку строк; строка задания перестроена
  (тег + название слева, кнопка справа, без «съезжания» кнопки на отдельную
  строку). Кнопки «Решать»/«Открыть» — единый аккуратный размер (38px, радиус 10),
  длинные подписи файлов обрезаются. Тёмная тема обновлена.

## [3.9.85] - 2026-06-16

### Фикс: невидимый текст кнопки на главной ученика

- Кнопка «Открыть каникулярное задание» в личном кабинете ученика
  (`StudentHomeLanding`) рендерилась с `type="primary"` (сплошной синий фон),
  но класс `.student-home-nav-btn` задаёт синий текст/иконку → синее на синем,
  текст не читался. Убран `type="primary"` — кнопка стала outline (синий текст +
  рамка на светлом фоне), как и задумано классом. Тёмная тема не затронута.

## [3.9.84] - 2026-06-16

### Каникулярное задание: файлы к неделе + дополнительное задание

- **Файлы к недельному периоду** (`config.weekFiles[week]`) — в редакторе
  каникулярного задания у каждой недели кнопка «Файл недели» (выбор/загрузка из
  Библиотеки материалов). У каждого файла — опциональное короткое пояснение
  (inline, сохраняется по blur). Файлы недели отображаются в ученическом
  интерфейсе внутри блока соответствующей недели.
- **Дополнительное (творческое) задание вне недель** (`config.extra =
  { text, files, links }`) — отдельная секция-редактор под доской недель: текст
  задания, прикреплённые файлы (с пояснениями) и список ссылок (URL + подпись).
  У ученика показывается отдельным блоком «✨ Дополнительное задание» только при
  наличии заполненного контента.
- Предпросмотр «Глазами ученика» в редакторе также отображает файлы недель и
  дополнительное задание.
- Без миграции БД — всё хранится в существующем JSON-поле `study_programs.config`.

## [3.9.83] - 2026-06-16

### Редактор работ: рендер чертежей в составе задач

- В табе «Состав» условие задачи теперь рендерится через `TaskStatementRenderer`
  (под-компонент `WorkTaskStatement`): для задач из «Решу ЕГЭ» вшитые в `statement_md`
  внешние картинки `![image](url)` подменяются на локальные `task_images` (sdamgia
  блокирует прямой fetch из браузера → раньше чертёж не отображался). Особенно важно
  для геометрии, где ориентироваться можно только по чертежу.
- `task_images` грузятся лениво и только если в условии реально есть inline-картинка —
  для большинства задач без картинок лишних запросов нет. Legacy-поле `task.image`
  работает как прежде.

### «Мои работы»: закрепление + папки-ярлыки

- **Закрепление** `works.is_pinned` (миграция 1779000022): кнопка 📌, закреплённые
  наверху списка (стабильная сортировка), метка в заголовке.
- **Папки-ярлыки** `works.folder` (миграция 1779000023): одноуровневая текстовая метка.
  Кнопка «папка» на карточке (AutoComplete с подсказками существующих папок), фильтр
  «Папка» в шапке, а при показе всех — группировка карточек заголовками папок.
  **Закреплённые** вынесены в секцию «Закреплённые» над всеми папками. Заголовки папок —
  **сворачиваемые** (клик прячет карточки, оставляя счётчик; состояние в localStorage;
  не аккордеон — папки независимы). Решает разрастание списка после массовых каникулярных заданий.

## [3.9.82] - 2026-06-15

### Индивидуальная летняя программа занятий (per-student)

Новый раздел «Моё пространство → Летняя программа»: на основе профиля слабостей
каждого ученика собирается персональный план на лето с цифровой выдачей в
ученическое приложение.

- **Профиль слабостей** (`shared/utils/weaknessProfile.js` + хук
  `useStudentWeaknessProfile`) — сводит внутренние прохождения Lemma и внешние
  результаты решу.ЕГЭ в тему-метрику (доля верных с recency-весом, тренд, статус
  🔴/🟡/🟢/нет данных). Матч ученика между системами — по `telegram_id` (фолбэк — имя).
- **Календарные недели** (диапазон каникул, деф. 1 июля – 31 августа → ~9 недель,
  `summerWeeks`): экзаменационные темы распределяются по неделям с датами, а счётные
  навыки (устный счёт) и тригонометрия выполняются каждую неделю. Ученик видит весь
  план сразу, но порционно по неделям.
- **План — доска по неделям.** «Собрать по слабым темам» авто-распределяет алгебру и
  геометрию по неделям (работа на каждую слабую тему → адресная `work_session`). Учитель
  правит вручную: переносит работы между неделями, добавляет работу в неделю (выбрать
  существующую ИЛИ сгенерировать из БАНКА по теме — без создания новых задач), удаляет.
  Устный счёт собирается из существующего банка (не генерим однотипные задачи в базу).
  Можно прикреплять файлы из Библиотеки. Кнопка «Глазами ученика» — предпросмотр.
- **Кабинет ученика** «Каникулярное задание» (`StudentSummerProgram`) — блоки по
  неделям, кнопка «Решать», прикреплённые файлы. Ученик читает только свою программу.
  **Нижнее меню** кабинета (`StudentBottomNav`): Главная · Задание · Прогресс ·
  Достижения · Выйти — на всех экранах ученика, **кроме самого прохождения теста**
  (ученик заходит по ссылке задания и может перейти ко всему заданию). Смена темы —
  кнопка в правом верхнем углу. Раздел переименован «Летняя программа» →
  «Каникулярное задание» (каникулы не только летние).
- **Памятка ученику** (учитель, `ShareMemoModal`) — страница со всеми ссылками по
  неделям + поле напутствия + прогресс выполнения; «Скопировать текст» (для личных
  сообщений) и «Печать». «Добавить работу → сгенерировать» получил каскад
  Контекст → Тема → Подтема + сложность (простой генератор тренажёров, в т.ч. устный счёт).
- БД: коллекции `study_programs` + `study_program_items` (миграция 1779000019),
  `telegram_id` в `ext_journal_*` (1779000020), доступ ученика к своей программе
  (1779000021). API — `pb/programs.js`. Интеграция журнала: `sync-to-lemma.mjs`
  протягивает `telegram_id`.

## [3.9.81] - 2026-06-13

### Теория — печать/PDF с полями на каждой странице и без разрыва чертежей

Печать теории (просмотр статьи, редактор, конструктор конспектов) была построена
на модели «один сплошной лист, поля = `padding` элемента, `@page margin: 0`».
Отсюда два дефекта в PDF/печати (на экране выглядело хорошо): со 2-й страницы
пропадал верхний отступ, а чертежи рвались посреди границы страницы. Перенёс
принцип печати рабочих листов геометрии — поля резервируются на **каждой**
странице, а «атомы» (чертёж/формула/таблица) не делятся.

- **Поля на каждой странице.** `printWithPageSize` (`theoryThemes.js`) задаёт
  реальные поля через `@page { margin: T R B L }` (было `margin: 0`); печатный
  CSS снимает собственный `padding`/фикс-ширину листа, чтобы поля не задвоились.
  В PDF (html2pdf) — опция `margin: [T,R,B,L]` + на время съёмки лист сужается
  до печатной ширины и `padding` снимается.
- **Чертёж не рвётся.** `page-break-inside: avoid` для `img`/`figure`/
  `.theory-ggb-block`/`.geogebra-embed`/`.katex-display`/`table`; в html2pdf
  `pagebreak: { mode: ['css','legacy'] }`.
- **Высокий каллаут с чертежом больше не обрезается.** Сам каллаут разрывать
  можно (неделимы только «атомы» внутри) — при разрыве текст остаётся на месте,
  а чертёж целиком уезжает на следующую страницу.

Затронуты: `TheoryArticleView`, `TheoryEditor`, `TheoryPrintBuilder` (+ их CSS),
`utils/theoryThemes.js`.

## [3.9.80] - 2026-06-13

### Библиотека материалов — массовое перемещение файлов в папки

В Библиотеке появился **множественный выбор файлов**: чекбокс на каждой карточке +
строка-панель «Выбрать все / Выбрано: N» над списком. Для выбранных:
- **«Переместить в папку»** → модалка с деревом папок (`TreeSelect`, корень +
  иерархия из `buildFolderTree`); по умолчанию подставляется текущая папка.
  Перемещение — параллельные `update`'ы через `materialsApi.moveMaterials(ids, folder)`
  (`Promise.allSettled`, частичный успех не валит всю операцию: «перемещено N»/
  «не удалось M»).
- **«Удалить»** выбранные (под `canDelete`, с подтверждением).

Выбор сбрасывается при смене папки / поиске / фильтре категории (массовое действие
не затрагивает скрытые файлы). Выбранные карточки подсвечены акцентной рамкой.
Перемещение доступно только при включённых папках и `canEdit`.

## [3.9.79] - 2026-06-12

### Моё пространство — редизайн «Заметок» + полировка раздела

**«Заметки» приведены к дизайн-коду раздела** (единственный экран без него):
- Шапка `WorkspacePageHeader` (значок amber) с кнопкой «Новая заметка»;
  переключатель «Все/Инбокс/Архив» показывает счётчик инбокса.
- Список — панель-карточка (`bg-raised` + рамка): у каждой заметки появилось
  **превью первой строки текста** (из `extractNoteText`, тот же проход, что и
  поисковый индекс) и **компактная дата** (сегодня → ЧЧ:ММ, «вчера», ДД.ММ;
  у заметки с датой урока — она). Действия (пин/архив/удалить) — плавающая
  мини-панель по hover (на таче видны всегда), не съедают ширину строки.
  Активная заметка — мягкий тинт + 3px-акцент слева.
- Редактор — «лист»: панель с рамкой, заголовок/мета/вложения выровнены по
  тексту BlockNote (54px), инструменты пин/инбокс/архив — текстовые кнопки
  с amber-подсветкой активного состояния, индикатор «✓ сохранено» с анимацией.
- Все захардкоженные синие (#1677ff) и rgba-цвета в CSS заметок → токены
  `tokens.css` (`--accent`, `--ink-*`, `--rule`, радиусы).

**Вложения заметки:** кнопка «Загрузить» (с компьютера) рядом с «Из Библиотеки»;
цвета иконок типов файлов → токены палитры (rose/teal/blue).

**Полировка раздела:** Библиотека материалов — цвета иконок PDF/файлов → токены;
Группы и КТП — пустой архив через единый `EmptyState` вместо сырого `Empty`.

## [3.9.78] - 2026-06-11

### Теория — печать статей

**Печать = превью (WYSIWYG).** Компактные вертикальные отступы (картинки, формулы,
каллауты, абзацы) перенесены из `@media print` в базовый стиль `themes.css` — теперь
превью в редакторе, просмотр статьи и печать выглядят одинаково.

**`@page` подгоняется под статью.** Хелпер `printWithPageSize(pageSettings)`
(`theoryThemes.js`) перед печатью подставляет `@page { size; margin: 0 }` по формату/
ориентации статьи (поля = padding листа). Применено в просмотре статьи, конструкторе
конспектов и редакторе.

**Фоны печатаются.** `print-color-adjust: exact` — иначе тёмная плашка H1 (белый текст)
и тинты каллаутов пропадали (H1 становился невидим). Контроль разрывов: каллауты/
картинки/таблицы/формулы не рвутся между страницами, заголовки не отрываются от текста.

**Кнопка печати в редакторе** (🖨, рядом с PDF) — печатает только лист превью
(`@media print` в `TheoryEditor.css` прячет хром, сбрасывает масштаб).

**Ручной разрыв страницы** — маркер `:::pagebreak` (или кнопка в тулбаре) →
`break-before: page` при печати, пунктир-индикатор на экране.

**Узкие поля по умолчанию** — 12мм сверху/снизу, 10мм по бокам (было 15 со всех сторон);
пресет «A4 стандарт» тоже 12/10. Удалён мёртвый `TheoryArticlePrint.css`.

## [3.9.77] - 2026-06-11

### Редактор работ — режим печати рабочих листов

В превью печати работы (`WorkPrintPreview`, кнопка «Печать / PDF» в редакторе)
добавлена кнопка **«Рабочий лист»** — печать листа в клетку 5 мм с полями для
решения от руки (тот же стек, что в «Генераторе»: задач на лист, размер чертежа
и текста S/M/L/XL, ФИО/Дата, лист ответов для учителя). Варианты работы
склеиваются в один список задач.

Режим вынесен в `WorkEditor` как **ранний `return <WorksheetGridPrint/>`** (паттерн
«отдельная страница»): контент редактора убирается из дерева, иначе он сохраняет
раскладку при печати и ломает абсолютное позиционирование `.wgp-root` →
печатались пустые листы.

## [3.9.76] - 2026-06-11

### Теория — новые каллауты + размеры картинок

**Каллауты:** в блоки теории добавлены `condition` («Условие задачи», грифельный),
`answer` («Ответ», зелёный) и `qed` («Что и требовалось доказать», с символом ∎).
Полный набор: condition · definition · theorem · example · remark · proof · answer ·
qed · note. Доступны в дропдауне «Блок теории ▾» и в md-синтаксисе `:::answer …:::`.

**Размер изображений S/M/L/XL:** в markdown размер пишется в фигурных скобках сразу
после картинки — `![alt](url){M}` (S=30%, M=50%, L=70%, XL=100% ширины страницы,
высота пропорционально; без токена — 100%). `useMarkdownProcessor` (postprocess)
вешает класс `theory-img--<size>` на `<img>`. В модалке вставки картинки добавлен
селектор «Размер на странице» (по умолчанию M) — токен дописывается автоматически.
Работает в превью и на печати/PDF.

## [3.9.75] - 2026-06-11

### Теория — тулбар редактора + загрузка картинок

**Тулбар в стиле панели «Чертёж» (геометрия):**
- Формат-тулбар редактора переписан как **скруглённая контейнер-панель**
  (`#fafafa`, рамка, отступ от краёв) из мелких Ant-кнопок, сгруппированных
  вертикальными `Divider` — единый язык с тулбаром редактора геометрии.
  Кастомные плоские кнопки убраны; триггер вставки таблицы приведён к тому же виду.
  Верхняя документная полоса ужата по высоте.

**Загрузка картинок на файловый сервер + кадрирование:**
- Кнопка «Изображение» → модалка с режимами **«Загрузить файл» / «По ссылке»**
  (Segmented). Загрузка: drag-drop/клик (`Upload.Dragger`) → превью → **кадрирование**
  (общий `CropModal`, обрезка по полям) → заливка на **files.l.oipav.ru**
  (`materialsApi.uploadMaterial`, pb-files) → в markdown вставляется `![alt](url)`.
  Кадрирование идёт по локальному data-url до заливки (без tainted-canvas).
  Если хранилище не подключено — предупреждение со ссылкой на «Библиотеку материалов».
  Режим «По ссылке» (внешний URL) сохранён.

## [3.9.74] - 2026-06-11

### Раздел «Теория» — переработка редактора и дизайна

**Редактор на CodeMirror (Monaco выпилен):**
- Редактор статей переведён с **Monaco** на **CodeMirror 6**
  (`theory/TheoryMarkdownEditor.jsx`, lazy). Тот же markdown+LaTeX-ввод, но без
  74 МБ зависимостей и **8.7 МБ языковых воркеров** в сборке; добавлен KaTeX-линтер
  (битые формулы подчёркиваются прямо в коде). Тулбар работает через императивный
  хэндл `editorRef.current.insert(...)`. Monaco удалён из `package.json` и `vite.config`.

**Единый печатный стиль вместо 5 «тем»:**
- 5 декоративных тем свёрнуты в одну чистую печатную (бывшая «компактная»),
  промоутнута в базовый `.theory-preview-content`. Пикер тем убран из редактора,
  просмотра и конструктора конспектов; мёртвый `getThemeStyles` удалён; загрузка
  шрифтов сокращена с 6 семейств до 2.

**Превью «вписать по ширине»:**
- Превью в редакторе показывает реальную страницу (A4/A5 как на печати),
  смасштабированную под ширину панели — без горизонтального скролла
  (`ResizeObserver` + `transform: scale` на обёртке). PDF-экспорт снимает масштаб
  на время съёмки → лист в полном разрешении.

**Новые инструменты набора математики:**
- **Палитра формул LaTeX** (`theory/FormulaPalette.jsx`) — поповер `fx▾`: структуры
  (дробь, степень, корень, система, матрица…), операторы, отношения/символы, греческие;
  вставка шаблона по курсору.
- **Блоки-каллауты теории** — синтаксис `:::definition|theorem|example|remark|proof|note … :::`
  → стилизованные цветные рамки «Определение / Теорема / Пример / Замечание /
  Доказательство / Заметка» в превью и на печати/PDF. Вставка — дропдаун «Блок теории ▾».
- **Статус сохранения** — чип «Не сохранено / Сохранение… / Сохранено» + `beforeunload`
  при несохранённых правках; автосейв честно помечен как локальный черновик.

**Дизайн-язык «Моё пространство» во всём разделе:**
- `TheoryBrowser`, `TheoryEditor`, `TheoryArticleView`, `TheoryPrintBuilder`,
  `TheoryCategoryManager` переведены на `workspace/ui` (`WorkspacePageHeader`,
  `EmptyState`, `Chip`) и токены (`tokens.css`): убраны bespoke hero-градиенты,
  хардкод-цвета (`#4361ee` → `var(--accent)`), крупное поле названия статьи,
  настройки листа в Popover вместо модалки.

## [3.9.73] - 2026-06-10

### Добавлено (геометрия: поиск, фильтр по источнику, A4-раскладки, тумблер клетки)
- **Поиск геометрических задач** — крупное поле поиска в шапке списка
  (`GeometryTaskList`): по коду, названию, условию, ответу и источнику
  (debounce 350 мс). Счётчик показывает «Найдено: N» при активном фильтре.
- **Фильтр по источнику** — Select «Источник» в панели фильтров; список
  уникальных источников отдаёт новый `api.getGeometrySources()`. В
  `getGeometryTasks` добавлен фильтр `source`, поиск расширен на `answer`/`source`.
- **Раскладки печати в режиме «Просмотр»** — кроме A5 теперь и **A4**, с
  регулировкой числа задач на листе: A5 — 6 (2×3) / 4 (2×2) / 2 (1×2);
  A4 — 9 (3×3) / 8 (2×4) / 6 (2×3) / 4 (2×2). Формат листа и сетка задаются
  параметрически; линии клетки 5 мм считаются точно под ячейку выбранной
  раскладки (равномерная рамка вне зависимости от числа колонок). Сохранённые
  листы хранят `page_size`/`tasks_per_page` и восстанавливают раскладку.
- **Тумблер «Клетка»** в «Просмотре» — включает/выключает клетку 5 мм (на
  экране и в печати/PDF), в обоих режимах (печать и «Вид ученика»).
- **Расширенный редактор в редакторе геометрии** — поля «Условие» и «Решение»
  переведены на `LatexField` (CodeMirror: подсветка LaTeX/markdown,
  перенос строк, поиск/замена Ctrl+F/Ctrl+H, KaTeX-линтер). Тумблер
  «Расширенный редактор» в шапке, выбор запоминается (`localStorage`
  `geoEditor.codeMode`).

### Изменено (превью геометрии)
- **Карточки списка** — добавлен сниппет условия (LaTeX, 3 строки, без
  дублирования чертежа) и чипы «источник/год»; в таблице источник/год видны
  в колонке «Тема / Подтема».
- **Нет чертежа — нет заглушки** — в режиме «Просмотр» убрана плашка
  «Чертёж не задан»: пустая область чертежа просто остаётся пустой.

### Исправлено (редактор макета в «Просмотре»)
- **Макет больше не «слетает» при перетаскивании.** Две причины: (1) первая
  правка задачи стартовала с *дефолтного* макета, и частичный патч ресайза/
  перемещения сбрасывал остальные поля слоя — теперь правка сеется из текущего
  отображаемого макета (оверрайд → снапшот листа → сохранённый → дефолт);
  (2) автосохранение (и ручной сейв) очищали оверрайды посреди жеста, сбрасывая
  макет на снапшот/дефолт — очистка убрана, оверрайды живут до конца сессии
  правки, а автосейв пишет в фон и пропускает уже сохранённое.

## [3.9.72] - 2026-06-10

### Добавлено (пространство: папки в Библиотеке, заметки на «Сегодня», урок↔заметка)
- **Папки в Библиотеке материалов** — иерархия любой глубины: хлебные крошки,
  карточки подпапок (создание/переименование/удаление; содержимое удалённой
  папки оказывается в корне), загрузка падает в текущую папку, перемещение
  файла через ✏️ (TreeSelect «Папка»). Поиск и фильтр категорий — по всей
  библиотеке. В пикере прикрепления — фильтр-дерево «Все папки».
  Бэкенд: коллекция `folders` + `materials.folder` в pb-files
  (`pocketbase/pb-files/bootstrap-folders.sh`, запускать на малине). Фронт
  работает и без бутстрапа — папки просто скрыты
  (`materialsApi.listFolders()` → null при 404).
- **Заметки на «Сегодня»** — карточка в правой колонке дашборда: закреплённые 📌
  + инбокс (до 7, пин выше), клик открывает заметку, чип класса.
- **Обратная связь урок↔заметка** — в модалке урока календаря блок «Файлы
  заметки урока» (read-only, чип «из заметки»): видно, что прикреплено к
  заметке, не открывая её. API `getLessonNote(lessonId)` в `pb/notes.js`
  (без автосоздания заметки).

## [3.9.71] - 2026-06-10

### Изменено (заметки — файлы и организация)
- **Панель «Вложения»** (`workspace/NoteAttachments.jsx`) вместо двух строчек
  Tag-ов: единый список файлов заметки и файлов привязанного урока (чип «урок»,
  read-only), просмотр в `FilePreviewModal` по клику на название, скачивание,
  открепление. Drag-drop прямо на панель — файл уходит в Библиотеку материалов
  и сразу прикрепляется; кнопка «Из Библиотеки» — прежний пикер.
  Список — сетка 2–3 колонки; при >6 файлах сворачивается («Показать все
  (ещё N)») — много вложений не выталкивает текст заметки.
- **Вставка картинок/файлов в текст заметки** — настроен `uploadFile` BlockNote:
  скриншот можно вставить из буфера или перетащить в редактор, файл сохраняется
  в Библиотеку (pb-files), в теле заметки остаётся только URL. Без подключённого
  хранилища — понятное предупреждение.
- **Поиск по заметкам** — поле в сайдбаре, ищет по заголовку и тексту тела
  (извлечение плоского текста из BlockNote-json, `workspace/notesText.js`,
  включая формулы math-блоков и таблицы).
- **Закрепление (пин)** — 📌 в списке и в шапке редактора, закреплённые всегда
  в верхней секции списка (`teacher_notes.is_pinned`, миграция 1779000018).
- **Группировка списка** — вместо плоского списка секции: «Закреплённые» →
  «Инбокс» → по классам → «Без класса» (при поиске/фильтре — плоский режим).
- **Архив вместо удаления** — кнопка удаления заменена на «В архив»
  (`teacher_notes.is_archived`), вкладка «Архив» в Segmented, оттуда «Вернуть»
  или окончательное удаление (с подтверждением). 📎 у заметок с файлами.

## [3.9.70] - 2026-06-10

### Изменено (раздел «Работы» — один список вместо двух)
- **`WorkEditorPage` лишился собственной левой панели-списка** (дублировала
  WorkManager вместе с подсчётом статистики). Теперь редактор открывается на
  конкретную работу во всю ширину, сверху кнопка «← К списку работ»
  (с подтверждением при несохранённых правках). «Сохранить как новую» ведёт
  в редактор новой работы по URL.
- **Пункт меню «Редактор работ» удалён** — вход в редактор: «Мои работы» → ✏️
  (или ссылки из «Сегодня»/журнала/прогресса). `/app/works/new/edit`
  редиректит на список работ.
- CTA «Создать работу» в «Сегодня» и журнале вели в пустой редактор —
  теперь ведут в генератор контрольных (`/app/worksheets/test`).

## [3.9.69] - 2026-06-10

### Добавлено (редактор работ — уроки и пересечения)
- **Связка «работа ↔ урок»** — строка «📎 Уроки» под шапкой редактора
  (`worksheet/WorkLessonLinks.jsx`): показывает уроки календаря, к которым
  прикреплена работа (чипы «дата · группа», открепление крестиком), кнопка
  «Прикрепить к уроку» (список уроков от −7 дней до +60). Обратная сторона
  мульти-селекта работ в LessonModal календаря — формат
  `lessons.materials = [{type:'work', id, title}]` общий.
  API: `getLessonsByMaterialId` в `pb/lessons.js`.
- **Предупреждение о пересечениях с другими работами**
  (`worksheet/WorkOverlapWarning.jsx`, таб «Состав») — точное пересечение по id
  задач: «задачи уже встречаются в работах: X — 5, Y — 2», чипы-ссылки ведут в
  редактор работы. Один запрос `getVariantsContainingTasks(taskIds, excludeWorkId)`
  в `pb/works.js` (OR-фильтр по relation `tasks`). Дополняет векторный бейдж
  новизны (тот ловит почти-дубли, этот — точные повторы по всем работам).

## [3.9.68] - 2026-06-10

### Добавлено (редактор работ — печать, защита правок, сессии, состав)
- **Печать / PDF из редактора работ** — кнопка в шапке `WorkEditor` открывает
  полноэкранный предпросмотр `worksheet/WorkPrintPreview.jsx` (переиспользует
  печатный стек генераторов: `VariantRenderer` + `AnswersPage` +
  `useWorksheetActions`). Настройки: шрифт, колонки, место решения, компактный
  режим, ФИО, ответы в тексте, лист ответов, скрытие префиксов. Scoped print
  `body:has(.wpv-overlay)` — печатается только лист.
- **Защита несохранённых правок** — правки вариантов/параметров помечают работу
  бейджем «не сохранено»; переключение на другую работу — через подтверждение;
  закрытие/перезагрузка вкладки — предупреждение `beforeunload`.
- **Селектор сессий в «Результатах»** — если у работы несколько выдач, можно
  переключаться между ними (раньше показывалась только одна «лучшая» сессия).
  И в редакторе, и в раскрытой карточке «Мои работы».
- **Удобства состава**: тумблер «ответы» (показ ответа под задачей, подсветка
  задач без ответа), кнопки «Дублировать вариант» и «Перемешать задачи»,
  строка-сводка по варианту (число задач, распределение сложности, без ответа).

### Исправлено
- `getSessionsByWorks` не запрашивал поле `is_open` → бейдж «Приём открыт» на
  карточках «Мои работы» никогда не показывался.

## [3.9.67] - 2026-06-10

### Добавлено (редактор работ — векторные инструменты)
- **Блок «🔎 Векторные инструменты» в табе «Состав»** (`WorkEditor`) — тот же,
  что в «Генераторе» (`WorksheetVectorTools`): авто-предупреждение о повторах
  внутри вариантов (A2), «Дополнить похожими» (A1, `FillSimilarModal`),
  «Параллельные варианты» (A4, `ParallelVariantsModal` — создаёт работы
  «оригинал/дубль N» и открывает их в редакторе), бейдж новизны набора
  относительно последних работ (текущая работа исключена из эталона).
- **Кнопка «Похожие» у каждой задачи варианта** — новый
  `worksheet/SimilarSwapModal.jsx`: семантические соседи задачи (`/similar`,
  sqlite-vec) с процентом похожести и действиями «Заменить» (свап на месте)
  и «Добавить» (в конец варианта). Тумблер «только эта тема», настройка числа
  кандидатов; кандидаты, уже присутствующие в варианте, отфильтровываются.

## [3.9.66] - 2026-06-09

### Улучшено (редизайн раздела «Моё пространство»)
- **Единый «человечный» UI-язык** для всех экранов раздела. Новый
  переиспользуемый слой `components/workspace/ui/`:
  - `WorkspacePageHeader` — единая шапка (цветной значок-accent + заголовок +
    подпись + действия) вместо разнобоя `Title level={3|4}` + Space/Row.
  - `EmptyState` — дружелюбные пустые состояния с призывом к действию вместо
    пассивных «…пусто».
  - `Chip`/`GroupChip`/`LessonStatusChip`/`SubmitChip` — единый язык статусов
    поверх `.lemma-chip` вместо разрозненных `<Tag color="…">`.
  - `groupColor.js` (`groupTone`/`groupHex`) — детерминированный цвет группы из
    5-палитры: одна и та же группа всегда одного оттенка во всех экранах.
- **«Сегодня»** пересобран в рабочий стол: блок быстрых действий, кликабельное
  табло, таймлайн уроков с цветом группы, статус-чипами и inline-действиями
  (✓ провёл / заметка).
- **Календарь** — цвет блока события = группа (а не один синий); статусы читаемы
  (проведён — бледнее, отменён — серый + зачёркнут); цвета приведены к токенам
  Hybrid.
- **Журнал сдачи** (+ сабтабы «Решу (внешние)» и «Тепловая карта») — статусы и
  оценки переведены на чипы; тепловая шкала — на soft-тинтах палитры
  (rose→amber→teal).
- Группы, КТП (список + редактор), Заметки, Библиотека материалов — единая шапка,
  цвет-идентичность группы, чипы, человечные empty.

## [3.9.65] - 2026-06-09

### Добавлено (календарь — расписание пар)
- **Расписание «пар» школы + интенсивы** при создании урока. В модалке урока
  (`TeacherCalendar` → `LessonModal`) строка «Время по расписанию»:
  - **Пара** — выбор пары (нулевая, 1-я…5-я) + часть (вся пара / 1-я / 2-я
    полупара); сразу проставляет правильное время старта по расписанию школы.
  - **Интенсив (2–4 пары)** — «с пары» → «по пару»; занятие длиной в несколько пар.
  - Календарь рисует блок правильной длины (интенсив = длинный блок, полупара —
    короткий), а не фиксированные 45 мин.
- **БД:** `lessons.time_slot` (text, nullable, миграция `1779000017`) — кодирует
  слот: `"0"`/`"N"`/`"Na"/"Nb"`/`"N-M"`. Старые уроки без `time_slot` — длительность
  угадывается по времени старта (обратная совместимость).

## [3.9.64] - 2026-06-09

### Улучшено (Библиотека материалов)
- **Загрузка файлов прямо в пикере** — при создании урока и в заметке кнопка
  «Загрузить с компьютера» + выбор категории; файл сразу попадает в Библиотеку и
  автоматически отмечается для прикрепления (раньше грузить можно было только
  через раздел «Библиотека»).
- **Редактирование файла** — кнопка ✏️ на карточке → модалка `MaterialEditModal`
  (название, категория, предмет, описание; `materialsApi.updateMaterial`).
- **Длинные имена** обрезаются многоточием с тултипом (карточки + список пикера) —
  flex + `minWidth:0`, больше не ломают вёрстку.

### Исправлено
- Вход в хранилище падал с 400: браузерный менеджер подставлял пароль Google
  (email совпадает с Google-аккаунтом) → `autoComplete="off"/"new-password"` на форме.
- `destroyOnClose` (deprecated в antd 5.29) → `destroyOnHidden` во всех компонентах.
- Статичный `message` из antd → `App.useApp()` в компонентах хранилища
  (убирает warning «Static function can not consume context»).

## [3.9.63] - 2026-06-09

### Добавлено
- **Файловое хранилище учителя — «Библиотека материалов».** Отдельный второй
  PocketBase (`pb-files`) развёрнут на Raspberry Pi (`https://files.l.oipav.ru`),
  чтобы занять свободное место малины и не нагружать боевой VPS-PB. Учитель грузит
  туда учебники, методички, сгенерированные PDF — и складывает в одном месте,
  вместо сотен файлов в «Загрузках».
  - **Раздел «Библиотека»** (`/app/materials`, группа «Моё пространство») —
    загрузка с компьютера drag-drop, категории (учебник/раб.лист/методичка/…),
    поиск, скачивание, удаление. Компонент `MaterialsLibrary.jsx`.
  - **Прикрепление к урокам и заметкам** — пикер `MaterialPickerModal` в модалке
    урока (календарь, пишет в `lessons.materials`) и в редакторе заметки (пишет в
    `teacher_notes.links`) как дескриптор `{type:'material', id, title, url}`.
  - **Архитектура:** `pb-files` владеет файлами (коллекция `materials`), боевой
    PB хранит только ссылки. Фронт — второй клиент `pbFiles` (`filesClient.js`,
    свой authStore + `VITE_PB_FILES_URL`). Запись закрыта логином в auth-коллекцию
    `users` (токен 60 дней); чтение публичное. TLS Let's Encrypt с авто-renew.
  - Инфра и bootstrap-схемы — `pocketbase/pb-files/` (README + скрипты).

## [3.9.62] - 2026-06-08

### Изменено
- **Обновлён лендинг (`lemma.oipav.ru`) — контент-рефреш под актуальный продукт.**
  С апреля накопился разрыв: лендинг не отражал целые пласты функциональности.
  Что обновлено в `LandingPageV2`:
  - **Статистика** пересчитана по боевой БД: 7000+ → **17 000+ задач**, 72+ → **88
    достижений**, 8 → **20+ генераторов** (минуты на работу без изменений).
  - **Hero** — новый подзаголовок (импорт «Решу ЕГЭ», 20+ генераторов, варианты КИМ,
    умный подбор, пространство учителя) + «17 000+ задач» в заголовке.
  - **Возможности** (`FeaturesV2`) — карточки переработаны: добавлены «Варианты ЕГЭ
    и ОГЭ» (КИМ), «Умный подбор» (семантический поиск / анти-дубль / параллели),
    «Игровые форматы»; «База задач» дополнена импортом с «Решу ЕГЭ»; блок аналитики →
    «Пространство учителя» (классы, журнал, КТП, календарь, тепловые карты ошибок).
  - **Процесс** (`TimelineV2`), **Для учеников** (`StudentV2`, +попытки/проходной балл/
    работа над ошибками), **Аналитика** (`ProgressInsightsV2`, +тепловые карты и внешний
    журнал «Решу»), **О проекте** (`TestimonialsV2`) — тексты приведены к актуальным фичам.
  - Вёрстка и CSS не менялись (бенто-сетка `repeat(3,1fr)` ложится 3×3 на 8 карточек).
  - **Детальная страница «Как работает»** (`how-it-works.html`, `HowItWorksPage`)
    обновлена синхронно: факты в Hero, два новых модуля («Умный подбор задач» —
    вектор bge-m3/sqlite-vec; «Пространство учителя» — классы/журнал/КТП/календарь/
    тепловые карты), модуль форматов дополнен полными вариантами КИМ ЕГЭ/ОГЭ, импорт
    «Решу ЕГЭ» с LLM-нормализацией, новые коллекции БД и группы API, актуальный
    tech-stack (BlockNote, sqlite-vec, Ollama bge-m3), бэкап-блок (S3 + vec.db),
    история версий пополнена вехами v3.9.31/38/50/61. 88 достижений везде.

## [3.9.61] - 2026-06-08

### Добавлено
- **Связь внешних задач с банком Лемме (по требованию).** В точечном разборе
  (тепловая карта → клик) у каждой задачи — колонка «В Лемме»: если задача с таким
  `sdamgia_id` уже есть → кнопка «открыть» (показывает условие+ответ+решение в
  модалке `TaskPreviewModal`, не уходя со страницы); если нет → «импортировать»
  (editor+) — парсит ровно эту задачу с решу и создаёт в теме `ege_base` по номеру
  задания (`importReshuProblem`, born-local картинки, поддержка многокартиночных
  условий из v3.9.60), затем открывает превью. API `getTaskIdsBySdamgiaIds` (батч-
  проверка существования по `sdamgia_id`). Массовый предзалив 1443 задач — отдельным
  шагом по желанию.

## [3.9.60] - 2026-06-08

### Исправлено
- **Парсер sdamgia: терялись все картинки условия, кроме первой.** Для задач с
  несколькими изображениями в условии (типичный кейс — «сопоставьте график функции
  и формулу/уравнение», несколько графиков) парсер с v3.9.33 вырезал inline-маркеры
  и оставлял картинки только в `condition_images[]` — рисовалась лишь первая (через
  отдельное поле `has_image`). Фикс (`pdf-service.js::processCondition`): при **2+**
  уникальных не-формульных картинках они остаются ИНЛАЙН как `![image](url)` на своих
  позициях (born-local подменяет на локальные `task_images`); при одной — поведение
  прежнее. Дедуп повторов чертежа по URL (retina/печать). Импортёр (`useTaskImport`)
  для мультикартиночных ставит `has_image=false`, чтобы не задвоить первую. Проверено
  на задании №7 (4 графика → 4 inline-маркера). Парсер выкачен на VPS.

## [3.9.59] - 2026-06-08

### Добавлено
- **Точечный разбор по заданию (внешние работы).** Клик по ячейке тепловой карты
  (или по номеру задания) → модалка разбора: кто ошибся / решил по этому заданию,
  работа/дата, **ссылка на ту самую задачу на «Решу ЕГЭ»** по `problem_id`
  (`mathb-ege.sdamgia.ru/problem?id=`), итог верно/ошибка (строки ошибок подсвечены).
  Тумблер «только ученик / весь класс» (при клике по ячейке ученика), % верных по
  классу, кнопка «Отработать тему» (собрать работу по этой теме). Тут уже используется
  `problem_id` для прямого перехода на решу.

## [3.9.58] - 2026-06-08

### Добавлено
- **Тематический разбор внешних работ решу.ЕГЭ — тепловая карта по темам.**
  В Журнале → «Решу (внешние)» новый переключатель «Оценки / Тепловая карта (темы)».
  `ExternalThematic`: сетка ученик × задание №1–21 (+ строка «Класс»), цвет по % верных
  (красный→зелёный). Мост «решу-ошибка → тема» строится по **`task_number` = номер
  задания базового ЕГЭ** (sdamgia_id для базы НЕ нужен) → тема `ege_base`. Фильтр по
  группе. **Кнопка «Работа над ошибками»**: задания ниже порога (настраивается) →
  собирает работу из задач этих тем (`getRandomTasks` по темам) → открывает редактор
  работы (выдача/печать). API `getExtTaskResultsAll`.

## [3.9.57] - 2026-06-07

### Добавлено
- **Интеграция с ege-journal — приём внешних результатов решу.ЕГЭ.** Приёмные
  коллекции `ext_journal_exams` / `ext_journal_results` / `ext_journal_task_results`
  (миграция `1779000016`, owner→teachers, хранят `problem_id` для связки с
  `tasks.sdamgia_id`). API `pb/extjournal.js` (чтение). В Журнале — переключатель
  «Сдача (Lemma) / Решу (внешние)»: `ExternalJournal` — сетка ученик × внешняя работа
  с оценками 2–5 (цветовые), фильтр по группе.
- **Sync на стороне ege-journal** (отдельный репозиторий): `scripts/sync-to-lemma.mjs`
  — пуш из локального журнала (дома) в облачный PB Lemma под отдельным sync-аккаунтом
  (role=editor); идемпотентно, dry-run по умолчанию, `--push` для заливки. Данные не
  выходят за пределы исходящего HTTPS из дома.

## [3.9.56] - 2026-06-07

### Добавлено
- **Замыкание петли (фаза 6): материалы урока.** В уроке (календарь) можно привязать
  работы (`lessons.materials` json, без новой миграции). Кнопки быстрого перехода к
  материалу ведут в редактор работы (`/app/works/:id/edit`), где уже есть выдача,
  результаты и **«Работа над ошибками»** (`ClassRemediationModal`). Так план урока
  связан с выдачей → результатами → коррекцией. Уроки с материалами помечаются 📎
  в календаре и на «Сегодня».

## [3.9.55] - 2026-06-07

### Добавлено
- **Экран «Сегодня» — позвоночник учительского фло** (`TodayDashboard`, роут
  `/app/today`). Сводка дня: уроки сегодня (по времени) + ближайшие дни недели +
  ближайшие дедлайны выдач (с пометкой «просрочено/через N дн.») + карточки-статистика
  (уроков сегодня / дедлайнов за 2 недели / просрочено). Быстрый переход к заметке
  урока, календарю, журналу. Read-mostly (поверх lessons + work_sessions), без миграций.

### Изменено
- **Дом учителя теперь «Сегодня».** Редирект с `/`, `/app` и нераспознанных `/app/*`
  ведёт на `/app/today` (раньше — «Все задачи»). Инверсия центра тяжести: дом =
  рабочее пространство, каталог задач = общий фонд (см. `BACKLOG.md`). Пункт меню
  «Сегодня» — первый в «Моём пространстве».

## [3.9.54] - 2026-06-07

### Добавлено
- **LaTeX-формулы в заметках (BlockNote).** Кастомный блок `math`
  (`notesMathBlock.jsx`): при фокусе формула редактируется как LaTeX-текст, при
  уходе курсора — рендерится в KaTeX. Вставка через slash-меню («/Формула»).
  Кастомная схема BlockNote + `SuggestionMenuController`.
- **Общие заметки (заметки ↔ календарь).** `teacher_notes` расширена (миграция
  `1779000015`): `group`→teaching_groups, `lesson`→lessons, `note_date`. В
  `NotesWorkspace` у заметки можно задать класс и дату (теги в списке, фильтр по
  классу). В календаре кнопка «Открыть заметку урока» (`getOrCreateLessonNote`)
  создаёт/открывает заметку урока в общем редакторе (с формулами) —
  `/app/notes?note=<id>`. Заметка урока = обычная заметка (единая сущность);
  textarea заметки в модалке урока убрана.

## [3.9.53] - 2026-06-07

### Исправлено
- **КТП: добавление раздела-заголовка падало с 404.** Кнопка «+ Раздел» сидировала
  модал объектом `{ is_section: true }` без `id`, а проверка `if (editing)` уводила
  сохранение в ветку update → `PATCH /ktp_entries/records/undefined`. Исправлено на
  `if (editing?.id)` (`KtpEditor.jsx`) — раздел теперь корректно создаётся.

## [3.9.52] - 2026-06-07

### Добавлено
- **Учительское фло — Заметки (фаза 5).** Коллекция `teacher_notes` (миграция
  `1779000014`): owner→teachers, title, body (json — документ BlockNote), is_inbox,
  links(json). API `pb/notes.js`.
- **Блочный редактор заметок** (`components/workspace/NotesWorkspace.jsx`, роут
  `/app/notes`) на **BlockNote** (Notion-стиль: slash-меню, блоки, чек-листы,
  списки, заголовки; русская локализация). Двухколоночная раскладка: список +
  редактор. Автосохранение тела (debounce) и заголовка, инбокс-флаг, фильтр
  «Все/Инбокс», удаление. Новые зависимости `@blocknote/{core,react,mantine}@0.31`
  (+ `@mantine/core,hooks@7` — React-18-совместимая ветка).

### Примечания
- BlockNote-редактор лениво грузится (отдельный чанк, в main не попадает).
- Математика (KaTeX) в заметках и блочный апгрейд `note_md` урока — следующим шагом
  (кастомный блок BlockNote).

## [3.9.51] - 2026-06-07

### Добавлено
- **Учительское фло — Календарь (фаза 4).** Сущность «Урок» (`lessons`, миграция
  `1779000013`): owner→teachers, group→teaching_groups, ktp_entry→ktp_entries
  (опц.), title, date_plan, date_fact, status (planned/done/cancelled), note_md,
  materials(json). API `pb/lessons.js`.
- **Календарь-линза** (`components/workspace/TeacherCalendar.jsx`, роут
  `/app/calendar`) на `react-big-calendar` (dayjs-локализатор, ru): уроки +
  дедлайны выдач на сетке месяц/неделя/день/список. **Drag события → перенос даты**
  (урок → `lessons.date_plan`, дедлайн → `work_sessions.deadline`) — закрывает боль
  пересчёта дат при сдвигах. Клик по пустому слоту → создание урока; клик по уроку →
  редактирование (тема/группа/статус/дата/заметка). Фильтр по группе, тумблер
  дедлайнов, тёмная тема. Новая зависимость `react-big-calendar@1`.

## [3.9.50] - 2026-06-07

### Добавлено
- **Учительское фло — «Моё пространство» (фазы 1–2).** Новая первая группа меню
  (секция `workspace`), начало учительского органайзера (см. `BACKLOG.md` §
  «Учительское фло»).
- **Классы и группы** (фаза 1) — сущность `teaching_groups` (миграция
  `1779000010`): owner→teachers, предмет, класс, часов/нед, УМК, учебный год,
  архив. Аддитивно добавлен nullable `students.teaching_group` (старое
  `student_class` не тронуто). UI: `components/workspace/GroupManager.jsx`
  (список/CRUD/архив/счётчик учеников) + `GroupDetail.jsx` (состав группы,
  привязка учеников, «привязать по совпадению» с `student_class`). API
  `pb/groups.js`. Роуты `/app/groups`, `/app/groups/:groupId`.
- **Дедлайны на выдаче** (фаза 2) — nullable `work_sessions.deadline` (миграция
  `1779000011`) + `DatePicker` «Дедлайн сдачи» в `SessionPanel`.
- **Журнал сдачи** (фаза 2) — `components/workspace/GradeJournal.jsx`, роут
  `/app/journal`. Сетка «ученик × выдача» поверх реальных попыток: статусы
  сдал / сдал с опозданием / незачёт / просрочено / выполняет / не сдавал (по
  лучшей попытке + дедлайну + проходному баллу). Без выдуманных оценок. API
  `groups.getGroupJournal(groupId)`.
- **КТП — календарно-тематическое планирование** (фаза 3) — коллекции `courses`
  + `ktp_entries` (миграция `1779000012`). UI: `KtpList` (список КТП по группам)
  + `KtpEditor` (таблица строк: тема + опц. связь с темой фонда, часы, неделя,
  дата, планируемые результаты, разделы-заголовки, перестановка). Роуты
  `/app/ktp`, `/app/ktp/:courseId`. **Экспорт в Word** (`docx`, ленивый чанк,
  `utils/ktpDocx.js`) **и печать/PDF** (`KtpPrintView`, A4 landscape). API
  `pb/ktp.js`. Новая зависимость `docx@9`.

### Примечания
- owner-фильтрация и настоящие PB-правила пока НЕ включены (правила требуют лишь
  логина учителя) — отдельный заход перед мультиучительством/ПДн.
- Журнал матчит попытки по `attempt.student` → учитывает только залогиненных
  учеников (анонимные device-only не попадают).
- Новым editor/viewer нужно выдать секцию `workspace` в «Пользователях».

## [3.9.49] - 2026-06-06

### Добавлено
- **Генератор вариантов ОГЭ (9 класс)** — новый раздел «Рабочие листы → Варианты
  ОГЭ (9 класс)», роут `/app/worksheets/oge-variant`. Полный вариант 25 заданий:
  часть 1 (№1–19, краткий ответ) + часть 2 (№20–25, развёрнутый). По образцу
  профильного: те же хуки/модалы, успеваемость по темам, сохранение/онлайн-выдача,
  параллельные варианты, правка/замена задач. Компоненты `OgeVariantGenerator.jsx`
  + `OgeKimPrint.jsx` (граница частей `PART1_LAST=19`).
- **Связный практический блок 1–5** — задания 1–5 (план/ситуация) собираются из
  ОДНОГО сюжета (общий план), как в настоящем ОГЭ. Тумблер «Связный блок 1–5» +
  **выбор конкретного сюжета** учителем (селектор по названиям). Новый API-метод
  `api.getOgeContextBlocks()` (в `pb/tasks.js`) — полные сюжеты из `task_contexts`.
- **КИМ-печать ОГЭ** — обложка с инструкцией ОГЭ, часть 1 с полями ответа, блок
  «Часть 2» с инструкцией про отдельный лист, лист ответов/решений.

### Исправлено
- **Ремонт данных ОГЭ 1–5: потерянный вопрос.** Парсер sdamgia при импорте брал
  условие только из первого `.pbody` (общий контекст блока), а конкретный вопрос
  (второй `.pbody`) терялся → у всех задач 1–5 был одинаковый текст без вопроса.
  Исправлен `pdf-service.js::parseProblemFromDiv` (склеивает все condition-`.pbody`
  вне `.solution/.answer/.prob_crits`) + выкачен на VPS. Скрипт-ремонт
  `scripts/repair-oge-1to5-questions.mjs` перечитал 279 задач → **272 обновлено**
  (теперь у каждого задания 1–5 свой вопрос), бэкап БД сделан.
- **Печать КИМ — обрезка правого края** (общий `EgeVariantGenerator.css`, чинит
  все 3 КИМ-генератора): глобальный `@page { margin:10mm 8mm }` из
  `TaskWorksheet.css` перебивал поля КИМ. Фикс: инъекция `@page { margin:0 }`
  (последним правилом в `<head>` при стиле КИМ) + `.kim-page { width:100% }` в печати.
- **Печать КИМ — часть 2 не печаталась + пустые страницы.** `break-inside:avoid`
  на полностраничном `.kim-page` заставлял Chrome «ронять» контент последней
  страницы; переполнение обложки/страниц каскадило в пустые листы. Фикс: убран
  `break-inside:avoid` с `.kim-page` (+ `overflow:hidden`); обложке задана высота
  206мм (футер не уезжает на 2-й лист); синхронное измерение высот (без гонки при
  печати сразу после включения КИМ).

### Документация
- `BACKLOG.md`: зафиксирована неуникальность `tasks.code` (находить задачи по
  `id`/`topic`/`context`, не по `code`).

## [3.9.48] - 2026-06-04

### Производительность
- **Route-level code splitting** (`App.jsx`): ~67 статических импортов страниц-
  компонентов переведены на `React.lazy()` + одна граница `<Suspense>` вокруг
  `<Outlet/>` в `AppLayout`. Главный чанк `main` уменьшился с **2.4 МБ до ~90 КБ**
  (gzip 29.7 КБ): десятки генераторов, `html2pdf`, mermaid (через Excalidraw) больше
  не в стартовом бандле, грузятся по требованию на своём роуте. Шелл
  (`LoginPage`/`ProtectedRoute`/`UserMenu`/контексты) — статические импорты.

### Удалено
- **Серверная PDF-генерация через Puppeteer/Chromium** — полностью выпилена.
  На практике использовалась только браузерная печать, а Chromium зря потреблял RAM
  на VPS. Печать и PDF теперь полностью на клиенте.
  - Фронт: удалён хук `usePuppeteerPDF`; `useWorksheetActions.handleExportPDF` идёт
    сразу в `html2pdf.js`; `ActionButtons` — простая кнопка PDF (без выбора метода);
    в `EgeVariant`/`EgeProfile`/`TestWork`-генераторах и `ResultActionBar` убраны
    пропсы `pdfMethod`/`puppeteerAvailable`; в Theory-компонентах убрана попытка
    Puppeteer; в `TDFPrintView` убрана кнопка «Скачать PDF» (осталась «Печать»).
  - Сервер (`pdf-service.js`, −140 строк): удалён роут `POST /generate`,
    `getBrowser()`, импорт puppeteer, browser-lifecycle. `/health` →
    `service: lemma-backend-helper`. Остальные 17 эндпоинтов (sdamgia, `/latex-fix`,
    векторный поиск) не тронуты.
  - VPS: `PUPPETEER_EXECUTABLE_PATH` убран из systemd-unit, npm `puppeteer-core`
    удалён, snap chromium удалён.

### Рефакторинг
- **`pocketbase.js` разбит на доменные модули**: god-модуль (3091 строка) →
  тонкий barrel-реэкспорт (40 строк) + 13 доменных модулей в
  `shared/services/pb/` (`client`, `extras`, `topics`, `tasks`, `catalog`, `works`,
  `theory`, `sessions`, `answers`, `achievements`, `students`, `geometry`, `tdf`,
  `worksheets`). `pb/client.js` — клиент `pb` + приватный `_logAudit`. Публичный
  контракт прежний: `import { api }` / `import pb` и места вызова (200+) не менялись.

### Инфраструктура
- **Деплой лендинга переведён в ручной режим**: авто-триггер `push` в `main` убран
  из GitHub Actions (оставлен `workflow_dispatch`); добавлен локальный скрипт
  `./deploy-landing.sh` (сборка на Mac → rsync на VPS → health-check). Лендинг
  меняется редко — дёргать сборку на каждый коммит незачем.
- Уборка корня репозитория: разовые артефакты импорта (`oge-*-result.json` и т.п.)
  перенесены в `_archive/`, добавлены правила в `.gitignore`.

## [3.9.47] - 2026-06-03

### Улучшено
- **Карточка зачёта/попыток на странице результата ученика** (`StudentResultPage`)
  переведена с инлайн-стилей на оформленную карточку в стиле страницы (классы
  `srp-*` в `StudentApp.css`), с тёмной темой и анимацией появления:
  - крупный статус — круглый значок ✓/✕ (градиент) + «Зачёт / Не зачёт» +
    поясняющая строка с лучшим результатом и нехваткой до зачёта;
  - прогресс-бар с вертикальной отметкой проходного балла и флажком «Порог N» —
    видно «дотянул/не дотянул» одним взглядом;
  - попытки в виде точек (использованные/свободные) + пилюля «осталось K» /
    «попытки закончились»;
  - кнопка «Пройти ещё раз» во всю ширину.

## [3.9.46] - 2026-06-03

### Добавлено
- **Несколько попыток на работу + проходной балл** (настройки на сессии выдачи).
  Обе функции по умолчанию выключены.
  - **Миграция `1779000009`** — поля `max_attempts` и `passing_score` в коллекции
    `work_sessions` (int, default 0 = выключено).
  - **Учитель** (`SessionPanel`) — два переключателя с числовыми полями: «Несколько
    попыток» (≥2) и «Проходной балл» (число верных задач, ≥1). Сохранение через
    `updateSession`.
  - **Ученик** (`StudentResultPage`) — блок зачёта «✅ Зачёт / ❌ Не зачёт»,
    рассчитанный по **лучшей** попытке относительно порога (с указанием нехватки),
    строка «Попытка N из M, осталось K» и кнопка **«Пройти ещё раз»** (доступна пока
    есть попытки и приём открыт). Повтор выдаёт **новый вариант** (round-robin) —
    как для обычных работ, так и для MC-тестов.
  - **`useStudentSession`** — `startAttempt(name, { forceNew })` (пропускает
    «продолжить старую попытку»), `startRetry`, производные `attemptsUsed` /
    `attemptsLeft` / `bestAttempt` / `canRetry` / `passingScore`; список попыток
    ученика держится актуальным через эффект на смену статуса/балла.

## [3.9.45] - 2026-06-02

### Исправлено
- **Печать ТДФ в режиме «В клетку»** (`TDFPrintView`) — клетка переведена со
  старого растрового `background-image: linear-gradient` на канонический паттерн
  div-линий с физическими mm/pt-единицами (нативный вектор в PDF), как в
  `GeometryWorksheetPrint` / `WorksheetGridPrint`. Попутно устранены две печатные
  грабли:
  - **Таблица занимала ~65% ширины листа** — из-за перерасхода линий (рендерились
    с большим запасом, до 290 мм). При печати Chrome включает overflow абсолютных
    элементов в расчёт печатной области и масштабирует страницу вниз. Теперь число
    линий считается ТОЧНО под размер блока (`h = ceil(высота/5)+2`,
    `v = ceil(ширина_колонки/5)+2`), запас +2, без overshoot.
  - **Клетка не заполняла ячейку целиком** — `height: 100%` не резолвится внутри
    `table-cell`. Высота клеточного поля задаётся явным px из известной высоты
    строки (`stretchRowH`). Имя пункта выводится оверлеем поверх клетки.
  - Клетка вынесена в обычный positioned-`div` (не `<td>`): `position: relative`
    на ячейке ломает `table-layout: fixed` при печати в Chrome.

## [3.9.44] - 2026-06-01

### Добавлено
- **Расширенный редактор (CodeMirror 6) в редакторе задач** (`TaskEditModal`) —
  тумблер «Расширенный редактор» вверху модалки переключает поля «Текст задания»
  и «Решение» с обычной `Input.TextArea` на CodeMirror 6: подсветка markdown,
  перенос длинных строк, авторазмер и встроенный поиск/замена (Ctrl+F / Ctrl+H).
  Поле «Ответ» (однострочное) намеренно не затронуто. Выбор режима запоминается
  в `localStorage` (`taskEditor.codeMode`).
  - Новый общий компонент `components/shared/LatexField.jsx` (проп `mode='plain'|'code'`,
    наружу — обычное поле формы `value`/`onChange` + `onTextChange` для предпросмотра).
    CodeMirror вынесен в `components/shared/LatexCodeMirror.jsx` и грузится **лениво**
    отдельным чанком (~212 KB gzip) — в основной бандл не попадает.
- **Подсветка «сломанного» LaTeX в расширенном редакторе** — линтер на базе KaTeX:
  каждая формула `$…$` / `$$…$$` прогоняется через `katex.renderToString`; не
  парсится → красное подчёркивание с текстом ошибки KaTeX, непарный `$` → жёлтое
  предупреждение «формула не закрыта». Маркер ошибки в gutter слева.
  Новая утилита `utils/katexLint.js` (`katexDiagnostics`), 8 юнит-тестов.

### Изменено
- **Компактная шапка редактора задач** (`TaskEditModal`) — поля «Сложность,
  Контекст, Тема, Подтема, Источник, Год, Теги» собраны в адаптивную сетку
  `Row`/`Col` (на широком экране 4+3 поля в ряд вместо 7 отдельных строк).

## [3.9.43] - 2026-06-01

### Добавлено
- **Кнопки «🧹 Корни» в редакторе задач** (`TaskEditModal`) — детерминированная
  починка битых корней/иррациональных выражений из плохого парсинга sdamgia,
  мгновенно и без сети. Покрывает три формата маркеров аргумента (обычные пробелы,
  мягкие переносы, слипшийся текст) и Unicode `√/∛/∜`.
  Пример: `\log_{\sqrt: начало аргумента: 3 конец аргумента}` → `\log_{\sqrt{3}}`.
  - Своя кнопка у каждого поля: «Текст задания», «Ответ», «Решение» — чинит только
    своё поле. В шапке текста задания дополнительно «🧹 Ответ+решение» — чинит оба
    нижних поля разом.
  - Новая утилита `utils/fixLatexRoots.js` (`fixLatexRoots`, `hasBrokenRoots`),
    13 юнит-тестов. Клиентский аналог части серверной `cleanLatexFormula`, но
    толерантный к пробельному варианту маркера без мягких переносов (его серверные
    regex'ы пропускали).

### Исправлено
- **КИМ-печать показывала устаревший текст задачи после правки через редактор**
  (`EgeVariantGenerator` база + `EgeProfileVariantGenerator` профиль). Ключ measure-
  фазы пагинации (`taskKey`) строился только по `id` задач, а `id` при редактировании
  не меняется → перерасчёт не запускался и печатались закэшированные старые страницы.
  Теперь ключ включает содержимое (`statement_md`, картинку, для профиля `solution_md`)
  — любая правка триггерит пересчёт со свежими задачами. (Обычные «листы работ» в
  Генераторе обновлялись корректно и раньше — там рендер читает варианты живьём.)

## [3.9.42] - 2026-05-31

### Изменено / Инфраструктура
- **Чертежи задач перенесены в свою БД (независимость от sdamgia).** Раньше картинки
  в `solution_md` (и частично `statement_md`) ссылались на внешний
  `*.sdamgia.ru/get_file?id=...`; локальные копии в `task_images` подставлялись только
  в превью редактора, а **печать** (решения профиля ч.2 `EgeProfileVariantGenerator`,
  `PrintableWorksheet`, `MarathonTeacherSheetFull`) тянула чертёж с sdamgia напрямую.
  - Одноразовый скрипт `pocketbase/scripts/migrate-drawings-local.mjs` (idempotent,
    dry-run/confirm): скачивает недостающие картинки через прокси `/fetch-image` в
    `task_images` и **переписывает markdown** (`statement_md`/`solution_md`/`criteria_md`),
    заменяя внешние URL на локальные `…/api/files/task_images/{id}/{file}`. Теперь любой
    рендер (вкл. печать) показывает локальный файл — sdamgia при печати больше не нужен.
  - **Импорт «рождается локальным»**: `useTaskImport` после заливки `task_images` сразу
    переписывает markdown созданной задачи на локальные URL (`rewriteImageUrls` +
    `updateTask`) — проблема не возвращается на новых импортах.

## [3.9.41] - 2026-05-31

### Добавлено
- **Векторные режимы подбора задач в «Генераторе»** (`OralWorksheetGenerator`,
  «Рабочие листы → Генератор»). Над каскадом темы — переключатель «Способ подбора»
  (`SelectionMethodPanel`); классический режим «По фильтрам» остаётся по умолчанию.
  Все режимы выдают ту же структуру вариантов — печать/сохранение/PDF не менялись.
  - **🎯 По образцу** — учитель выбирает задачу-эталон (`TaskSelectModal`) и одним
    ползунком регулирует похожесть: вправо → близкие клоны-тренажёр, влево → та же
    тема, но другие сюжеты. Эндпоинт `POST /seed-select` (`selectBySeed` в vec-search).
  - **🌈 Разные сюжеты** — максимально разнообразный набор внутри темы: стратегия
    «Разнообразие (MMR)» (жадный max-min) или «Кластеры сюжетов» (k-means → медоиды).
    Эндпоинт `POST /diverse` (`selectDiverse`).
  - **♻️ Анти-дубль** — подбор задач темы, НЕ похожих на ранее выданную работу
    (честная пересдача без повторов сюжетов); порог сходства регулируется ползунком.
    Эндпоинт `POST /novelty` (`selectNovelty`).
- **Сортировка «По похожести (лесенка)»** в Генераторе — упорядочивает лист от
  похожих к разным (по `cos` в seed-режиме, иначе цепочкой через `/pairs`).
- **Регулировка похожести параллелей** в `ParallelVariantsModal` — пресет
  «Похожие / Сбалансировано / Разные» (полосы cos), прокидывается в `/parallel-variants`.
- **Бейдж «новизны» набора** (`NoveltyBadge` в блоке векторных инструментов) — после
  генерации показывает, насколько свежий лист относительно задач 5 последних работ
  (% задач, не встречавшихся ранее, + счётчик почти-повторов). Эндпоинт
  `POST /novelty-score` (`scoreNovelty`): maxSim каждой задачи набора к объединению
  задач недавних работ; текущая редактируемая работа из эталона исключается.

### Технические детали
- `pocketbase/vec-search.js`: `selectBySeed`, `selectDiverse` (mmr+clusters),
  `selectNovelty` + хелперы `loadTopicVectors`, `mmrSelect`, `kmeansMedoids`, `cosF32`.
- `pocketbase/pdf-service.js`: роуты `/seed-select`, `/diverse`, `/novelty`.
- `useWorksheetGeneration.generateFromVector(params, options)` — единый метод для трёх
  режимов; дотягивает полные задачи через `api.getTasksByIds`, обрабатывает
  `not_indexed`/503/пустой пул с подсказкой про индексатор.
- Требует деплоя `pdf-service` на VPS. Индекс инкрементится вручную
  (`vector-benchmark → npm run index`) — новые задачи попадают в режимы после прогона.

## [3.9.40] - 2026-05-31

### Добавлено
- **Генератор вариантов ЕГЭ профильного уровня** (`EgeProfileVariantGenerator`,
  «Рабочие листы → Варианты ЕГЭ (профиль)») — по образцу базового генератора:
  - 19 заданий: №1–12 (часть 1, краткий ответ), №13–19 (часть 2, развёрнутый ответ).
    Деление на части по `exam_part` темы (граница — задание 12).
  - Таблица структуры по темам профиля (`egeProfileTopics`), фиксация задач,
    индикатор успеваемости, режимы вариантов, сохранение/выдача — как у базового.
  - **Печать в стиле КИМ**: обложка с профильной инструкцией (3 ч 55 мин), часть 1
    с полями ответа, отдельный блок «Часть 2» + инструкция про бланк № 2, задания
    13–19 без поля ответа (как в ФИПИ). Тот же двухфазный рендер с DOM-измерением и
    раздельной пагинацией частей; часть 2 всегда с новой страницы.
  - **Лист ответов и решений** (обычная печать): краткие ответы №1–12 + развёрнутые
    решения №13–19 (`solution_md`) с указанием `max_score`.
  - Существующий пункт меню переименован в «Варианты ЕГЭ (база)».

### Изменено
- **Узкие боковые поля КИМ-печати** (база и профиль): `.kim-page` боковой padding
  7→5 мм (верх/низ без изменений, 5 мм). Ширина измерительной зоны `.kim-measure-page`
  синхронно 134.5→138.5 мм — пагинация по высоте не меняется.
- **Модалка «Параллельные варианты» (`ParallelVariantsModal`)**:
  - кнопка ✏️ на каждой карточке (образец и параллели) — открывает `TaskEditModal`
    с полной задачей (`api.getTask`); после сохранения карточка обновляется во всём
    семействе по `task_id` без перегенерации (удобно чинить битый LaTeX на месте).
    Удаление задачи — с фолбэком `forceDeleteTask` при 400.
  - карточки выровнены по строкам: построчный grid (row-major) вместо отдельных
    колонок-стопок → соответствующие задачи всегда на одном уровне.
  - колонки больше не вылезают за модалку: `repeat(cols, minmax(0, 1fr))` (вместо
    `1fr`) — длинная неразрывная формула больше не раздувает грид.

### Исправлено
- `ParallelVariantsModal`: убран осиротевший вызов `setSaved` (несуществующий
  сеттер) в ручном выборе замены — он ронял модалку.

## [3.9.39] - 2026-05-30

### Добавлено
- **Векторные инструменты в «Рабочие листы → Генератор»** (`WorksheetVectorTools`):
  - «Дополнить похожими» (`FillSimilarModal`) — догенерация листа семантически близкими задачами через `/similar` (фичи A1 + догенерация); кандидаты сгруппированы по исходным задачам, выбор чекбоксами, добавление в конец листа.
  - «Параллельные варианты» (`ParallelVariantsModal`) — создание работ «оригинал / дубль N» по образцу через `/parallel-variants` (A4).
  - авто-предупреждение о повторах внутри вариантов (`VariantSimilarityWarning`, `/pairs`, A2). Теперь **сворачиваемое** — по умолчанию свёрнуто, видна только сводка с числом найденных пар; разворачивается по клику.

## [3.9.38] - 2026-05-29

### Дедуп-вкладка: удалённые задачи скрываются сразу

`/duplicates` теперь фильтрует из кластеров задачи, которых уже нет в базе
(удалённые): пустые строки «—» не показываются, размер кластера («N задач»)
считается по живым, кластеры с <2 оставшимися пропадают. Происходит на
каждом запросе (живой JOIN), поэтому удалённые задачи исчезают из вкладки
**при перезагрузке** — без ожидания и без пересчёта дублей. (Вектор в vec.db
убирается отдельно прунингом на агенте ≤30 мин — это про место, не про вкладку.)

### Пересчёт дублей — ручной (по запросу учителя)

Дедуп-кластеры обновляются **только вручную**, когда учитель сам решит
(тяжёлая O(n²) кластеризация ~5-6 мин — не место для авто на каждом импорте):
- команда: **`cd vector-benchmark && npm run dedup`** (= `index.mjs --push --dedup`);
- считается **на Mac** (локальная vec.db) → заливается на VPS ручкой
  `POST /upload-clusters` (токен, ретрай ×3). VPS не грузится — только пишет
  файл (серверный пересчёт вешал event loop pdf-service, отказались);
- launchd-агент (индекс векторов, ≤30 мин) дубли **НЕ** трогает;
- токен для ручного запуска берётся из `vector-benchmark/.index-token`
  (gitignored), вводить не нужно.
Индекс векторов (A1/A2/A4/C4) при этом по-прежнему обновляется авто-агентом.

### Дедуп-вкладка: массовое удаление + без рефреша

- У каждой задачи в кластере — **чекбокс**; выбрал несколько → панель
  «Удалить выбранные (N)» одной кнопкой. Кнопка «Выбрать всех сирот»
  (ref_count=0, безопасно удаляемые).
- **Удаление больше не рефрешит страницу** — задачи убираются из локального
  состояния (кластеры с <2 задач уходят сами). И одиночная 🗑, и массовое.
- Используемые в работах задачи в массовом режиме пропускаются (БД не даёт),
  в итоге показывается «удалено N, не удалось M».

## [3.9.37] - 2026-05-29

### Быстрое удаление дубля из вкладки (иконка 🗑)

В дедуп-вкладке у каждой задачи — иконка удаления (рядом с ✏️), без
открытия большого редактора. Popconfirm предупреждает: для сироты —
«удалится безвозвратно», для используемой — «БД, скорее всего, не даст».
При отказе БД (задача в работах) показывается понятная подсказка —
заменить/убрать через ссылки на работы или «Пометить дублями». После
успешного удаления очередь обновляется (вектор уберётся прунингом).

## [3.9.36] - 2026-05-29

### Версия приложения под логотипом

Под логотипом Lemma (боковое меню / шапка) выводится текущая версия мелким
серым шрифтом (`v3.9.36`). Берётся из `__APP_VERSION__` (package.json,
впекается при сборке) — всегда соответствует загруженной сборке. Удобно
проверять, что в браузере свежая версия.

## [3.9.35] - 2026-05-29

### A4: создание параллельных вариантов как работ в «Мои работы»

Модал «Параллельные варианты» теперь доводит дело до результата:
- кнопка **«Создать работы в Мои работы»** создаёт работы из образца и
  параллелей: «<название> — оригинал», «— дубль 1», «— дубль 2» …
- если запущено из сохранённой работы («Мои работы» → 🧬) — исходная
  помечается оригиналом и не дублируется; из генераторов — оригинал
  создаётся из образца;
- после создания — список работ с ролями и кнопками «Открыть»;
- связь сохраняется и на уровне задач (`variant_family`, роли base/parallel_N).
Поле «Название» редактируется. Закрыта дыра «куда делось семейство» —
результат теперь в «Мои работы».

### A4: ручной подбор задач в параллельных вариантах

В модале «Параллельные варианты» теперь нет «дырок»:
- пустой слот (нет похожей задачи) → кнопка **«Выбрать»** → каталог задач
  (`TaskSelectModal`) → подставляется в ячейку (бейдж «вручную»);
- у любой подобранной задачи — кнопка **«заменить»** (↻) → тот же каталог;
- уже занятые в семействе задачи исключаются из выбора (без повторов).
Заполненные вручную задачи попадают в «Сохранить как семейство».

### Дедуп-вкладка: ссылки на работы + «Не дубли» сохраняется

- У каждой задачи в кластере — **кликабельные чипы работ**, где она
  используется (`works` в `/duplicates`): клик открывает редактор работы
  (там можно заменить/убрать задачу через `TaskReplaceModal`). Зелёный
  «не используется» = можно удалять.
- **«Не дубли» теперь персистится** (новый тип `reviewed_not_dup` в
  `task_families`, миграция 1779000005): помеченный кластер больше не
  возвращается в очередь ревью (раньше скрывался только в текущей сессии).
  `/duplicates` фильтрует кластеры, чьи члены уже в `dedup_cluster` ИЛИ
  `reviewed_not_dup`. `api.markNotDuplicate`.

### Редактор задач и чистка дублей в дедуп-вкладке

В вкладке «🔎 Похожие (вектор)» (каталог) теперь можно править/удалять задачи
прямо из кластера:
- у каждой задачи бейдж **«в N работах»** (`ref_count` в `/duplicates` —
  считает ссылки в `variants.tasks`): красный «в N раб.» = удаление урежет
  работы (безопаснее «Пометить дублями»), зелёный «не исп.» = можно удалять.
- кнопка ✏️ открывает существующий `TaskEditModal` (правка / удаление /
  починка LaTeX, с audit-логом). После удаления очередь обновляется.

**Прунинг осиротевших векторов:** удалённые задачи больше не оставляют
«мусорные» векторы. `index.mjs --push` собирает актуальные id и зовёт
`POST /prune-vectors` (токен) — на каждом launchd-прогоне (30 мин) vec.db
самоочищается. (Поиск и так игнорировал осиротевшие через JOIN — это гигиена.)

### Работа над ошибками с похожими задачами (C4, фаза 1)

В модал «Работа над ошибками» (страница ученика) добавлен подбор **похожих
задач для свежей отработки**: кнопка «Подобрать» по выбранным проваленным
задачам → для каждой подбираются 2 похожие (тот же навык, другие числа —
cos 0.70–0.97), с чекбоксами. Итоговая работа = проваленные + выбранные
похожие; выдача ученику (сессия + ссылка) как раньше.
- pdf-service: `POST /remediation { failed_task_ids, per_task, exclude_ids? }`
  — KNN внутри темы в полосе «тот же навык, не клон», сгруппировано по
  исходной задаче, дедуп, без оригиналов.
- `StudentDetailPage`: секция «🩹 Похожие для отработки» в существующем
  модале (реюз `createWork`/`createVariant`/`createSession`).

**Класс-режим (фаза 3):** в результатах сессии (`TeacherResultsDashboard`) —
кнопка «🩹 Работа над ошибками класса»: агрегирует провалы по всем попыткам,
показывает частые ошибки (≥ порога, по умолчанию 40%), подбирает похожие и
создаёт одну работу + сессию на весь класс. `ClassRemediationModal` (новый).

## [3.9.34] - 2026-05-29

### Семейство параллельных вариантов «по образцу» (A4)

Кнопка **«🧬 Параллельные варианты (по образцу)»**: для каждой задачи образца
подбирается похожая (та же тема и тип, другие числа — cos 0.85–0.995),
собираются N параллельных вариантов. Сценарий: подготовка → контрольная →
пересдача.
- pdf-service: `POST /parallel-variants { task_ids, count, min_cos?, max_cos? }`
  — пул сиблингов на позицию (KNN внутри темы в полосе), жадное распределение
  без повторов; нехватка по позициям отдаётся в `shortage`.
- `components/worksheet/ParallelVariantsModal.jsx` (новый) — колонки
  «Образец | Параллель 1..N», бейдж % близости, пустые слоты при нехватке.
- **Доступно в трёх местах:** «Контрольные работы» (после генерации),
  «Варианты ЕГЭ» (после генерации), «Мои работы» (по сохранённой работе — A4
  поток 2: берёт задачи первого варианта работы как образец).
- **Сохранение семейства:** кнопка «Сохранить как семейство» →
  `task_families(variant_family)` + members с ролями `base` / `parallel_N`
  (`api.markVariantFamily`). Связь подготовка↔контрольная фиксируется в БД.

### Семантический поиск похожих задач (векторная БД) — Этап 1

Первая фича на основе векторного поиска: в `TaskEditModal` появилась
сворачиваемая панель **«🔎 Похожие задачи»** — показывает ближайших соседей
задачи с процентом похожести (бейдж) и превью условия. Тумблер «только эта
тема» (по умолчанию вкл — тема надёжный признак типа задачи).

**Архитектура (Option B+, см. `lemma_vector_search_idea.md`):**
- Эмбеддинги `bge-m3` (1024d) считаются локально на Mac через Ollama,
  хранятся в отдельной `vec.db` (sqlite-vec, `distance_metric=cosine`),
  которой владеет pdf-service. Боевая `data.db` открыта только на чтение.
- Поиск: `POST /pdf/similar { task_id, limit?, same_topic_only?, min_cos? }`
  → `{ items: [{ task_id, cos, pct, code, topic, statement }] }`.
  JOIN `vec_tasks ⋈ tasks` одним запросом (~4мс на 11k задач).
- Калибровка процента: `cos 0.55 → 0%`, `cos 0.95 → 100%` (валидирована
  бенчмарком на 80 размеченных парах: порог дедупа cos≥0.90 = 100% precision).

**Backend:**
- `pocketbase/vec-search.js` — модуль поиска (readonly data.db + ATTACH vec.db).
- `pocketbase/pdf-service.js` — роуты `POST /similar`, `GET /similar/health`.
- Миграция `1779000004` — коллекции `task_families` + `task_family_members`
  (для будущего дедупа B2 и семейств вариантов A4; задачи помечаются, не мёржатся).
- `vector-benchmark/index.mjs` — индексатор (Mac → vec.db), инкрементальный
  по `text_hash`. Первичная индексация всей базы (11277 задач) ≈ 20 мин.

**Frontend:**
- `components/SimilarTasksPanel.jsx` (новый) — переиспользуемая панель.

### Дедуп-ревью в каталоге (B2)

В `TaskCatalogManager` новая вкладка **«🔎 Похожие (вектор)»** — очередь
кластеров похожих задач на ревью:
- `dedup-scan.mjs` кластеризует базу (cos≥0.93 внутри темы, union-find) и
  классифицирует: точные дубли (общий дискриминативный ответ) vs
  параметрические семейства. Прогон на 11277 задачах: 296 кластеров дублей.
- pdf-service: `GET /duplicates` — отдаёт предвычисленные кластеры с живым
  JOIN к data.db; уже размеченные (члены в dedup_cluster) скрываются.
- `markDedupCluster` пишет `task_families(dedup_cluster)` + members. Задачи
  НЕ удаляются — только связываются (используются во многих работах).
- `components/catalog/VectorDuplicatesTab.jsx` (новый) — карточки кластеров,
  кнопки «Пометить дублями» / «Не дубли», пагинация.

### Авто-обновление индекса (инкремент)

Новые/изменённые задачи теперь сами попадают в векторный индекс — без
ручного scp/рестарта:
- pdf-service: `POST /index-vectors` (защита `X-Index-Token`) — приём
  батчей векторов, upsert в `vec.db` отдельным writable-соединением,
  инвалидация поискового соединения (свежие данные видны сразу).
- `index.mjs --push` — инкрементальный прогон (по `text_hash`) шлёт только
  новые векторы прямо на VPS.
- launchd-агент `com.lemma.vecindex` на Mac (раз в 30 мин) гоняет `--push`
  автоматически. Прогон вхолостую (нет изменений) ≈ 28с, эмбеддит 0.
- Зависит от включённого Mac (эмбеддинг через локальную Ollama). Полностью
  серверная индексация — отдельное решение (нужна embed-модель на VPS).

### Предупреждение о повторах внутри варианта (A2)

В `TestWorkGenerator` (контрольные работы) под статистикой вариантов —
предупреждение, если внутри варианта есть похожие задачи (cos≥0.78):
- pdf-service: `POST /pairs { task_ids, min_cos }` → похожие пары среди набора
  (попарный косинус в JS, N задач варианта — мгновенно).
- `components/worksheet/VariantSimilarityWarning.jsx` (новый) — Alert со
  списком пар «№X (код) ↔ №Y (код) · N%» по вариантам; тихо молчит, если
  сервис недоступен или повторов нет.

## [3.9.33] - 2026-05-28

### Фикс дубликата картинок в импортированных sdamgia-задачах

Symptom: после импорта задачи карточка показывала одну и ту же картинку
дважды — один раз inline через markdown, второй раз отдельным `<Image>` под
условием. Заметно на ЕГЭ-База и ЕГЭ-Профиль (часть 1) — везде где `processCondition`
извлекает картинку.

**Причина:** в v3.9.31 серверный `processCondition` оставлял `![image](url)`
прямо в тексте `condition` markdown «для backward-compat». Но 34 места рендера
условия (TaskCard, EgeVariantGenerator, PrintableWorksheet, MarathonGenerator
и др.) исторически ожидают **чистый текст + отдельный `<Image>`** из file-поля
`tasks.image` — этот контракт держится с самого первого markdown-парсера, где
картинка всегда **вырезалась** из statement_md и сохранялась отдельно.

**Фикс** (`pocketbase/pdf-service.js`):
- В `processCondition` маркер `___IMAGE_N___` теперь заменяется на пустую
  строку, а не на `\n![image](${fullUrl})\n`. Картинки остаются доступны через
  `condition_images` массив (для `TaskEditModal` и `RefreshFromSdamgiaModal`,
  которые используют `TaskStatementRenderer` с подменой URL).

**Миграция уже импортированных задач** (`migrations_scripts/strip_inline_image.py`):
- Безопасный скрипт с dry-run / `--apply` режимами. Перед удалением проверяет
  что у задачи есть либо `image` file, либо запись в `task_images` с
  role='condition' — иначе пропускает (защита от потери картинки).
- На VPS: 3369 sdamgia-задач, 286 с inline ![image], **у всех 286 локальная
  копия есть** (0 опасных случаев). Все 286 обновлены за один прогон.
- Бэкап БД: `pre_image_dedup_20260528_231544.db` (27 МБ).

## [3.9.32] - 2026-05-28

### Парсер sdamgia: новый формат alt MathML + LLM на импорте

**Парсер alt MathML без пробелов и soft-hyphens** (`pocketbase/pdf-service.js`):
- Sdamgia постепенно мигрирует на новый формат alt-текста: слова склеены без
  пробелов и без мягких переносов (`кореньиз`, `началоаргумента`, `конецаргумента`,
  `леваяквадратнаяскобка` и т.п.). Старые regex'ы для `корень из:` /
  `начало аргумента:` молча перестают работать.
- В `cleanLatexFormula()` добавлен **pass-1 над словарём**: для каждой
  многословной записи автоматически генерируется concat-вариант (`плюсминус`,
  `умножитьна`, `натуральныйлогарифм`, `леваяквадратнаяскобка`, ...). Покрывает
  десятки фраз без ручного дублирования каждой строки.
- Добавлены явные regex'ы для `√:началоаргумента:X конецаргумента` →
  `\sqrt{X}` (Unicode U+221A) и `∛`/`∜` → `\sqrt[3]`/`\sqrt[4]`.
- Дополнительные regex'ы для `корень из : началоаргумента : ...` (с
  пробелами вокруг двоеточий) и `\sqrt[N] : началоаргумента : ...`.
- Fraction regex стал терпимее к пробелам вокруг `:` и форме `конецдроби`/
  `конец дроби`.

**LLM-обработка по галочке на этапе предпросмотра импорта**
(`ege-tasks/src/{hooks/useTaskImport.js,components/TaskImporter.jsx}`):
- Рядом с обычной галочкой «выбрать для импорта» появилась вторая «🤖» —
  пометить задачу для прогона через `/latex-fix` во время импорта.
  По умолчанию все 🤖 не нажаты (импорт без LLM работает как раньше).
- В шапке списка кнопки массового выбора: **«🤖 для проблемных»** (только
  задачи с `latex_needs_review`), **«🤖 для всех»**, **«Снять 🤖»**.
- Хук `useTaskImport`: новый Set `llmTasks`, методы `toggleLlmTask`,
  `selectAllLlm`, `selectAllLlmNeedsReview`, `deselectAllLlm`. В
  `handleImport()` для каждой отмеченной задачи перед `createTask` идёт
  fetch к `/latex-fix` на statement/solution/criteria. Любая ошибка LLM
  не блокирует импорт — задача уходит в БД с оригинальным текстом + warning.
- Кнопка импорта показывает «Импортировать N задач (🤖 LLM для M)».

**LLM-промт + safety net** (`pocketbase/pdf-service.js`):
- В `LATEX_FIX_SYSTEM_PROMPT` добавлен явный блок «🚨 ОБЁРТКИ ФОРМУЛ»:
  всегда `$...$` / `$$...$$`, **никогда** `\(...\)` / `\[...\]` (KaTeX-плагин
  react-markdown их не видит). Если во входе есть `\(...\)` — заменить на
  `$...$`, если `\[...\]` — на `$$...$$`.
- Server-side safety net `normalizeLatexDelimiters()`: даже если модель
  проигнорирует правило, ответ всё равно конвертируется. Аккуратно
  пропускает содержимое уже корректных `$...$` / `$$...$$` блоков, чтобы
  не задеть похожие на скобки символы внутри формул.

## [3.9.31] - 2026-05-27

### Импорт задач ЕГЭ часть 2 из «Решу ЕГЭ» (sdamgia.ru)

Полноценная поддержка задач второй части ЕГЭ-профиль: развёрнутое решение с
несколькими чертежами, критерии оценивания, максимальный балл, ссылка на
исходную задачу, локальное хранение всех картинок.

**База данных** (миграции `1772000040`, `1772000041`):
- В коллекции `tasks` новые поля: `sdamgia_id` (index), `sdamgia_url`,
  `exam_part` (1 или 2, default 1), `criteria_md`, `max_score`,
  `latex_needs_review`.
- Новая коллекция `task_images` — раздельное хранение чертежей задачи по
  ролям `condition`/`solution`/`criteria` с `order`, `file`, `sdamgia_file_id`,
  `original_url`. Cascade-delete по `task`.
- Бэкфил `exam_part=1` для 7760 существующих задач.

**Серверный парсер** (`pdf-service.js` + новый модуль `latex-fixer.js`):
- В `/parse-sdamgia` ответе на каждую задачу теперь возвращаются:
  `sdamgia_url`, `criteria_md`, `max_score`, `latex_needs_review`,
  структурированные `condition_images`/`solution_images`/`criteria_images`
  с объектами `{url, file_id, order, role}`.
- В URL sdamgia автоматически добавляется `&crit=true` (иначе .prob_crits скрыт).
- Критерии — md-таблица; max_score — из правой колонки или явного «Максимальный балл».
- `cleanLatexFormula()` дополнительно прогоняет 13 эвристик KaTeX-нормализатора
  `fixLatex()`: `\angleABC → \angle{ABC}`, степени/индексы в круглых
  скобках → фигурные, `90 г → 90^{\circ}`, `\log a 32 → \log_{a} 32`,
  `0,5 → 0{,}5`, многосимвольные степени, балансированный обход двойных
  subscript'ов и т.п. Покрытие — 20 юнит-тестов.
- Каждая формула задачи прогоняется через `katex.renderToString` — если хоть
  одна не валидна, ставится флаг `latex_needs_review: true`.
- Бенчмарк на 2810 формулах из 4 категорий (272/276/277/282): broken до
  фиксера 96 (3.4%), после — 42 (1.5%), регрессий 0.
- Новая ручка `POST /latex-fix`: принимает текст + role, прогоняет через
  Timeweb AI gateway (deepseek), возвращает исправленный текст. In-memory
  LRU-кэш по sha256(text), max 500. Используется только вручную (учитель
  жмёт кнопку для проблемной задачи).

**Фронт — API**:
- `getTaskImages(taskId)` — картинки по ролям, ordered.
- `findTaskBySdamgiaId(id)` — идемпотентность импорта.
- `uploadTaskImageFromUrl({task, role, order, url, sdamgia_file_id})` —
  fetch blob → залить + дедуп по `(task, role, order)`.
- `getTaskImageRecordUrl(record)`.

**Фронт — импорт**:
- `parseSdamgiaResult` пробрасывает все новые поля сервера в задачу.
- `useTaskImport.handleImport`: дедуп №1 по `sdamgia_id`, №2 по тексту;
  сохранение новых полей; после `createTask` — аплоад картинок в `task_images`
  по ролям; провал по картинкам не откатывает создание задачи.
- `useTaskImport.applyLatexFix(index, fields)` — обновление задачи в
  состоянии после LLM-фикса (сбрасывает `latex_needs_review`).
- В UI импорта: селектор «Часть экзамена 1/2», бейджи в предпросмотре
  («Часть 2», «N б.», «📷 N», «⚠ Проверить LaTeX»), кнопка «🤖 LLM-fix»
  для проблемных задач.

**Фронт — рендеринг**:
- Новый `TaskStatementRenderer` — тонкий wrapper над `MathRenderer`, который
  ловит `![image](внешний_url)` и подменяет URL на локальный из коллекции
  `task_images` (по `original_url` или извлечённому `sdamgia file_id`).
  Старые задачи без `task_images` продолжают работать с внешним URL.
  Чистая функция `rewriteImageUrls` покрыта 8 юнит-тестами.
- `TaskEditModal`: грузит `task_images` через `api.getTaskImages` при
  открытии; превью условия/решения через `TaskStatementRenderer`. Новый
  Collapse «Критерии оценивания» (для задач части 2) с md-таблицей критериев.
  В заголовке модала — бейджи «Часть 2», «N б.», «⚠ Проверить LaTeX» и
  кнопка-ссылка «↗ Решу ЕГЭ (id N)» по `sdamgia_url`.

**End-to-end тест**: 5 задач из категории 276 — задачи, картинки (9 шт., с
двойным включением одного чертежа в одну задачу), критерии (729 байт),
`exam_part=2`, `max_score=3` — всё ложится корректно; повторный запуск —
0 добавлено, 5 пропущено (идемпотентно).

**Open-loop**: чтобы кнопка «🤖 LLM-fix» начала работать, нужно положить
`TIMEWEB_AI_URL` и `TIMEWEB_AI_KEY` в env pdf-service-ege на VPS. Без них
ручка отвечает 503 с понятной ошибкой, остальные функции работают как обычно.

## [3.9.30] - 2026-05-23

### Аватарки пользователей

- Миграция `1779000003`: поле `avatar` (file, max 2MB, image-only)
  в коллекции `teachers`. PocketBase генерирует превью 64x64 и 120x120.
- `api.updateTeacher(id, { avatar: File })` — загрузка через FormData;
  `avatar: null` — удаление; обычные текстовые поля работают как раньше.
- `api.getTeacherAvatarUrl(teacher, size)` — хелпер; `size`:
  `small` (64x64) / `medium` (120x120) / `null` (оригинал).
- `ProfileModal` — окно "Мой профиль" из UserMenu: смена имени, аватарки,
  пароля. Доступно любому залогиненному учителю (updateRule self/superadmin).
- `UserMenu` показывает реальную аватарку вместо иконки `<UserOutlined>`.
- `UserManager`: колонка с аватаркой в таблице; загрузчик в модале
  редактирования (только при editing — для создания учитель сам поставит
  через "Мой профиль").
- После загрузки аватарки в ProfileModal вызывается `authRefresh()` —
  модель в `authStore` обновляется → UserMenu сразу показывает новую.

## [3.9.29] - 2026-05-21

### Система авторизации учителей с ролями и секциями

**Новые коллекции PocketBase** (миграции `1779000000`, `1779000001`):
- `teachers` (auth) — учителя с ролями (`superadmin`/`editor`/`viewer`) и
  `allowed_sections` (json-массив доступных разделов)
- `audit_log` (base) — журнал значимых действий (create/update/delete для
  tasks, works, geometry_tasks, tdf_sets, theory_articles, mc_tests, teachers)
- Самостоятельная регистрация запрещена — пользователей создаёт суперадмин
- Первый суперадмин: `evilfaust` (создан автоматически миграцией)

**AuthContext + связанные компоненты:**
- `AuthContext` / `useAuth()` — `teacher`, `role`, `hasSection`, `canEdit`,
  `canDelete`, `isSuperAdmin`, `login`, `logout`. Очистка чужих токенов
  (студенческих) при инициализации.
- `LoginPage` (`/login`) — username + password + "Запомнить меня".
  Без флага токен очищается при закрытии последней вкладки.
- `ProtectedRoute` — поддерживает `requireSuperAdmin`, `requireEdit`,
  `requireSection`. Все `/app/*` маршруты теперь под защитой.
- `UserMenu` в шапке — имя, роль, кнопка "Выйти".
- `<CanEdit>`, `<CanDelete>`, `<SuperAdminOnly>` — обёртки для условного
  рендера UI-элементов.

**Управление пользователями** (`/app/admin/users`, только superadmin):
- Таблица учителей с ролями и набором секций
- Создание/редактирование (username, name, password, role, секции)
- Удаление с подтверждением (нельзя удалить себя)

**Журнал действий** (`/app/admin/audit`, только superadmin):
- Таблица с фильтрами по действию, коллекции, учителю, диапазону дат
- Запись fire-and-forget из мутирующих API-методов через `_logAudit()`
- Иммутабельный лог (нельзя редактировать или удалять записи через UI)

**Фильтрация меню по правам:**
- Каждый верхний пункт меню маркирован `section` (одна из `ALL_SECTIONS`)
- Пункты, на которые у пользователя нет доступа, скрываются
- Поддержка `editOnly: true` — пункт виден только editor/superadmin
- superadmin всегда видит все секции (hasSection всегда true)

**Viewer-режим:**
- Маршруты-редакторы (`work-editor`, `import`, `achievements`,
  `geometry/topics`, `tdf/sets/:id/edit|variants`, `theory/articles/new|edit`,
  `theory/categories`) под `requireEdit` — viewer попадает на `/app/tasks`
- Кнопки create/edit/delete скрыты в TaskList, TaskFilters, TaskCard,
  WorkManager, TheoryBrowser, TheoryArticleView, GeometryTaskList,
  TDFManager, MCTestGenerator, catalog/* tabs, worksheet/ActionButtons
  (кнопка "Сохранить" в большинстве генераторов)

**Защита API:** UI-only (по согласованию). Все правила коллекций PocketBase
для `tasks`/`works`/etc. остались публичными — viewer теоретически может
обойти UI через DevTools, но это не угроза для приватной платформы.

## [3.9.28] - 2026-05-20

### Марафон: рабочий лист ученика и листы учителя

**Рабочий лист ученика** (`MarathonWorksheetPrint`):
- Режимы: 2 / 3 / 4 / 5 задач на листе A4
- Каждый блок задачи: поле ФИ + квадратики попыток (2–6) + условие + клетка 5 мм для решения
- Чертёж накладывается поверх клетки (левый верхний угол, размеры S/M/L/XL)
- Линия разреза ✂ между блоками
- Настройки: задач на листе, кол-во попыток, размер текста S/M/L, размер чертежа S/M/L/XL
- Клетка реализована HTML-div'ами с физическими mm/pt-единицами → чистый вектор при печати

**Компактный лист ответов** (`MarathonTeacherSheet`):
- Один A4, автосетка карточек (2–5 колонок) по числу задач
- Каждая карточка: тёмная шапка с №, чертёж, крупный ответ (5 mm, LaTeX)
- Для большого набора простых задач (марафон на скорость)

**Полный лист учителя** (`MarathonTeacherSheetFull`):
- Таблица: № | условие + ответ (с чертежом float-right) | решение/подсказка
- Разбивается на страницы A4, строки не разрываются (`break-inside: avoid`)
- Для сложных задач, где нужен контекст при проверке

Все три используют единый scoped print-паттерн `body:has(.root-class)` + `position: absolute; top:0; left:0`.

## [3.9.27] - 2026-05-17

### Live-дашборд марафона: светлая тема для проектора

`MarathonLiveBoard` — кнопка `☀️ Светлая / 🌙 Тёмная` в хедере переключает тему.
Светлая тема оптимизирована для вывода через проектор:

- Фон: белый с мягким голубовато-лавандовым градиентом и точечной сеткой
- Хедер: белый, синяя нижняя рамка; заголовок — тёмный сине-фиолетовый шиммер
- Таблица: белая с чёткой тенью, высококонтрастные ряды
- Топ-1 имя: янтарно-коричневый градиент; топ-3 — `#92400e` (тёмная бронза)
- Задачи: решённые — насыщенный зелёный, провал — белый с красной рамкой,
  в процессе — синий пульс (своя анимация `mlb-task-pulse-light`)
- Вспышка строки, корона и шиммер адаптированы под светлый фон
- Реализовано через класс `.mlb-root--light` — тёмная тема без изменений

### Движок достижений: диапазон score + тип streak

`achievementEngine.js`:
- `score` теперь поддерживает `condition_value.max` (не только `min`) — диапазоны вроде «50–70%»
- Новый тип `streak`: N попыток подряд с результатом ≥ `min_score`
- Новые иконки достижений `icon079–icon089.jpg`

## [3.9.26] - 2026-05-16

### Редизайн «Генератора» в рабочих листах

Маршрут `/app/worksheets/oral` (`OralWorksheetGenerator`) — пункт меню «Генератор».
Файл сокращён 946 → ~440 строк за счёт декомпозиции в 6 подкомпонентов
в `ege-tasks/src/components/worksheet/oral-generator/`:

- `GeneratorHeader.jsx` — крупный Segmented «Лист задач / Карточки» в самой шапке
  (раньше этот первичный выбор был спрятан в 5-й секции Collapse «Формат печати»)
  + кнопка «Мои работы» (открывает `LoadWorkModal`).
- `HeroSection.jsx` — каскад **Контекст → Тема → Подтема** в одной строке
  (паттерн как в `TaskFilters.jsx`); смена контекста сбрасывает тему/подтему/теги/распределения;
  смена темы — подтему/теги/распределения. Бейдж «N задач доступно» (`useTaskCounter`).
  Ниже — `[N] × [M] = X задач` (вариантов × задач/вариант), Segmented «Режим вариантов»
  (Разные / Перемешанные / Одинаковые), главная кнопка «Сгенерировать».
- `FiltersAndDistribution.jsx` — Collapse + 3 таба (Фильтры / По тегам / По сложности).
  Активные распределения видны как теги в названии таба.
- `AppearanceSection.jsx` — Collapse «Оформление», контент **ветвится по `outputMode`**:
  sheet → `FormatSettings` + sheetFormat A4/A5; cards → cardFormat A6/A5/A4 + ответы/решения/ФИО.
- `ResultActionBar.jsx` — sticky `Affix` сверху превью после генерации: статистика
  «N × M = X задач • Лист задач · A4» + `ActionButtons`. Раньше статистика была отдельным
  большим блоком `VariantStats`, а кнопки терялись под формой при скролле.
- `WorksheetPreview.jsx` — рендер превью: для sheet — variants → pages + `WorksheetGridPrint`
  + `AnswersPage`; для cards — `PrintableWorksheet`.

`filters.exam_type` теперь передаётся в `generateFromFilters` — генератор точечно
берёт задачи из выбранного контекста.

Логика `handleGenerate`, валидация распределений, печать/PDF/MD-экспорт, сохранение/загрузка
работ, drag&drop, шифровка — без изменений, только переехало в orchestrator.

### Удалён legacy `WorksheetGenerator.jsx`

Файл `ege-tasks/src/components/WorksheetGenerator.jsx` (705 строк, карточки A5/A6/A4)
не был подключён ни к одному роуту с момента слияния режимов «Лист / Карточки» в
`OralWorksheetGenerator` (`3778645 feat: Объединение генераторов в один инструмент`).
Чистка мёртвого кода.

### Поддержка нескольких формул в типе «геометрическая формула» (ТДФ)

`tdf_items.short_notation_md` теперь принимает несколько формул (каждая с новой строки).
В контрольном варианте `TDFPrintView` рендерится отдельный слот «X = ___» для каждой
формулы (LHS извлекается до первого `=` через `parseFormulas()`). Layout пункта
перекомпонован: `№ | чертёж | колонка [название → подсказка → слоты]`. Подсказка
«Запишите N формул:» с правильной формой множественного числа через `pluralRu()`.

## [3.9.25] - 2026-05-15

### Тестовая инфраструктура (Vitest + Testing Library)

Добавлено покрытие unit-тестами для критических модулей: парсеры ответов, генераторы
дистракторов, движок достижений, утилиты пиксель-арта, парсер заданий и др. 287 тестов
в 21 файле, прогон ~2 сек.

**Установлено:** `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`.

**Запуск:**
```bash
cd ege-tasks
npm test            # однократный прогон
npm run test:watch  # режим наблюдения
```

**Покрытие — утилиты** (`src/utils/`):
- `answerChecker` — числа, дроби, LaTeX `\frac`/`\dfrac`, смешанные дроби, альтернативы `|`
- `normalize`, `filterTaskText`, `shuffle`, `taskCodeGenerator`, `matrixOps`
- `distractorGenerator` — детерминированный shuffle с seed (FNV-1a + Mulberry)
- `cryptogram` — нормализация фразы, биекция букв↔задач, decoys
- `escapeFilter` — экранирование для PocketBase filter (защита от инъекций)
- `splitMatrix` — разбиение на тайлы для командного пиксель-арта
- `markdownTaskParser` — `parseEgeTasks`, `parseYamlFrontmatter`, `convertToLatex`
- `qrMatrix` + `qrGridFill` — QR-листы
- `crosswordLayout` — алгоритм расстановки слов
- `achievementEngine` — fallback-цепочка редкостей, условные достижения

**Покрытие — компоненты и хуки:**
- `MathRenderer` — Markdown + LaTeX рендеринг
- `AchievementBadge` — рендер по rarity, locked, размеры
- `RouteStatementRenderer` — токенизация LaTeX + плейсхолдеры `[①]–[⑨]`
- `EgeScoreCalculator` — клики по тайлам, переключение баллов
- `useAutosave` — fakeTimers, localStorage с articleId
- `useDistribution` — CRUD items, валидация, `expectedTotal`
- `useMarathon` — управление задачами/учениками, `markAttempt` с лимитом 3 попытки

**Конфигурация:**
- `vitest.config.js` — jsdom environment, alias `@shared`
- `src/__tests__/setup.js` — polyfills для `window.matchMedia` и `ResizeObserver`
  (требуются Ant Design в jsdom)

### Fix — KaTeX warnings в консоли

Плейсхолдеры маршрутного листа `[①]–[⑨]` внутри LaTeX-блоков (`$...$`) выдавали в консоль
сотни warning'ов: `Unrecognized Unicode character` + `No character metrics`.

**Решение** — `MathRenderer.jsx`:
- Препроцессор `preprocessLatex()` оборачивает `①–⑩`, `❶–❿` внутри `$...$`/`$$...$$` в `\text{…}`
- Опции `rehype-katex`: `strict: 'ignore'`, `trust: true`, `throwOnError: false`

Теперь кружковые цифры в формулах рендерятся как обычный текст, консоль чистая.

## [3.9.24] - 2026-05-15

### ТДФ — Новый тип «Геометрические формулы»

Восьмой тип пункта ТДФ — `geometry_formula` (lime). Для тренировки формул площади, объёма
и т.п.: ученику даётся **частичный чертёж**, он подписывает буквы и записывает формулу.

**Два чертежа на одну карточку:**
- **Подготовка** (`drawing_image` + `geogebra_base64`) — полный чертёж со всеми обозначениями
- **Контроль** (`drawing_image_control` + `geogebra_base64_control`) — частичный, без подписей

**Workflow редактора** (вкладка «Чертежи» в `TDFItemModal`):
1. Вкладка «🟢 Подготовка» — учитель рисует полный чертёж в GeoGebra, нажимает «Сохранить PNG»
2. Вкладка «🔴 Контроль» — нажимает «Загрузить эталонный чертёж» → копия XML переносится
   через `prepApi.getBase64()` → `ctrlApi.setBase64()`, далее удаляет лишние подписи
3. «Сохранить PNG» в контроле — частичный чертёж готов

**Компактный формат печати контрольного варианта** (`TDFPrintView`):
- Автодетект: вариант состоит только из `geometry_formula` И mode='blank' → новый layout
- A4 portrait делится на 1 или 2 узкие полоски (переключатель в тулбаре)
- Каждая полоска: заголовок + ФИ + список {№, чертёж контроля, «S = ____» с линией для записи}
- Лишние контролы (ориентация, размер чертежа, сетка) скрыты — неприменимы

**БД (миграции 1772000037-39):**
- `drawing_image_control` (file 5MB) + `geogebra_base64_control` (text, max 500000)
- `formula_control_hidden` (bool, default true)
- `geometry_formula` добавлен в select `type`
- ⚠️ Лимит `geogebra_base64` поднят с дефолтных 5000 до 500000 — PB 0.36.4 не понимает `max:0`

**Файлы:**
- `pocketbase/pb_migrations/1772000037_add_geometry_formula_to_tdf.js`
- `pocketbase/pb_migrations/1772000038_expand_tdf_geogebra_fields.js`
- `pocketbase/pb_migrations/1772000039_set_explicit_max_geogebra_fields.js`
- `ege-tasks/src/components/tdf/TDFItemModal.jsx` — UI с двумя GeoGebra-апплетами и динамической высотой
- `ege-tasks/src/components/tdf/TDFPrintView.jsx` + CSS — гео-формат печати
- `ege-tasks/src/shared/services/pocketbase.js` — `getTdfItemControlDrawingUrl(item)`

---

## [3.9.23] - 2026-05-14

### Арифметика — Генератор «Степени и логарифмы»

Второй генератор устного счёта в разделе «Арифметика» — **Степени и логарифмы** (`/app/arith/log-exp`).
Ученик решает уравнения вида `2^x = 8` или `log₃ x = 2` и вписывает ответ.

**13 категорий уравнений** (показательные + логарифмические):
- Показательные: `aˣ = aⁿ`, `(p/q)ˣ = (r/s)ˣ` → x=0, `a^(x±k) = aⁿ`, `a^(−kx) = aⁿ`, `aˣ · bˣ = c`
- Логарифмические: `logₐ x = n`, `log_x a = n`, `logₐ(kx) = n`, `logₐ x ± logₐ b = n`,
  `k + logₐ x = n`, `logₐ(x/k) = n`, `lgx = lg a + lg b`

**Режим уравнений (`equationMode`):**
- Инструкция «Решите уравнения:» вместо «Вычислите:»
- Задание: полное уравнение + подсказка «x =» для ответа
- Лист учителя: «x = [красный ответ]»
- В превью на экране показываются уравнения с ответами

**Файлы:**
- `ege-tasks/src/hooks/useLogExpEquations.js` — генераторы 13 категорий
- `ege-tasks/src/components/LogExpEquationsGenerator.jsx` — UI с группировкой категорий
- `ege-tasks/src/components/trig/OralCountingPrintLayout.jsx` — добавлен prop `equationMode`
- `ege-tasks/src/components/trig/OralCountingPrintLayout.css` — `.oral-task-expr--eq`, `.oral-task-x-prompt`

---

## [3.9.21] - 2026-05-13

### Арифметика — Генератор устного счёта

Новый раздел «Арифметика» в меню с первым генератором — **Устный счёт** (`/app/arith/oral-counting`).

**10 категорий задач** (каждая включается/выключается независимо):
- Дробь × целое, Целое ÷ дробь, Дробь ÷ целое, Дробь ÷ дробь, Целое × (1/n)
- Целое ± смешанное число
- Степень десятичной дроби: `(-0.2)²`, `(-0.3)³`
- Умножение/деление на степень 10: `7.3 · 10³`, `-2.4 : 0.1`
- Деление десятичных: `3.5 : 0.5`, `0.28 : 0.4`
- Десятичные ± целое: `4.2 − 8`, `-10 + 2.8`

**Знак деления** — «:» (российский стандарт).

**Три режима печати:**
- Один вариант на лист A4
- 2 варианта верх/низ (`twoPerPage`)
- 2 варианта лево/право (`sideBySide`) — по умолчанию; каждая половина — отдельный столбец

**Оформление листа:**
- 2-строчная шапка: строка 1 — «Вариант N» + «ФИО:» (широкое поле); строка 2 — «Класс:» + «Дата:»
- Знак `=` стоит сразу за выражением (без правого выравнивания)
- Опциональная 2-колоночная сетка заданий на полном листе

**Тесты:** кнопки «Тест A/B/C/D» и «Вписать ответ» через существующий `TrigMCSaveModal` (`generatorType='oral_counting'`).

**Файлы:**
- `ege-tasks/src/hooks/useOralCounting.js` — логика генерации + `CATEGORY_LABELS` + `DEFAULT_SETTINGS`
- `ege-tasks/src/components/OralCountingGenerator.jsx` — UI
- `ege-tasks/src/components/trig/OralCountingPrintLayout.jsx` — компонент печати
- `ege-tasks/src/components/trig/OralCountingPrintLayout.css` — стили печати

---

## [3.9.20] - 2026-05-10

### Геометрия — SVG-конвертер GeoGebra

Новый модуль `utils/ggbToSvg.js` — конвертация GeoGebra XML в векторный SVG через JSXGraph без открытия браузерного апплета. Результат хранится в поле `drawing_svg` (до 200 KB) и рендерится в рабочем листе и просмотре без зависимости от GeoGebra API.

**Поддерживаемые объекты:** точки, отрезки, лучи, прямые, полигоны, углы (прямые — квадратик, обычные — дуга).

**Нюансы конвертации:**
- Раздельные масштабы X/Y (`scale` / `yscale` из `<coordSystem>`) — корректно обрабатываются
- Текстовые подписи: GeoGebra 6 хранит содержимое в `<expression exp="...">`, GeoGebra 5 — в `<textExpression val="...">` (поддержаны оба формата); `{,}` → `,`
- Засечки равенства на отрезках (`<decoration type="1|2|3"/>`) — 1/2/3 черточки перпендикулярно середине, инжектируются как SVG `<line>`
- Позиции вершинных меток берутся напрямую из `labelOffset` GeoGebra (без эвристических переопределений)
- Направление дуги угла: `<angleStyle val="0">` = CCW (обычный), `val="1">` = CW — при CW меняются местами `ptFrom`/`ptTo` в JSXGraph
- Подписи вершин: `font-size=15`, `font-weight=bold` — крупнее и жирнее свободных текстов

**Интеграция:**
- `GeometryTaskEditor` — кнопка «Конвертировать в SVG» запускает `ggbXmlToSvg(xml)`, SVG-превью показывается под апплетом; кнопка «Сохранить SVG» сохраняет в БД и переключает `drawing_view='svg'`
- `TabDrawing` — новый режим «SVG (векторный)» в переключателе `drawing_view`; доступен только после конвертации
- `GeometryWorksheetPrint` — SVG-чертёж рендерится через `dangerouslySetInnerHTML` (масштабируется так же, как PNG)

**БД:** 4 миграции (1772000032–35) — добавлено поле `drawing_svg: TextField(max=200000)` в коллекцию `geometry_tasks`; добавлено в `LIGHT_FIELDS` и `TEXT_FIELDS` (копируется при дублировании задачи)

**Исправления:**
- `cropImage.js` — `ensureDataUrl()`: перед операциями с canvas внешний URL (PocketBase) конвертируется через `fetch → blob → FileReader.readAsDataURL`, устранена ошибка «Tainted canvas»
- `GeometryWorksheetPrint.css` — `background: none` на контейнере чертежа (убран белый фон, мешавший SVG)

### Марафон — CSS и трекер

- Убрано ограничение `max-width: 1280px` у корневого блока марафона (не обрезает широкие таблицы)
- `MarathonTracker`: CSS-рефакторинг стилей трекера v2; анимация `mgLivePulse` для live-индикатора

---

## [3.9.19] - 2026-05-07

### Геометрия — новые инструменты редактора и списка

- **Дублирование задачи** (`GeometryTaskList`): кнопка с иконкой `CopyOutlined` в табличном и карточном видах; `api.duplicateGeometryTask(id)` — полная копия записи (текстовые поля + файл чертежа fetch→re-upload, code + «-копия»)
- **Удаление фона** (`TabDrawing`): кнопка «Убрать фон» рядом с «Обрезать PNG»; flood-fill от 4 углов, tolerance=25 (консервативно для геометрических чертежей); результат передаётся через `onCropApplied`
- **Скрыта заглушка «нет чертежа»** в рабочем листе (`GeometryWorksheetPrint`): если у задачи нет изображения и `showDrawing=true` — блок чертежа не рендерится совсем

### ТДФ — исправления печати

- **Исправлено обрезание 4-го блока** при печати варианта: добавлен `STRETCH_SAFETY_PX = Math.ceil(2 * pxPerMm)` в расчёт высоты строк; убран `height: 100%` у `.tdf-table--stretch` (конфликтовал с явными высотами `<tr>` в print)
- **Дата в бланке** — заменена автоматическая дата на поле `Дата: ____________` для ручного заполнения

---

## [3.9.19] - 2026-05-03

### Улучшение пикселизации изображений

Новая система обработки изображений для пиксель-арта — заменяет старый `ImageUploader` на `TeamImageUploader` (командный) и подключается к обычному `PixelArtWorksheet`. Без изменений API и БД.

**Новые утилиты:**
- `utils/imageProcessing.js` — 4 режима бинаризации:
  - `Оригинал` — прежний алгоритм (box avg + глобальный порог)
  - `Силуэт` — растяжка контраста + авто-порог Otsu
  - `Контур` — оператор Собеля (рёбра объекта) + нормализация
  - `Контраст` — S-кривая, усиленный контраст
  - `autoCropFile()` — автоматическая обрезка по bounding box содержимого (порог 230, отступ 7%)
  - `computeOtsuFromFile()` — вычисление оптимального порога по методу Otsu
- `utils/matrixOps.js` — постобработка булевой матрицы:
  - `removeNoise` — удаление изолированных тёмных клеток (< 2 соседей)
  - `closeHoles` — закрытие белых дырок (≥ 6 тёмных соседей → тёмная)
  - `thicken` — дилатация на 4-связных соседей
  - `smooth` — сглаживание краёв (< 3 соседей → белая)
  - `invertMatrix` — инверсия

**Новый компонент `pixel-art-team/TeamImageUploader.jsx`:**
- Переключатель режимов (Оригинал / Силуэт / Контур / Контраст)
- Кнопка «Автообрезать» с возможностью отката
- Кнопка «Авто-порог» (Otsu, обновляет слайдер)
- Мини-превью всех 4 режимов для быстрого выбора
- Кнопки очистки матрицы (5 операций)
- Авто-пересчёт при изменении любого параметра (debounce 250 мс)

**Параметры командного пиксель-арта:**
- Дефолтная сетка: 48×48 → **60×60** (плитки 30×30, ~4 мм клетка)
- Максимум слайдера: ×40 → **×50** (до 100×100 для 2×2, ~2.4 мм)
- Подсказка под слайдером показывает расчётный размер клетки в мм

**Откат в обычном пиксель-арте:**
- `PixelArtWorksheet` переведён на `TeamImageUploader`
- `handleImageChange` упрощён — убран прямой вызов `processImage`
- Порог и размер сетки теперь авто-пересчитываются (ранее — кнопка «Пересчитать»)

## [3.9.16] - 2026-04-29

### Рабочий лист геометрии — режимы макета и стиль

**GeometryWorksheetPrint:**
- Добавлены режимы **3/A4**, **4/A4**, **5/A4** задач на лист (ранее только 2/A5); переключатель `Segmented` в панели настроек
- Поля 5 мм со всех сторон (вместо 3 мм сверху/снизу и 4 мм по бокам)
- Бейдж задачи упрощён: синий прямоугольник «Задача №N» → жирный текст «№N» без фона
- Исправлена надёжность `@page` при печати: правило инжектируется прямо перед `window.print()` — устранён конфликт со статическим `@page` из `TaskWorksheet.css`

### Шифровки — улучшения

**CryptogramGenerator:**
- Фильтр задач по контексту (`exam_type`): выпадающий список «Контекст», список тем фильтруется по нему
- KaTeX-рендеринг ответов в таблице-шифре
- Исправлен URL изображений задач: `task.image` → `api.getTaskImageUrl(task)` + проверка `has_image`

### Технические изменения

- `useVersionSync` переписан в `.jsx`: callback убран, уведомление об обновлении встроено в хук
- Версия обновлена до **3.9.16**

## [3.9.15] - 2026-04-27

### Маршрутный лист — редизайн UI и исправление ошибок

**Редизайн интерфейса:**
- `RouteSheetGenerator` переведён на `TrigGeneratorLayout`: двухпанельный layout (левая 320px — настройки и действия, правая — цепочка задач)
- Левая панель: секции «Параметры листа», «Добавить задачи», «Сохранение», «Печать» (появляется при наличии задач)
- Правая панель: `TrigPreviewPane` с пустым состоянием и `TrigStatBadge`-счётчиками
- Цветовые токены CSS (`var(--accent)`, `var(--ink-*)`, `var(--bg-*)`) вместо хардкода

**Редизайн печатного макета:**
- Полная переработка `RouteSheetPrintLayout.jsx` / `.css`
- Страница ученика: шапка с ФИО/Класс/Дата, пронумерованные карточки задач с полем ответа, цепочные коннекторы ▼ со стрелкой и подсказкой «используй ответ ①»
- Отдельная страница ключа учителя (`break-before: page`): бейдж «КЛЮЧ УЧИТЕЛЯ», компактная таблица ответов ① = 12 / ② = 4..., полная цепочка с подставленными значениями
- Подвал «© Lemma 2025–2026» на обеих страницах
- Scoped print CSS `body:has(.rs-print-root)` — не конфликтует с другими разделами

**Исправления:**
- Устранена ошибка `Component is not a function at updateForwardRef` при загрузке сохранённых листов — PocketBase-квирк: expand единственной задачи возвращает object, не array; добавлена нормализация в `useRouteSheet.loadFromSaved`
- Исправлена ошибка `400 Bad Request` при `sort=-created` — коллекция `route_sheets` не имела autodate-полей; миграция `1772000026` добавляет `created`/`updated`
- Исправлена ошибка `403` при сохранении — миграция `1772000025` устанавливает правильные access rules (`""`)
- Исправлена ошибка `500` в `/generate-chain` — `ReferenceError: topic is not defined`; переменная `topic` убрана из `console.log` в `raspberry/ollama-wrapper.js`

**MC-тесты — улучшение отображения результатов:**
- `StudentResultPage`: в разборе ошибок показывает текст выбранной опции вместо числового индекса
- `TeacherResultsDashboard`: загружает данные MC/Trig-теста для отображения текста опций

**Технические изменения:**
- Новые миграции: `1772000025_fix_route_sheets_rules.js`, `1772000026_add_autodate_to_route_sheets.js`
- `raspberry/ollama-wrapper.js`: удалена ссылка на несуществующую переменную `topic` в `/generate-chain`

## [3.9.14] - 2026-04-25

### Архитектура задач — поле exam_type и тригонометрические темы

**Новые возможности:**
- Фильтр **«Контекст»** в «Все задачи» — выбор ЕГЭ базовый / ЕГЭ профильный / ВПР / Тригонометрия / Устный счёт / Мордкович / Прочее; список тем в дропдауне фильтруется по выбранному контексту
- Добавлено поле `exam_part` в коллекцию `topics` — часть экзамена (1 или 2) для будущего профильного ЕГЭ
- В `exam_type` добавлены значения `ege_profile` и `trig`
- Созданы **7 тем** тригонометрических генераторов (`exam_type='trig'`): Вычисление выражений, Простейшие уравнения, Обратные функции, Двойной аргумент, Уравнения f(kx+b)=a, Формулы приведения, Формулы сложения
- При сохранении тригонометрического MC-теста задачи автоматически привязываются к нужной теме (`exam_type='trig'`)
- В каталоге тем (TopicTab) добавлены поля «Контекст» и «Часть экзамена» при создании/редактировании темы

**Технические изменения:**
- Новая миграция: `1772000023_add_ege_profile_trig_to_topics.js`
- `pocketbase.js`: новые методы `getTrigTopics()`, `getEgeProfileTopics()`; поддержка `exam_type` в `_buildTasksFilter`
- `ReferenceDataContext`: новые вычисляемые значения `trigTopics`, `egeProfileTopics`; кэш v2 → v3
- Исправлено отображение тем без `ege_number` во всех компонентах (было `№undefined`, стало просто название)

## [3.9.13] - 2026-04-25

### Тригонометрия — новый UI-паттерн генераторов и доработка шифровок

**Новые возможности:**
- Большинство тригонометрических генераторов переведены на единый современный интерфейс в стиле `TrigMixedGenerator`: общий заголовок, двухпанельный layout, компактные секции настроек слева и новый блок предпросмотра справа
- Добавлен общий UI-каркас `components/trig/TrigGeneratorLayout.jsx` для переиспользования в тригонометрических экранах
- Обновлены интерфейсы генераторов:
  - `TrigExpressionsGenerator`
  - `InverseTrigGenerator`
  - `TrigEquationsGenerator`
  - `TrigEquationsAdvancedGenerator`
  - `ReductionFormulasGenerator`
  - `AdditionFormulasGenerator`
  - `DoubleAngleGenerator`
  - `TrigValuesGenerator`
  - `UnitCircleGenerator`
  - `UnitCircleCryptogramGenerator`

**Шифровки:**
- В `CryptogramGenerator` повторяющиеся буквы теперь объясняются через номера позиций у самой задачи: одна задача может быть помечена как `8, 10`, если найденную букву нужно вписать сразу в две клетки
- Таблица `ответ → буква` снова не раскрывает позиции повторов ученику
- Экран обычных шифровок переведён на новый паттерн интерфейса с двухпанельным layout и обновлённым предпросмотром
- Внутренняя логика `buildCryptogramForVariant()` теперь хранит позиции каждой буквы в фразе для учительского ключа и UI-подсказок

**Технические изменения:**
- Новый файл: `ege-tasks/src/components/trig/TrigGeneratorLayout.jsx`
- `ege-tasks/src/utils/cryptogram.js`: добавлены `positions` для `answerKey` и реальных элементов шифровки
- `ege-tasks/src/components/CryptogramGenerator.css`: переработаны стили страницы шифровок и печатного блока

## [3.9.12] - 2026-04-25

### Тригонометрические MC-тесты — редактор и исправление рендеринга LaTeX

**Новые возможности:**
- **Редактор тригонометрических MC-тестов** (`TrigMCTestEditor`) — модальное окно с двумя вкладками:
  - «Редактор»: изменение названия, класса, числа вариантов ответа, порядка опций; просмотр задач по вариантам с редактированием каждой опции, переназначением правильного ответа, drag&drop перестановкой и перегенерацией вариантов ответов
  - «Выдача»: SessionPanel для выдачи теста ученикам (QR-код, ссылка, попытки)
- Кнопка «Редактировать» добавлена ко всем тестам на вкладке «Из генераторов» в разделе «Тесты с выбором»
- **MC-тесты из генераторов — поддержка формул сложения и формул приведения**: в `AdditionFormulasGenerator` и `ReductionFormulasGenerator` появилась кнопка «Тест с выбором»

**Исправления:**
- LaTeX-опции в ученическом MC-тесте (`StudentMCTestPage`) теперь корректно рендерятся: добавлена функция `wrapOptionText()`, оборачивающая сырые LaTeX-строки в `$...$`
- Задачи из тригонометрических генераторов сохраняются как реальные записи в коллекции `tasks` (поле `source: 'trig_generator'`, `topic` не обязателен), что позволяет ученикам видеть разбор ошибок в результатах

**Технические изменения:**
- Новый компонент `components/trig/TrigMCTestEditor.jsx`
- `MCTestGenerator.jsx`: импорт `TrigMCTestEditor`, state `editingId`, кнопка «Редактировать»
- `TrigMCSaveModal.jsx` полностью переписан: `createTasksAndBuildVariants()` создаёт реальные `tasks`-записи с rollback при ошибке, экспортирует `GENERATOR_LABELS`
- `useStudentSession.js`: `loadTrigMCVariantTasks` делегирует в `loadMCVariantTasks` (форматы унифицированы)
- `StudentApp.jsx`: маршрутизация упрощена — оба типа тестов обрабатывает `StudentMCTestPage`

## [3.9.11] - 2026-04-22

### Тригонометрические MC-тесты — выдача через ученический интерфейс

**Новые возможности:**
- Тесты из тригонометрических генераторов теперь можно выдавать ученикам
- В разделе «Тесты с выбором» → вкладка «Из генераторов» → кнопка «Выдать» раскрывает SessionPanel с QR-кодом и ссылкой
- Ученики проходят тест в новом интерфейсе `StudentTrigMCTestPage` (задания в LaTeX через KaTeX)
- Вопросы хранятся inline (`{question, answer, options}`) — без привязки к коллекции `tasks`
- Round-robin раздача вариантов, автосохранение ответов в localStorage
- Полная поддержка достижений (если включены в сессии)

**Технические изменения:**
- Новая миграция `1772000021_add_trig_mc_test_to_sessions.js` — поле `trig_mc_test: relation→trig_mc_tests` в `work_sessions`
- API: `createTrigMCTestSession(id)`, `getSessionsByTrigMCTest(id)` в `pocketbase.js`
- `SessionPanel` расширен: новый prop `trigMcTestId` (аналог `mcTestId`)
- `useStudentSession.js`: ветка `session.trig_mc_test` с `loadTrigMCVariantTasks()`
- `StudentApp.jsx`: маршрутизация на `StudentTrigMCTestPage` при `session.trig_mc_test`

## [3.9.10] - 2026-04-22

### MC-тесты — интеграция с разделом «Мои работы» и статистикой

**Раздел «Мои работы» теперь поддерживает оба типа:**
- Tabs внутри `WorkManager`: «Контрольные работы» / «Тесты с выбором»
- На вкладке MC: список тестов с метаданными (вариантов, задач, режим перемешивания, попыток), expand-карточка с подвкладками «Выдача» (`SessionPanel mcTestId={...}`) и «Результаты» (`TeacherResultsDashboard`)
- Кнопка «Открыть в редакторе» переходит в раздел «Тесты с выбором» с уже загруженным тестом (через новый prop `initialMcTestId`)

**Статистика учеников теперь корректно учитывает MC-тесты:**
- Расширен `expand` в API: `getAttemptsByStudentAllWithWorks`, `getAttemptsByDeviceAllWithWorks`, `getAttemptsForRegisteredStudents` теперь подгружают `session.mc_test`
- Fallback названия: `work?.title || mc_test?.title || 'Тест'` во всех местах отображения попыток
- Группировка работ (counts, dashboard, прогресс) учитывает `mc_test` как полноценную «работу» при подсчёте уникальных тестов

**Багфикс выдачи MC-тестов:**
- Поле `attempts.variant` (relation) сделано optional, добавлено числовое поле `mc_variant`
- MC-попытки создаются с `mc_variant: number` вместо строки в relation-поле
- Чтение варианта: `attempt.mc_variant ?? attempt.variant` (fallback для legacy записей)
- Миграция: `1772000019_add_mc_variant_to_attempts.js`

**Файлы:**
- API: `shared/services/pocketbase.js` — expand с `session.mc_test`
- Компоненты: `WorkManager.jsx` (Tabs + MC tab), `MCTestGenerator.jsx` (`initialMcTestId` prop), `App.jsx` (`selectedMcTestId` state)
- Статистика: `StudentDetailPage.jsx`, `StudentProgressDashboard.jsx`, `student/StudentProgressPage.jsx`
- Хук: `useStudentSession.js` (mc_variant)

## [3.9.9] - 2026-04-22

### Новый раздел: Тесты с выбором ответов (MCTestGenerator)

Полноценный генератор тестов формата A/B/C/D с авто-генерацией дистракторов и интеграцией в систему выдачи учеников.

**Возможности:**
- Подбор задач по одной теме или из нескольких (multi-topic)
- Авто-генерация дистракторов (`distractorGenerator.js`): числовые / дробные / текстовые стратегии; учитель может править вручную
- Два режима порядка ответов: **зафиксированный** учителем или **перемешивание у каждого студента** (детерминированный seed = hash(attemptId+taskId), стабильный при перезагрузке)
- Несколько вариантов в одном тесте (по умолчанию 1–2, расширяется)
- Радио-кнопки в интерфейсе ученика (`StudentMCTestPage`), автосохранение в localStorage
- Печатная вёрстка `MCTestPrintLayout` в стиле тригонометрических генераторов (компактная двухколоночная сетка опций, таблица ответов внизу, страница-ключ учителя)

**База данных:**
- Новая коллекция `mc_tests`: `{title, description, class_number, topics, options_count(2-8), shuffle_mode("fixed"|"per_student"), variants(json)}`
- Поле `work_sessions.mc_test` (relation→mc_tests, optional) — для выдачи MC-теста через знакомый QR/ссылка-механизм
- `work_sessions.work` сделано **опциональным** (раньше required) — позволяет создавать сессии и для классических работ, и для MC-тестов
- `attempt.variant` для MC хранит номер варианта строкой ("1"/"2") вместо relation (нет separate variants record)
- `attempt_answers.answer_raw` хранит индекс опции ("0"/"1"/"2"/"3")
- Миграции: `1772000017_create_mc_tests.js`, `1772000018_make_work_optional_in_sessions.js`

**Архитектурные решения:**
- Переиспользование инфраструктуры `work_sessions` / `attempts` / `attempt_answers` / `achievements` — MC-тесты автоматически получают QR-выдачу, систему достижений и dashboard учителя
- Синтетический variant id `mc-N` (без записи в БД), tasks обогащаются `mc_options` на лету в `useStudentSession.loadMCVariantTasks`
- Round-robin выбор варианта по min-count из существующих attempts
- В `StudentApp.jsx` диспатч: `session.mc_test` → `<StudentMCTestPage>`, иначе → `<StudentTestPage>`

**Файлы:**
- Компоненты: `MCTestGenerator.jsx`, `mc-test/MCOptionsEditor.jsx`, `mc-test/MCTestPrintLayout.jsx` (+CSS)
- Хук: `useMCTest.js`
- Утилита: `distractorGenerator.js`
- Студенческий интерфейс: `student/StudentMCTestPage.jsx`
- API: 7 новых методов (`getMCTests`, `getMCTest`, `createMCTest`, `updateMCTest`, `deleteMCTest`, `getTasksByIds`, `createMCTestSession`, `getSessionsByMCTest`)

### Деплой
- Студенческое приложение задеплоено на VPS (`student.oipav.ru`)
- Учительский фронтенд задеплоен на Raspberry Pi (`l.oipav.ru`)

## [3.9.8] - 2026-04-20

### Удалён tldraw

- `TldrawSection.jsx` и пакет `@tldraw/tldraw` удалены — требует коммерческую лицензию для production-деплоя
- В меню «Лаборатория» остался только Excalidraw
- Билд уменьшился на ~1.7MB (chunk `TldrawSection` исчез)

### Инфраструктура: деплой на Raspberry Pi 4

- Приложение задеплоено на домашний сервер: `https://l.oipav.ru/`
- nginx в Docker: порт 80→443 редирект, TLS (Let's Encrypt), Basic Auth, gzip (73% сжатие JS)
- Nginx Proxy Manager удалён — заменён plain nginx
- Автообновление сертификата: cron каждый понедельник в 3:00

## [3.9.7] - 2026-04-18

### Новый раздел: Смешанная работа (TrigMixedGenerator)

Агрегатор всех тригонометрических генераторов — позволяет собрать в одном листе задания из разных разделов тригонометрии.

**Архитектура:**
- `GENERATOR_REGISTRY` — реестр генераторов (расширяемый: добавить новый = одна запись в массиве)
- `INITIAL_GEN_CONFIGS` — начальные настройки для каждого генератора
- `hookMap` — маппинг id → хук; все хуки инстанциируются безусловно
- Drag & drop порядок разделов (HTML5 native, без библиотек)
- Единая печатная вёрстка `TrigMixedPrintLayout` с `body:has(.tmixed-print-root)` scoped print

**Поддерживаемые генераторы (8):**
тригонометрические выражения, обратные функции, формулы приведения, формулы сложения, простейшие уравнения, единичная окружность, формулы двойного аргумента, уравнения f(kx+b)=a

**Настройки печати:** 2 колонки (compactMode), 2 варианта на страницу, место для решения, лист ответов учителя, заголовки разделов

### Новый генератор: Формулы двойного аргумента

**`DoubleAngleGenerator.jsx`** + **`useDoubleAngle.js`**

**Три типа задач:**
- **Числовое вычисление** — `2sin35°cos35° = ?` → `1/2`; `cos²45° − sin²45° = ?` → `0`; полуугла: 15°, 30°, 45°, 60°, 75°, 90°, 105°
- **Символьное упрощение** — `2sin3x·cos3x = ?` → `sin6x`; произвольные переменные (α, x, t, φ, β) и коэффициенты 1–8
- **Распознавание формул** — `(cosα + sinα)² = ?` → `1+sin2α`; `sin²α = ?` → `(1−cos2α)/2`; пул из 10 идентичностей

Функции: sin, cos, tg. Настраиваемые типы задач, формулы cos2α, количество вариантов и задач.

### Новый генератор: Уравнения f(kx+b) = a

**`TrigEquationsAdvancedGenerator.jsx`** + **`useTrigEquationsAdvanced.js`**

**Два типа задач:**
- **Тип 1: f(kx) = a** — уравнения с линейным аргументом: sin2x=√2/2, cos(x/2)=−1/2, tg(x/3)=√3/3, ctg2x=1, sin(x/4)=√3/2 и т.д. (~40 уравнений); sin, cos, tg, ctg с k=2,3,4,1/2,1/3,1/4; отрицательные аргументы sin(−2x), tg(−4x)
- **Тип 2: A·f(kx+b) = c** — уравнения со сдвигом: 2cos(x/2−π/6)=√3, 2sin(3x−π/4)=−√2 и т.д. (~20 уравнений из Мордкович §18)

Каждое уравнение снабжено точным ответом в форме общей формулы.

### override-паттерн в generate()

Добавлен параметр `override` во все 8 тригонометрических хуков (useTrigExpressions, useInverseTrig, useReductionFormulas, useAdditionFormulas, useTrigEquations, useUnitCircle, useDoubleAngle, useTrigEquationsAdvanced). Позволяет агрегатору передавать настройки напрямую, минуя React async state batching:
```js
generate(override) { const s = override ? { ...settings, ...override } : settings; ... }
```

### Улучшения TrigMixedPrintLayout

- Единичная окружность: точки расположены **строчкой** (flex-wrap) вместо столбика — экономия места
- Inline-режим (выражения, приведения, сложение): убрано подчёркивание после знака `=` — пустое место без линии
- TwoLine-режим (уравнения): убрана строка `t = ___` — ученик сам определяет место для ответа

---

## [3.9.6] - 2026-04-17

### Новый генератор: Формулы сложения

**`AdditionFormulasGenerator.jsx`** + **`useAdditionFormulas.js`**

**Три типа задач:**
- **Числовое распознавание** — распознать формулу в выражении и вычислить стандартное значение: `sin12°cos78° + cos12°sin78° = ?` → `1`; `tg22°+tg8°)/(1−tg22°·tg8°) = ?` → `1/√3`
- **Символьное упрощение** — переменные, случайные коэффициенты: `sin5α cos3α − cos5α sin3α = ?` → `sin2α`; формулы sin/cos суммы и разности
- **Нестандартные углы** — вычислить точное значение: `sin75°(=45°+30°) = ?` → `(√6+√2)/4`; пул из 15 задач (75°, 15°, 105°, 165°; sin, cos, tg)

**Настройки:** типы задач (чекбоксы), функции (sin/cos/tg), операция (сумма/разность), кол-во вариантов и задач, лист ответов.

---

## [3.9.5] - 2026-04-17

### Новый генератор: Формулы приведения

**`ReductionFormulasGenerator.jsx`** + **`useReductionFormulas.js`** — генератор листов по формулам приведения.

**Три типа задач:**
- **Формулы приведения** — упростить `sin(π/2 + α)`, `cos(270° − α)` и т.д. (радианы/градусы/оба)
- **Перевёрнутый аргумент** — упростить `cos(α − π/2)`, `sin(α − 3π/2)` и т.д. (на порядок сложнее: студент должен помнить о знаке для нечётных функций)
- **Числовые выражения** — найти значение: `14√3·cos 750°`, `4√3·tg(−330°)`, `7{,}5·ctg 38°·ctg 52°`; ответ всегда целое или конечная десятичная дробь

**Числовые паттерны (независимо включаются):**
- `a√3 · f(угол)` — коэффициент √3 × значение ±√3/2, ±√3 или ±1/√3 → рациональный ответ
- `a√2 · f(угол)` — коэффициент √2 × значение ±√2/2 → рациональный ответ
- `f(A°) · f(90°−A°)` — произведение ctg(A°)·ctg(90°−A°) или tg(A°)·tg(90°−A°) = 1; нестандартные углы типа 38°, 52°

**Настройки:** типы задач (чекбоксы), единицы угла для символьных задач, функции (sin/cos/tg/ctg), кол-во вариантов (1–8), задач в варианте (4–20), числовые паттерны, 2 варианта на стр., лист ответов.

**Архитектура:** переиспользует `TrigExprPrintLayout.jsx` (questionMode='inline'); инструкция автоматически подбирается по включённым типам.

---

## [3.9.4] - 2026-04-16

### Улучшения рабочих листов и интерфейса

**WorksheetGridPrint — расширенные настройки клетчатого листа:**
- Новый режим «задача поверх клетки» (`overlayGrid`): сетка заполняет весь блок, условие отображается поверх неё — экономит место, ученик решает прямо в листе
- Переключатель «Клетка под задачей» — можно отключить сетку совсем (лист без поля для решения)
- Поддержка картинок задач (`has_image`) — изображение выводится вместе с условием
- Расширен список вариантов высоты поля: 12/16/22/30/40/55 мм (с подсказкой «~N задач/А4»)
- Улучшен Form layout: `Row`/`Col` вместо `Space`, корректное выравнивание на разных экранах
- Печатный блок рендерится только во время печати (`isPrinting`) — нет лишнего DOM в обычном режиме
- Default высота поля: 18 → 22 мм

**FormatSettings — реорганизация секций:**
- Разделитель «Текст / Страница» — логическое разбиение настроек
- «Скрыть типовые фразы» и «Название варианта» перемещены в верхнюю группу «Текст»

**ActionButtons — улучшенный выбор метода PDF:**
- Метод PDF (Высокое качество / Стандартный) вынесен в Dropdown-меню с галочкой активного метода
- Метка «недоступно» для Puppeteer если сервис не отвечает

**OralWorksheetGenerator + TestWorkGenerator — косметика UI:**
- Иконки Ant Design вместо эмодзи в заголовках вкладок (FilterOutlined, TagsOutlined, BarChartOutlined и др.)
- В TestWorkGenerator убран вводящий в заблуждение Alert-баннер и Timeline блоков

**filterTaskText — расширен список типовых фраз:**
- Добавлены «Упростите выражение» и «Упростите выражение: »

---

## [3.9.3] - 2026-04-13

### Тригонометрия — три новых генератора

**Новый раздел «Вычисление выражений»** (`TrigExpressionsGenerator` + `useTrigExpressions`):
- Суммы, разности, произведения из sin/cos/tg/ctg
- Углы из диапазона ±2π (33 стандартных угла)
- Алгоритм подбора «красивых» результатов: до 400 попыток, пул 40+ предвычисленных значений (0, ±1, ±1/2, ±√2/2, ±√3/2, ±√3 и др.)
- Настройки: тип выражений (sum/product/mixed), количество слагаемых/множителей (2–5), функции, отрицательные углы

**Новый раздел «Обратные функции»** (`InverseTrigGenerator` + `useInverseTrig`):
- Простые значения: arcsin, arccos, arctg, arcctg от табличных аргументов
- Суммы/разности обратных функций
- Вложенные: sin(arccos(a)), cos(arctan(a)) и т.д.
- Смешанный режим: все три типа в одном варианте

**Новый раздел «Уравнения»** (`TrigEquationsGenerator` + `useTrigEquations`):
- Простейшие уравнения: sin t = a, cos t = a, tg t = a, ctg t = a
- Ответы — готовые общие формулы (например, `t = (-1)^n π/6 + πn`)
- Без поля для ответа на печати (`questionMode='plain'`) — места достаточно

**Общая инфраструктура печати** (`TrigExprPrintLayout.jsx` / `.css`):
- Переиспользуется всеми тремя генераторами
- Props: `instruction` (текст задания), `questionMode` ('inline' | 'twoLine' | 'plain')
- Шапка варианта в одну строку: Вариант N | ФИО ___ | Класс ___ | Дата ___
- Страница ответов учителя в 2 колонки с ответами красным цветом
- Scoped print CSS: `body:has(.texpr-print-root)`

---

## [3.9.2] - 2026-04-11

### Шифровки — новый инструмент для уроков

**Новый раздел «Шифровки»** в меню учителя:

- **`CryptogramGenerator.jsx`** — главный компонент: левая панель (название, фраза, задачи) + правая панель (превью таблицы-шифра и строки ответа)
- **`CryptogramGenerator.css`** — стили для экрана и печати (A5 portrait, двухколоночный список задач)
- **`utils/cryptogram.js`** — логика шифровки: `buildCryptogramForVariant`, `getCryptogramLetterCount`, `getCryptogramUniqueLetterCount`, `normalizeCryptogramPhrase`

**Ключевая логика:**
- Количество задач = число **уникальных** букв в фразе (одна задача → одна буква, повторения используют тот же ответ)
- Над клетками строки ответа — **порядковый номер позиции** (1, 2, 3…), не ответ — ученик не видит слово, пока не решит
- Обманки — только целые числа и конечные десятичные дроби (без LaTeX-дробей и корней)
- Пробелы между словами создают наглядные разрывы в строке ответа

**Подбор задач:**
- **Автогенератор** — выбор темы и подтемы, кнопка «Сгенерировать (N)» подтягивает задачи с ответами из каталога
- **Ручной выбор** — через `TaskSelectModal` с возможностью переупорядочить и удалить

**Параметры:**
- Переключатель «Срезать «Вычислите», «Найдите» и т.д.» (использует `filterTaskText`, включён по умолчанию)
- Фраза «Найдите частное от деления» добавлена в `filterTaskText.js`

**Печать (A5 portrait):**
- Инструкция ученику: «Найди свой ответ в таблице — запиши соответствующую букву»
- Страница-ключ учителя (после page-break) с правильной фразой
- Scoped print CSS через `body:has(.cgp-root)`

---

## [3.9.1] - 2026-04-06

### Марафон — полировка после запуска

**Карточки учеников (MarathonCardsPrint):**
- Переработан дизайн: **6 карточек на A4** (сетка 2×3) вместо 4
- Новая вёрстка: логотип Lemma + круг с номером + горизонтальный разделитель + условие задачи
- Убран подвал «Задача № N · Ученик: ___»

**Лист учителя (MarathonTeacherSheet):**
- Убрана фраза «Документ конфиденциальный. Не раздавать ученикам.»
- Таблица: **2 столбца** — «Условие и ответ» (1/3) + «Решение / Подсказка» (2/3); старые столбцы №/Сложность/Ответ удалены
- Номер задачи и ответ теперь внутри ячейки условия
- Задачи не разрываются при переносе страниц (`break-inside: avoid`)
- Поля на всех страницах одинаковые (`@page { margin: 10mm 12mm }`)

**Рейтинговый бланк (MarathonRatingPrint):**
- **Горизонтальная печать** (A4 landscape)
- **Двухстрочная шапка**: номер задачи (colspan=2) → «попытки» + «очки» под каждой задачей
- **Поле для очков** — отдельный узкий столбец рядом с кружками (○ ○ ○)
- **Цветная легенда** как в цифровом трекере: +3 зелёный / +2 синий / +1 оранжевый / 0 серый

**Система очков (MarathonTracker + MarathonRatingPrint):**
- Решено с 1-й попытки → **+3**
- Решено со 2-й попытки → **+2**
- Решено с 3-й попытки → **+1**
- Три неудачи → **0** (было −1, убрали штраф)
- Ячейка отображает числовой балл вместо ✓/✗

**Исправление печати (было: пустые листы):**
- Убран React Portal; print-блоки рендерятся inline
- CSS-изоляция через `body:has(.marathon-*-print-root) { visibility: hidden }` — паттерн из QRWorksheetGenerator
- `window.print()` вызывается через 300ms после рендера (загрузка логотипа и KaTeX-шрифтов)
- `@page` инжектируется под режим: `portrait` для карточек/учителя, `landscape` для рейтинга

---

## [3.9.0] - 2026-04-06

### Марафон — формат устной защиты задач

**Новый раздел «Марафон»** в меню учителя:

- **`MarathonGenerator.jsx`** — главный компонент, 4 вкладки: Настройка / Карточки учеников / Лист учителя / Рейтинг. Сохранение/загрузка марафонов в БД, кнопки «Новый» / «Сохранить» / «Загрузить».
- **`marathon/MarathonCardsPrint.jsx`** — печать карточек учеников с условием задачи, цветная шапка по сложности
- **`marathon/MarathonTeacherSheet.jsx`** — лист учителя с решениями
- **`marathon/MarathonRatingPrint.jsx`** — бумажный бланк рейтинга: строки = ученики, столбцы = задачи
- **`marathon/MarathonTracker.jsx`** — цифровой интерактивный трекер с автосохранением в БД

**Хук `useMarathon`:**
- Состояние: title, classNumber, tasks (полные объекты), students (имена), trackingData, savedId
- Методы: addTasks, removeTask, moveTask (порядок = сложность), addStudent, removeStudent, saveMarathon (create/update), saveTracking (частичное обновление), loadMarathon, loadSavedList, deleteMarathon, reset, initTracking
- `tracking_data` структура: `{ "Иванов Иван": { "0": { attempts: 0, solved: false, failed: false }, ... } }`

**База данных:** новая коллекция `marathons` (миграция `1772000014`); поля: title, class_number, tasks(relation[]→tasks), task_order(json), students(json), tracking_data(json). Superuser only. Применена на VPS.

**5 API методов:** getMarathons, getMarathon(id), createMarathon, updateMarathon, deleteMarathon.

---

## [3.8.4] - 2026-04-05

### Тригонометрия — генератор листов «Единичная окружность»

**Новый раздел «Тригонометрия»** в меню учителя:

- **`UnitCircleGenerator.jsx`** — главный компонент-генератор: панель настроек, превью вариантов, кнопки «Сгенерировать / Печать / Сохранить / Загрузить / Сброс»
- **`trig/UnitCircleSVG.jsx`** — SVG-окружность: оси со стрелками, засечки на 16 стандартных позициях, нумерованные точки, KaTeX-подписи углов через `<foreignObject>`
- **`trig/UnitCirclePrintLayout.jsx`** + **`UnitCirclePrintLayout.css`** — печатная вёрстка A4: страницы учеников + страницы ответов учителя; scoped print CSS

**Два типа заданий:**
- **Прямая задача** — точки отмечены на окружности, ученик подписывает углы (сетка ответов с пустыми строками)
- **Обратная задача** — список углов задан, ученик отмечает точки на пустой окружности
- **Смешанная** — чередование прямой/обратной в рамках одного варианта

**Стандартные углы:** 16 позиций (кратные π/6, π/4, π/3, π/2). Смещение ±k×2π (k до ±4), что даёт углы вида 13π/6, −11π/6, 25π/6 и т.д.

**Настройки:**
- Количество вариантов (1–30)
- Кружков на странице A4: 2 (один под другим) или 4 (сетка 2×2)
- Точек на кружок (4–12)
- Максимальное смещение k×2π (0–4)
- Подписи на окружности: ничего / только оси / все 16 позиций
- Показывать градусы (0°, 90°, 180°, 270°)
- Засечки на 16 стандартных позициях
- Лист ответов учителя

**LaTeX-дроби:** все углы рендерятся через KaTeX — `\dfrac{13\pi}{6}`, `-\dfrac{11\pi}{6}` и т.д. В SVG через `<foreignObject>`, в HTML через `dangerouslySetInnerHTML`.

**База данных:** новая коллекция `unit_circle_worksheets` (миграция `1772000012`); поля: title, task_type, variants_count, settings(json), tasks_data(json). Superuser only.

**Хук `useUnitCircle`:** генерация вариантов, управление настройками, CRUD (getUnitCircleWorksheets / createUnitCircleWorksheet / updateUnitCircleWorksheet / deleteUnitCircleWorksheet).

---

## [3.8.3] - 2026-04-01

### Пиксель-арт — сохранение/загрузка, двухколоночный макет, исправления печати

**Сохранение и загрузка:**
- Новая коллекция БД `pixel_art_worksheets` (миграция `1772000010`)
- Кнопки «Сохранить» / «Загрузить» в карточке параметров печати
- При повторном сохранении кнопка меняется на «Обновить» — обновляет существующую запись без дубля
- Модал загрузки показывает список сохранённых листов с количеством задач, размером сетки и датой
- Восстанавливаются: задачи, матрица, параметры сетки, порог, настройки печати, заголовок
- 4 API метода: `getPixelArtWorksheets`, `createPixelArtWorksheet`, `updatePixelArtWorksheet`, `deletePixelArtWorksheet`

**Двухколоночный макет задач:**
- Переключатель «Задачи в две колонки» в параметрах печати
- При включении задачи выводятся в сетку 2 колонки (как в QR-листах)
- Поле раскраски выровнено по центру страницы (`margin: 0 auto`)

**Исправление печати (4 страницы → 1):**
- Печатный блок перенесён за пределы превью-контейнера (паттерн как в QRWorksheetGenerator)
- `position: fixed` в `@media print` + `body:has(.pixel-art-print-root)` — scoped print CSS

### Главная страница ученика — личный кабинет

**Новый компонент `StudentHomeLanding`** (для URL `/student/` без sessionId):
- Поле ввода кода сессии с кнопкой перехода
- Логотип Лемма (полупрозрачный, 26px)
- Секция авторизации:
  - Не вошёл: кнопки «Войти» и «Зарегистрироваться» (открывают полный экран `StudentAuthPage`)
  - Вошёл: приветствие с именем + кнопки «Мой прогресс» / «Мои достижения» + «Выйти»
- Тёмная тема поддержана

### Исправление деплоя — логотип на ученическом фронте

- `deploy-student-to-vps.sh`: добавлено копирование `lemma-logo-new.png` и `icon-new.png` в `/var/www/student-ege/`
- Ранее логотип не отображался на странице `/student/` на VPS

---

## [3.8.2] - 2026-04-01

### Ученический интерфейс — светлая и тёмная тема

**Переключатель тем:**
- Кнопка 🌙/☀️ в верхней навигационной панели и на главной странице ввода кода сессии
- Тема сохраняется в `localStorage` (`student-theme`), восстанавливается при следующем открытии
- По умолчанию — **светлая тема**
- Ant Design компоненты переключаются через `theme.darkAlgorithm` / `theme.defaultAlgorithm`

**Тёмная тема покрывает все экраны:**
- Главная, авторизация, стартовый экран, тест, результаты, достижения, галерея, прогресс, скелетон
- Фон: тёмно-синий `#0d0d1a → #0f0e17`; карточки: `rgba(26,26,46)` с полупрозрачной рамкой
- Инпуты, кнопки, шкалы, бейджи достижений — все адаптированы

**Inter-шрифт:**
- Подключён через Google Fonts в `student.html` — тот же шрифт, что на лендинге

**Унификация кнопок верхней панели:**
- Все кнопки топбара (назад, прогресс, достижения, тема, выход) — нативные `<button>` с классом `student-theme-toggle`
- Единый стиль: скруглённый квадрат 36×36px, backdrop-blur, тень, плавный hover
- Кнопка «Назад» чуть шире — с текстовым лейблом рядом с иконкой
- Кнопка «Достижения» (`TrophyOutlined`) — золотой акцент `#faad14`

### Новый логотип и иконка

- `lemma-logo.png` → `lemma-logo-new.png`, `icon.png` → `icon-new.png`
- Обновлено во всех точках: `index.html`, `student.html`, `landing.html`, `App.jsx`, `LandingPage.jsx`, `LandingPageV2.jsx`, `HowItWorksPage.jsx`, `og:image`
- Старые файлы удалены из `public/`

### ТДФ — улучшения печати

**Книжная ориентация:**
- Переключатель «Альбомная / Книжная» в панели управления `TDFPrintView`
- Книжный режим: шрифт 9pt, компактные отступы, чертёж max-height 65px, ширина страницы 210mm
- `@page` инжектируется динамически перед `window.print()` — не конфликтует с другими разделами

**Исправления:**
- Исправлен отступ сверху на второй и последующих страницах — `padding` перенесён из `.tdf-print-page` в `@page { margin }` (теперь применяется к каждой странице)
- Поля печати уменьшены: `7mm 8mm 6mm` (было `12mm 12mm 10mm`)

### Исправление ошибки кадрирования в ТДФ

- Ошибка «tainted canvas» при попытке кадрировать изображение существующего пункта ТДФ
- Причина: `drawingDataUrl` устанавливался как remote PocketBase URL → canvas тинтовался
- Исправление: при загрузке существующего пункта remote URL конвертируется через `fetch → blob → FileReader.readAsDataURL` в data URL

---

## [3.8.1] - 2026-03-30

### Улучшения интерфейса

**ТДФ — кадрирование чертежей:**
- В `TDFItemModal` на вкладке «Чертёж» добавлена кнопка **«Кадрировать»**
- Открывает `CropModal` — тот же компонент, что используется в геометрических задачах
- Доступна для изображений, загруженных из файла или нарисованных в GeoGebra
- 4 слайдера полей (слева/справа/сверху/снизу) с визуальным превью зоны обрезки

**Результаты учеников — порядковый номер задачи:**
- В `TeacherResultsDashboard` при просмотре ответов добавлен **бейдж с порядковым номером** задачи внутри варианта
- Определяется из поля `order` варианта (`[{taskId, position}]`), fallback — индекс в массиве `tasks`
- Не требует дополнительных запросов к API

---

## [3.8.0] - 2026-03-28

### QR-листы — новый инструмент для уроков

Полностью новый инструмент генерации обучающих листов с QR-кодом. Ученик решает задачи, находит числа-ответы в таблице и закрашивает клетки — из них складывается QR-код, который отсканировать = следующее задание.

**Компоненты:**
- **QRWorksheetGenerator** — главный компонент (настройки, управление задачами, предпросмотр, действия)
- **QRTaskPanel** — панель выбора задач: TaskSelectModal, поля ответов (InputNumber), кнопки редактирования/удаления/перемещения
- **QRGridPreview** — экранный превью сетки с режимами «ученик» / «учитель» (подсветка правильных клеток)
- **QRPrintLayout** — печатная A4-вёрстка: страница ученика + опциональная страница-ключ учителя (ответы + QR-код + подсвеченная сетка)

**Утилиты:**
- `qrMatrix.js` — генерация QR-матрицы из URL через библиотеку `qrcode`, оценка размера `estimateQRSize`
- `qrGridFill.js` — заполнение числовой сетки: правильные ячейки = модули QR, остальные — случайные числа из диапазона ответов
- `useQRWorksheet.js` — хук управления состоянием (tasks, customAnswers, qrUrl, matrix, grid, mode, title, generate, reset, loadFromSaved, updateTask)

**Сохранение и загрузка:**
- Новая коллекция `qr_worksheets` в PocketBase (title, qr_url, tasks relation, custom_answers JSON, grid JSON, qr_size)
- Кнопки «Сохранить» и «Загрузить» — список сохранённых листов с возможностью удаления
- Загрузка восстанавливает полный порядок задач из `expand.tasks`

**Интеграция с «Моими работами»:**
- Выпадающий список работ (ленивая загрузка при первом открытии)
- Выбор работы → автоматически создаётся/получается сессия → подставляется URL для студента

**Сокращение URL (clck.ru):**
- Кнопка «Сократить» рядом с полем URL
- Использует открытый API `https://clck.ru/--?url=...` (CORS разрешён, без прокси)
- Кнопка отключается, если URL уже сокращён (содержит `clck.ru`)

**Редактирование задач прямо в листе:**
- Кнопка-карандаш в каждой карточке задачи → открывается `TaskEditModal`
- Изменения сохраняются в БД и обновляются локально без перезагрузки

**Печать:**
- Уменьшенные поля (10mm вместо 15mm/20mm)
- Убраны серые разделители между задачами
- Картинки к задачам: `float: right` с обтеканием текстом (maxWidth 38%/42%, maxHeight 55mm/40mm)
- Режим двух колонок (Switch «2 колонки») для листов с картинками
- Страница-ключ учителя: список ответов + QR-код + подсвеченная сетка

**База данных:** `pocketbase/pb_migrations/1772000009_create_qr_worksheets.js`

**API (4 метода):** `getQrWorksheets`, `createQrWorksheet`, `updateQrWorksheet`, `deleteQrWorksheet`

---

## [3.7.9] - 2026-03-28

### Деплой лендинга на VPS

- Лендинг доступен по адресу **https://lemma.oipav.ru**
- Настроен автоматический деплой через GitHub Actions (`.github/workflows/deploy-landing.yml`): сборка Vite → rename `landing.html` → `index.html` → rsync на VPS в `/var/www/landings/lemma/`
- Деплой запускается автоматически при каждом push в ветку `main`, а также вручную через `workflow_dispatch`

## [3.7.9] - 2026-03-27

### Генератор вариантов ЕГЭ — КИМ-формат (EgeVariantGenerator)

Полностью переработана печать в стиле КИМ.

**Архитектура рендеринга:**
- Переход на **A5-страницы** (`@page { size: A5 portrait; margin: 0; }`) вместо A4-landscape-разворотов. Каждая страница — самостоятельный блок 148.5mm × 210mm, CSS прост и предсказуем.
- Для A4-буклета: выбрать «Страниц на листе: 2» в диалоге печати Chrome/Safari.
- `handleKimPrint` теперь инжектирует `@page { size: A5 portrait; margin: 0; }` (был A4 landscape) — Chrome корректно распознаёт формат в диалоге печати.
- `@page` вынесен на верхний уровень CSS (не внутри `@media print`) — обязательное условие для распознавания Chrome.

**Измерение высот задач:**
- Удалена проблемная функция `measureTaskHeights` (использовала `createRoot + flushSync` в оторванном от DOM элементе — CSS-правила не применялись, таблицы измерялись неверно).
- Реализован **двухфазный рендер** через `useLayoutEffect`:
  1. Фаза measure: все задачи рендерятся в скрытый контейнер **внутри React-дерева** (CSS применяется корректно — таблицы, KaTeX, изображения).
  2. `useLayoutEffect` синхронно читает `offsetHeight` каждой задачи, вычисляет пагинацию.
  3. Фаза render: отображает список A5-страниц.
- Измерительный контейнер: `width: 132.5mm` (= 148.5mm A5 − 2×8mm padding).
- Константы высот пересчитаны для A5: 180mm (обычная страница), 159mm (первая — с рамкой-примечанием).

**Удалено:** `.kim-spread`, `KimBlankPage`, `waitForImage`, `createRoot/flushSync` импорты.

## [3.7.9] - 2026-03-26

### Генератор вариантов ЕГЭ базового уровня (EgeVariantGenerator) — начальная версия

### Модуль ТДФ — Теоремы, Определения, Формулы (новый раздел Геометрии)

Полностью новый обучающий модуль для работы с теоремами, определениями и формулами. Интегрирован в раздел «Геометрия».

**Компоненты:**
- **TDFManager** — список наборов ТДФ с фильтром по классу, CRUD, переходы к редактору и вариантам
- **TDFEditor** — редактор конспекта: список пунктов с drag&drop, секции-заголовки, вставка/редактирование пунктов, кнопка печати эталонного конспекта
- **TDFItemModal** — модал создания/редактирования пункта: тип, название, вопрос (MD+LaTeX), формулировка (MD+LaTeX), краткая запись, чертёж (PNG-загрузка или GeoGebra)
- **TDFVariantBuilder** — конструктор вариантов опросников: выбор пунктов, нумерация, печать бланков
- **TDFPrintView** — печатный вид A4 landscape, два режима: `etalon` (полный конспект) и `blank` (бланк для ученика с пустыми линиями)

**Типы пунктов** (7 типов с цветовой маркировкой):
Теорема · Определение · Формула · Аксиома · Свойство · Признак · **Следствие** (новый)

**База данных** (3 новые коллекции):
- `tdf_sets` — наборы ТДФ (title, description, class_number)
- `tdf_items` — пункты (type select, name, question_md, formulation_md, short_notation_md, drawing_image file, geogebra_base64)
- `tdf_variants` — варианты опросников (number, title, item_ids JSON)

**API** (13 методов): `getTdfSets/Set/createTdfSet/updateTdfSet/deleteTdfSet`, аналогично для `Items` и `Variants`, плюс `getTdfItemDrawingUrl`

**Навигация**: 3 view в App.jsx — `tdf`, `tdf-editor`, `tdf-variants`

### ТДФ — Реструктуризация печатного вида

- **Объединение столбцов «Тема» и «Формулировка»**: в одной ячейке — название темы жирным шрифтом, формулировка ниже обычным (с пунктирным разделителем)
- **Столбец «Вопрос / задание» сдвинут правее**: теперь идёт после объединённого столбца (перед «Чертёж»), а не сразу после «Тема»
- **Метка типа в ячейке номера**: «Теорема», «Признак» и т.д. отображаются вертикально снизу от цифры (`writing-mode: vertical-rl`)
- Итого 5 колонок вместо 6: №, Тема/Формулировка, Вопрос/задание, Чертёж, Краткая запись

### ТДФ — Новый тип «Следствие»

- Тип `corollary` добавлен в `TDFItemModal`, `TDFEditor`, `TDFManager`, `TDFVariantBuilder`, `TDFPrintView`
- Цвет: `gold` (Ant Design tag)
- Обновлена схема БД: поле `type` в коллекции `tdf_items` — добавлено значение `corollary`

## [3.7.8] - 2026-03-18

### Экспорт в Markdown (Obsidian)
- **Кнопка «Экспорт MD»** добавлена в три раздела: «Устный счёт» (OralWorksheetGenerator), «Контрольные работы» (TestWorkGenerator), «Редактор работ» (WorkEditor).
- Генерирует `.md` файл: `# Название`, `## Вариант N`, нумерованные задачи с кодом, условием (LaTeX остаётся как есть — Obsidian поддерживает), ответ в блоке `> **Ответ:**`.

### Печать — корректные поля на 2-й странице
- В `TaskWorksheet.css` добавлен `@page { margin: 10mm 8mm; }` — поля теперь одинаковы на всех страницах, включая продолжение на 2-й странице и далее.
- Уменьшен `padding` у `.printable-worksheet` в print-режиме с `7mm` до `2mm` (поля теперь управляются через `@page`).

### PDF через сервис — максимальная компактность
- Обновлены `extraCSS` в TestWorkGenerator и OralWorksheetGenerator: `padding: 0` у `.printable-worksheet`, уменьшены отступы у `.variant-header`, `.task-item`, `.tasks-content`, `.answers-page` — пустое пространство минимизировано.

### Объединение аккаунтов студентов (StudentProgressDashboard)
- Новая кнопка «Объединить» в заголовке раздела «Прогресс учеников».
- Позволяет перенести все попытки с одного аккаунта на другой и удалить исходный.
- Реализовано через серверный hook `POST /api/students/merge` (требует права суперпользователя).
- API: `mergeStudents(fromStudentId, toStudentId)` → `{ moved, deletedUsername, targetUsername }`.

### Импорт задач — выбор типа источника sdamgia
- В TaskImporter добавлен выпадающий список «Тип работы»: ЕГЭ базовый / ЕГЭ профильный / ОГЭ / ВПР 5-8 класс.
- `SDAMGIA_SOURCE_LABELS` вынесены в `utils/markdownTaskParser.js` и используются совместно в парсере и UI.
- Источник задачи (`source`) автоматически проставляется из выбранного типа.

## [3.7.7] - 2026-03-10

### Ученики — расширенный раздел прогресса (StudentProgressPage)

**Разбор ответов по работе:**
- Каждая строка в «Истории» кликабельна (стрелка `›` справа, курсор-указатель)
- По клику открывается детальный просмотр: условие задачи с LaTeX (MathRenderer), чертёж (если есть `image` или `image_url`), ответ ученика с иконкой ✓/✗
- Мини-сводка в шапке: «✓ N верно / ✗ N ошибок»
- Правильный ответ намеренно **не показывается** — только статус ответа ученика
- Кнопка «← Назад» для возврата к списку истории
- Задачи отсортированы по порядку варианта
- **Технически**: данные задач берутся через `api.getVariant()` (коллекция `tasks` — Superuser only, через expand variant доступна студентам)

**Серия (streak):**
- Баннер 🔥 при ≥2 подряд тестах с результатом ≥70%
- Подсчёт ведётся от последней попытки назад

**Галерея достижений (AchievementGallery):**
- Новый фильтр «Статус» перед фильтрами типа и редкости
- Значения: Все / Получено (N) / Не получено (N) с актуальными счётчиками

## [3.7.6] - 2026-03-10

### Ученики — детальная страница (StudentDetailPage)

**Аналитика:**
- **Период анализа** — новый `Segmented`-переключатель «Всё время / 3 месяца / Месяц» над hero-статистикой. Фильтр влияет на все аналитические блоки: hero-карточки, график, темы, проблемные задачи. История попыток остаётся полной.
- **Тренд ▲/▼** — в карточке «Средний балл» отображается сравнение последних 3 попыток с предыдущими 3 (появляется при наличии ≥ 4 попыток).
- **Тултипы на графике** — при наведении на точку всплывает подсказка с названием работы, датой и результатом. Клик по точке подсвечивает соответствующую строку в таблице истории (синяя подсветка, 3 сек) + скролл к разделу.
- **Группировка тем по разделам** — блок «Результаты по темам» разбит на секции «Алгебра» / «Геометрия» по полю `topic.section` из БД. Заголовки разделов скрыты, если все темы относятся к одному разделу.

**Проблемные задачи:**
- **Правильный ответ** — в футере каждой карточки теперь отображается `Правильный ответ: [тег]` рядом с ошибочными ответами ученика.
- **Создать работу над ошибками** — кнопка в заголовке раздела (появляется когда список открыт). Открывает модал с предзаполненным названием, списком проблемных задач с чекбоксами и процентом ошибок. Создаёт `work` + `variant` + `session` одним действием. На успешное создание: экран с готовой ссылкой для ученика (кнопка «Копировать») и переход в редактор работы.

**История попыток:**
- **Раскрытие деталей попытки** — каждая строка таблицы разворачивается и показывает список ответов: статус ✓/✗, код задачи, тему, ответ ученика, правильный ответ (если ошибка). Кнопка «Условие» раскрывает полное условие задачи с LaTeX и картинкой прямо в строке.

## [3.7.5] - 2026-03-08

### Теория — рефакторинг GeoGebra-модуля
- **GeoGebraBlocksModal**: переработан с нуля — Modal (980px) заменён на Drawer (96vw, до 1440px) с новым UX: «Снять PNG» → «Обрезать» (опционально) → «Сохранить» / «Сохранить и вставить».
- **useGeoGebraInjection** (`hooks/useGeoGebraInjection.js`) — новый shared-хук для внедрения GeoGebra-изображений в HTML; устранён дублирующийся `useEffect` в `TheoryEditor` и `TheoryArticleView`.
- **Высота GeoGebra в Drawer**: измеряется через `offsetHeight` после открытия (`afterOpenChange` + `requestAnimationFrame`) → GeoGebra заполняет ровно доступное пространство.
- **Чистый вид чертежей**: рамка `.geogebra-embed` (граница, фон, метка «GEOGEBRA», id) показывается только через CSS `:empty` — пока хук не вставил контент. После инжекции виден только чертёж и подпись.
- **Плейсхолдер**: блок без сохранённого PNG показывает подсказку «нарисуйте чертёж и нажмите «Снять PNG»» вместо пустого невидимого div.

### TaskList — массовые операции
- **Bulk-редактирование** задач через боковую панель (Drawer): массовое изменение тегов (добавить/заменить/удалить), сложности, темы, подтем, источника.
- **Создание работы из выборки**: из выделенных задач можно создать новую контрольную работу одним кликом.
- **Открытие редактора работ** из TaskList (`onOpenWorkEditor` prop).

### Геометрия
- **GeometryWorksheetPrint** (`components/GeometryWorksheetPrint.jsx`) — новый компонент: генерация и печать рабочего листа из задач геометрии. Кнопка «Рабочий лист» в GeometryTaskList открывает выбранные (или все) задачи в режиме печати.
- **TaskCard**: кнопка «Дублировать задачу» — создаёт копию с суффиксом `-copy` без вложенных файлов.
- **GeometryTaskPreview**: изолированный `@page { size: A5 }` — монтируется при открытии и убирается при закрытии, не ломая печать других разделов.

## [3.7.4] - 2026-03-01

### Рефакторинг
- **GeometryTaskEditor** (985 → 414 строк): извлечены 4 вкладки в `geometry/TabCondition`, `TabDrawing`, `TabLayout`, `TabSolution`; CropModal заменён на shared-компонент.
- **TaskEditModal** (853 → 567 строк): inline CropModal заменён на `shared/CropModal`; состояние изображений вынесено в хук `useImageUpload`.
- **OralWorksheetGenerator** (971 → 871 строк): inline-логика loadAvailableTags и task counter заменена на хуки `useAvailableTags`, `useTaskCounter`.
- **GeoGebraDrawingPanel** (205 → 138 строк): inline CropModal заменён на `shared/CropModal`.

### Новые shared-компоненты и хуки
- `components/shared/CropModal.jsx` — универсальный модал обрезки PNG (заменил 3 дубля).
- `hooks/useAvailableTags.js` — загрузка тегов по теме/подтеме.
- `hooks/useTaskCounter.js` — дебаунсированный счётчик задач по фильтрам.
- `hooks/useImageUpload.js` — состояние загрузки/выбора изображений.
- `hooks/useGeoGebraOperations.js` — GeoGebra операции (save/clear/export).
- `hooks/useTopicSubtopicCreation.js` — создание тем/подтем с дедупликацией.

### Исправлено
- **TaskImporter**: `App.useApp()` вызывался внутри `useMemo` (нарушение Rules of Hooks) — перенесён на верхний уровень компонента.

## [3.7.3] - 2026-02-27

### Изменено
- **StudentDetailPage (Ученики → Прогресс → Детали)**:
  - блок **«Проблемные задачи»** переведён в компактный карточный формат и по умолчанию **свёрнут под кат** (`Показать/Скрыть`);
  - для каждой проблемной задачи добавлен раскрывающийся просмотр **условия** и отображение **изображения задачи** (если есть `image`/`image_url`);
  - в карточках ошибок отображаются ответы ученика в компактном виде (уникальные значения + счётчик остальных);
  - секция **«Динамика результатов»** сделана менее перегруженной: уменьшен масштаб содержимого графика, убраны подписи процентов над точками, улучшены подписи дат (добавляется время при нескольких попытках в один день).

## [3.7.2] - 2026-02-26

### Изменено
- **StudentApp (`student.oipav.ru`)**:
  - добавлен компактный mobile-first лендинг для корневого адреса `/` (если `sessionId` отсутствует);
  - добавлен быстрый переход в сессию по коду (`/student/{sessionId}`) через поле ввода.
- **Деплой student frontend**:
  - `raspberry/deploy-student-to-vps.sh` переведён на актуальный путь `/var/www/student-ege`;
  - скрипт теперь синхронизирует не только `student.html` и `assets/`, но и `achievements/` + `vite.svg`.

## [3.7.1] - 2026-02-26

### Изменено
- **Система достижений** — база расширена до **72** достижений (было 48):
  - добавлены новые random-бейджи с иконками `icon049..icon073`;
  - обновлён сидер `seed-achievements.js` и добавлена поддержка авторизации через `PB_SUPERUSER_EMAIL/PB_SUPERUSER_PASSWORD`.
- **Лендинг**:
  - секция «Мотивация через игру» обновлена под актуальную базу (**72+**);
  - добавлены карточки «Железный человек» и «Джедай»;
  - убраны карточки «Знаток» и «Мастер» из витрины;
  - hover-анимация карточек достижений синхронизирована с верхним блоком «Система достижений».

## [3.7.0] - 2026-02-26

### Добавлено
- **Лендинг** — полноценная промо-страница приложения (`/landing.html`), третья точка входа Vite multi-page app:
  - 8 секций: Hero, Stats, Features, How It Works, Student Experience, Achievements, Testimonials, CTA+Footer
  - Тёмный Hero с gradient blobs, плавающими математическими символами, анимированным gradient-текстом
  - Анимированные счётчики (requestAnimationFrame + IntersectionObserver)
  - CSS phone mockup с имитацией студенческого интерфейса
  - Витрина достижений с реальными иконками (common/rare/legendary)
  - Scroll-reveal анимации, каскадный stagger-эффект
  - Полностью адаптивный (desktop → tablet → mobile → small mobile)
  - Мобильное бургер-меню
  - **Полностью автономный** — без Ant Design, PocketBase, Monaco (16.2 KB JS gzip)

### Файлы
- `ege-tasks/landing.html` — HTML entry point с meta/OG-тегами, шрифт Inter
- `ege-tasks/src/landing/main.jsx` — React bootstrap
- `ege-tasks/src/landing/LandingPage.jsx` — корневой компонент (навбар, scroll-reveal)
- `ege-tasks/src/landing/LandingPage.css` — CSS дизайн-система (переменные, 10 анимаций, 4 брейкпоинта)
- `ege-tasks/src/landing/components/` — 8 подкомпонентов секций
- `ege-tasks/vite.config.js` — добавлен `landing` в `rollupOptions.input`

## [3.6.5] - 2026-02-24

### Добавлено
- **Изображения в обычных задачах** — `TaskEditModal` теперь поддерживает 3 режима:
  - **По ссылке** — ввод URL (как раньше)
  - **Загрузить** — загрузка PNG/JPG с компьютера + кадрирование
  - **Нарисовать** — встроенный GeoGebra-апплет → экспорт PNG → кадрирование
- **`GeoGebraDrawingPanel`** — новый компонент: упрощённая панель GeoGebra без сохранения состояния (только PNG-результат). Кнопки: «Сохранить PNG», «Обрезать», «Очистить».
- **`utils/cropImage.js`** — утилиты обрезки PNG вынесены в shared-модуль: `cropPngByMargins`, `normalizeCrop`, `dataUrlToFile` и др.

### Улучшено
- **GeoGebraApplet**: polling через `requestAnimationFrame` (до 180 кадров) заменён на `ResizeObserver` — ноль принудительных reflow, event-driven подход.
- **GeometryTaskEditor**: убраны каскадные fallback'ы при сохранении (до 12 HTTP-запросов → 1). Схема зафиксирована по реальной БД на VPS.

### Рефакторинг
- `GeometryTaskEditor` — локальные утилиты обрезки заменены на импорт из `utils/cropImage.js`.

## [3.6.4] - 2026-02-23

### Добавлено
- **Геометрия: переключаемый список задач** — режимы `Таблица` и `Карточки` в `GeometryTaskList`.
  В карточках доступны: выбор задач, действия (редактировать/просмотр/удалить), превью PNG-чертежа,
  LaTeX-ответ и пагинация `10/20/50`.

### Изменено
- **Предпросмотр листа геометрии (`GeometryTaskPreview`)**:
  - drag&drop смены порядка задач доступен прямо в режиме просмотра (когда выключено `Редактировать макет`);
  - нумерация карточек сделана сквозной по позиции в листе (`1..N`), без привязки к коду задачи.

### Документация
- Добавлен файл **`SCHEMA_GEOMETRY.md`** как актуальный source of truth по коллекциям геометрии на production:
  типы полей, обязательность, правила сохранения PNG/GeoGebra и проверка схемы.

## [3.6.3] - 2026-02-23

### Исправлено
- **Синхронизация схемы `geometry_tasks` с реальным VPS (`task-ege.oipav.ru`)**:
  - поле `preview_layout` добавлено в коллекцию и таблицу;
  - поле `topic` восстановлено и переведено в `relation → geometry_topics`;
  - поле `subtopic` приведено к `relation → geometry_subtopics`;
  - пустые `topic` заполнены по выбранным `subtopic`.
- **`task_type` больше не блокирует сохранение**:
  - в схеме VPS снят `required`, существующие пустые значения заполнены `ready`;
  - фронтенд-сохранение использует валидные значения enum (`ready|build|mixed`) при fallback.
- **PNG-чертеж в геометрии хранится как файл**:
  - поле `geogebra_image_base64` приведено к типу `file` (PocketBase storage);
  - предпросмотры/списки используют URL файла из storage.

### Документация
- Обновлены `README.MD` и `CLAUDE.MD` под фактическую схему геометрического модуля:
  `topic/subtopic` relations, `preview_layout`, `task_type` enum, `geogebra_image_base64` как file field.

## [3.6.2] - 2026-02-22

### Добавлено
- **Быстрый предпросмотр задачи** прямо из списка: модальное окно с `GeometryPreviewCard`,
  переключатель «Показывать ответ», режим редактирования макета (drag+resize) и сохранение
  макета в `preview_layout` — всё без перехода в полный предпросмотр.
- **Скрипт миграции** `scripts/migrate-geometry-images.mjs`: перенёс 17 PNG-чертежей
  из текстового поля `geogebra_image_base64` в файловое поле PocketBase `drawing_image`.

### Исправлено
- **Критический баг редактора**: при открытии задачи из списка `statement_md`, `solution_md`
  и `geogebra_base64` не попадали в редактор (не входят в `LIGHT_FIELDS`). Редактор показывал
  пустые поля и затирал данные при сохранении. Исправлено: `openEdit()` теперь вызывает
  `getGeometryTask(id)` (полная запись) перед открытием редактора.
- **GeoGebra-аплет загружался пустым** при редактировании — следствие того же бага.
  После исправления `geogebra_base64` (XML-состояние `.ggb`) корректно загружается в аплет.
- **Масштабирование в быстром предпросмотре**: `mode="print"` использовал `mm`-единицы,
  которые не масштабируются с контейнером. Заменено на `mode="student"` (px).
- **Клиппинг чертежа**: убраны `max-height: 45mm` и `max-height: 220px` из CSS.
- **Колонка «Чертёж»** в списке: `geogebra_base64` не входит в LIGHT_FIELDS,
  поэтому значок GGB определяется через `drawing_view === 'geogebra'`.

### Изменено
- **PNG-хранилище**: чертежи перенесены из base64 TEXT-поля в файловое поле `drawing_image`
  (FileField, max 10 МБ). Преимущества: −33% размера, HTTP-кеширование, нет в API-ответах списка.
  - `getGeometryImageUrl(task)` — хелпер URL файла (fallback: legacy base64).
  - `LIGHT_FIELDS` для `getGeometryTasks()`: исключает тяжёлые поля (geogebra_base64, solution_md),
    `statement_md` включён (нужен для быстрого предпросмотра).
  - `GeometryTaskEditor.handleSave()`: PNG → `File` через `atob()`, сохраняется в `drawing_image`.
- **Индикатор загрузки** на кнопке «Редактировать» в строке таблицы (`loading` по `task.id`).

### Удалено
- **Поле `task_type`** из `geometry_tasks` (DB + фронтенд).
  Значения «Готовый чертёж / Построение / Смешанный» не имели практического смысла:
  режим «Построение» требует живого GeoGebra-аплета на стороне студента и автоматической
  проверки результата, что не реализовано и не планируется.
  Геометрический модуль сфокусирован на сборе задач для печатных листов A5.
  - Удалены: `TASK_TYPE_OPTIONS`, `TASK_TYPE_LABELS`, фильтр, колонка таблицы
  - Миграция: `1771900000_remove_task_type_from_geometry_tasks.js` (применена на VPS)

## [3.6.1] - 2026-02-22

### Добавлено
- **Модуль «Геометрия» (MVP для учителя)**:
  - список геометрических задач с фильтрами и редактором;
  - GeoGebra-апплет и сохранение состояния чертежа в задаче;
  - PNG-экспорт из GeoGebra с интерактивной обрезкой;
  - режим предпросмотра «Печать A5 (6 задач)» и «Вид ученика»;
  - ручная компоновка блоков «Чертёж» и «Дано» в каждой задаче (перетаскивание, resize, масштаб текста);
  - сохранение макета задачи в `geometry_tasks.preview_layout`;
  - сохранение и загрузка печатных листов A5 через отдельную коллекцию `geometry_print_tests`.

### Изменено
- Геометрический предпросмотр приведён к общей логике приложения:
  - сохранение/открытие сделано через модальные окна (как в генераторе), без `prompt/alert`;
  - добавлен явный список «Листы A5» в разделе `Геометрия -> Задачи`.
- Улучшен рендеринг текстов с формулами:
  - снижен шум предупреждений KaTeX для смешанного русско-математического контента.
- Типографика геометрического предпросмотра:
  - для обычного текста используется тот же формульный стек (`KaTeX_Main`), без `Times New Roman`.

## [3.6.0] - 2026-02-20

### Добавлено
- **Раздельные Vite-приложения** — монорепо с двумя независимыми бандлами из одного репозитория
  - `index.html` + `src/teacher/main.jsx` — учительское приложение (локально)
  - `student.html` + `src/student/main.jsx` — студенческое приложение (VPS)
  - `npm run build` собирает оба бандла одновременно
- **`src/shared/`** — общий код для обоих приложений
  - `shared/services/pocketbase.js` + `pocketbaseUrl.js` — API и URL-резолвер
  - `shared/components/MathRenderer.jsx` — рендерер LaTeX + Markdown
  - `shared/utils/shuffle.js` + `escapeFilter.js` — утилиты
  - Оригинальные пути сохранены как реэкспорты (обратная совместимость)
- **`student.oipav.ru`** — отдельный домен для студенческого интерфейса
  - Let's Encrypt SSL сертификат
  - nginx конфиг на VPS: `/var/www/student-ege/`
  - Фронтенд задеплоен: `https://student.oipav.ru`
- **`VITE_STUDENT_URL`** — переменная окружения для домена студентов
- **`raspberry/deploy-student-to-vps.sh`** — скрипт деплоя студенческого приложения на VPS
- Alias `@shared` в `vite.config.js` для удобного импорта shared-модулей

### Изменено
- **`SessionPanel.jsx`** — `buildStudentUrl()` использует `VITE_STUDENT_URL` вместо `window.location.origin`
  - QR-коды и ссылки теперь ведут на `https://student.oipav.ru/student/{sessionId}`
- **`raspberry/nginx.conf`** — обновлён для поддержки двух SPA
  - `/student/` → `student.html`
  - `/` → `index.html`
- **`ege-tasks/index.html`** — entrypoint учительского приложения → `src/teacher/main.jsx`
- **`ege-tasks/vite.config.js`** — multi-input build + `@shared` alias
- Удалён `src/main.jsx` (заменён двумя специализированными entrypoints)
- Обновлён деплой Raspberry Pi (nginx конфиг + оба HTML файла)

### Архитектура
```
Учитель (локально):           Студент (VPS: student.oipav.ru):
index.html                    student.html
src/teacher/main.jsx    →     src/student/main.jsx
App.jsx + все компоненты      StudentApp.jsx + student/ компоненты
         ↓                              ↓
         └──── src/shared/ ────────────┘
               (pocketbase, MathRenderer, shuffle, escapeFilter)
                        ↓
               PocketBase (task-ege.oipav.ru)
```

---

## [3.5.1] - 2026-02-20

### Изменено
- **Темы оформления теоретических статей** — полный редизайн всех 5 тем (`themes.css`)
  - **Классическая** — редакционный стиль: Playfair Display для заголовков, Source Serif 4 для текста, тёмно-синяя палитра + золотые акценты, таблицы с тёмной шапкой
  - **Минимализм** — японский вабисаби: DM Sans weight 200–300, дымчатый фон `#f8f7f5`, таблицы без рамок (только горизонтальные линии)
  - **Академическая** → переработана в **строгую монографию LaTeX-стиля**: Libre Baskerville, двойная линия под H1 (как в монографиях), автонумерация разделов через CSS-счётчики (`1. Раздел`, `1.1 Подраздел`), абзацный отступ + полный justify, таблицы в booktabs-стиле (только горизонтальные линии), списки с тире
  - **Тетрадь** → переработана в **скетчбук / Moleskine**: Caveat (рукописный шрифт), белый фон с dot-grid сеткой, коричнево-сепийные чернила `#3d2b1f`, градиентное подчёркивание H1, выделение `strong` как маркером (жёлтый фон-градиент), KaTeX без изменения цвета (читается на белом)
  - **Компактная** — техническая документация: IBM Plex Mono для заголовков, DM Sans для текста, тёмная шапка H1, максимальная информационная плотность
- Подключены Google Fonts через `@import` для характерных шрифтов каждой темы

---

## [3.5.0] - 2026-02-17

### Добавлено
- **PDF-сервис на VPS** — Puppeteer (puppeteer-core + системный Chromium) на VPS
  - systemd сервис `pdf-service-ege` (порт 3001)
  - nginx reverse proxy: `https://task-ege.oipav.ru/pdf`
  - Поддержка `PUPPETEER_EXECUTABLE_PATH` для системного Chromium
  - Динамический импорт puppeteer/puppeteer-core
- **Telegram-бот на VPS** — полная переписка для мониторинга VPS
  - systemd сервис `telegram-bot-ege`
  - Команды: `/status` (сервисы + система), `/db` (статистика БД через sqlite3),
    `/backups` (бэкапы), `/health` (HTTP health-check), `/restart`, `/logs`, `/help`
  - Старый бот на Pi отключён

### Изменено
- **`start.sh`** — упрощён: по умолчанию только фронтенд (backend на VPS)
  - `--local-pdf` — фронтенд + локальный PDF-сервис
  - `--full` — полностью локальный режим (PB + PDF + Frontend)
- **`stop.sh`** — упрощён (VPS-сервисы не затрагиваются)
- **`package.json`** (корневой) — обновлены скрипты dev/dev:local-pdf/dev:full
- **`raspberry/deploy-to-raspberry.sh`** — упрощён до 4 шагов (только фронтенд + nginx)
- **`raspberry/nginx.conf`** — убрано проксирование /api/, /_/, /pdf/ (всё на VPS)
- **`pocketbase/pdf-service.js`** — поддержка puppeteer-core и env переменных
- **`VITE_PDF_SERVICE_URL`** — `https://task-ege.oipav.ru/pdf`
- Обновлена документация (CLAUDE.md, TELEGRAM_BOT.md, QUICK_REFERENCE.md)

---

## [3.4.0] - 2026-02-17

### Добавлено
- **Миграция PocketBase на VPS** — единая база данных для всех клиентов (Mac, Raspberry Pi)
  - PocketBase 0.36.4 развёрнут на VPS (147.45.158.148)
  - Домен: `https://task-ege.oipav.ru` с Let's Encrypt SSL
  - nginx reverse proxy → PocketBase на порту 8095
  - systemd сервис `pocketbase-ege` с автозапуском
- **Автоматические бэкапы на VPS** — cron каждые 6 часов
  - Безопасный бэкап через `sqlite3 .backup` (safe while running)
  - Ротация: максимум 20 бэкапов
  - Логирование в `/opt/pocketbase/backups/backup.log`

### Изменено
- **`VITE_PB_URL`** — обновлён на `https://task-ege.oipav.ru`
- Фронтенд теперь работает с удалённой базой (локальный PB больше не требуется для разработки)
- Обновлена документация (CLAUDE.md, CHANGELOG.md)

---

## [3.3.2] - 2026-02-15

### Добавлено
- **Локализация изображений задач**:
  - новый скрипт миграции `migrate_task_images_to_pb.js` для переноса внешних `image_url` в файловое поле `tasks.image`,
  - npm-команда `npm run migrate:task-images`,
  - прокси-эндпоинт `POST /fetch-image` в PDF-сервисе для безопасной загрузки картинок без CORS-проблем.
- **Универсальный резолвер изображения задачи** — `api.getTaskImageUrl(task)` в `pocketbase.js`.

### Изменено
- **Импорт задач (TaskImporter)** — при импорте изображение скачивается и сохраняется в PocketBase как файл; если загрузка не удалась, сохраняется внешняя ссылка как fallback.
- **Отображение картинок задач** унифицировано в интерфейсах:
  - `TaskCard`,
  - `PrintableWorksheet`,
  - `VariantRenderer`,
  - превью вариантов в разделе «Мои работы» (`WorkManager`).
- **Верстка превью варианта в «Мои работы»** — добавлены стили для стабильного отображения изображений без поломки карточек.

---

## [3.3.1] - 2026-02-15

### Изменено
- **StudentProgressDashboard** — в модальном окне «Детали» удалено редактирование класса ученика; класс теперь только для просмотра (управление классами перенесено в админку PocketBase).
- **StudentApp (AchievementGallery)** — галерея достижений разделяет прогресс:
  - достижения за текущую сессию (текущий тест),
  - достижения за все попытки ученика.
- **Легенда статусов достижений** — добавлены маркеры:
  - зелёная точка — получено за текущий тест,
  - синяя точка — получено ранее,
  - замок — не получено.
- **UI достижений** — выровнены размеры карточек в сетке, маркеры перенесены в угол карточки, улучшена читаемость маркеров.
- **StudentTestPage / StudentResultPage** — в шапке показывается название работы (`work.title`) вместо заголовка для учеников (`student_title`).

---

## [3.3.0] - 2026-02-14

### Добавлено
- **Глубокий редизайн модуля «Теория»** — полная переработка всех 5 компонентов
  - **TheoryBrowser** — hero-секция со статистикой, градиентные карточки, категории-pills с цветными точками, кастомные карточки статей с hover-анимациями
  - **TheoryEditor** — rich formatting toolbar (заголовки, форматирование, списки, таблицы, формулы, изображения, ссылки), split-pane с ресайзом мышкой, status bar (слова, формулы, символы, тема, автосохранение)
  - **TheoryArticleView** — TOC sidebar с IntersectionObserver, floating action bar (Edit/Print/PDF/Theme), связанные задачи горизонтальными карточками
  - **TheoryCategoryManager** — визуальные карточки с градиентными иконками вместо таблицы, drag-and-drop для порядка
  - **TheoryPrintBuilder** — sidebar с выбором и drag-and-drop переупорядочиванием статей, индикатор кол-ва страниц
- **EditorToolbar** — новый субкомпонент с 4 группами кнопок форматирования для Monaco Editor
- **TableInsertPopover** — popover для вставки таблиц NxM
- **5 тем оформления** — classic, minimal, academic, notebook, compact (ранее только 2)

### Изменено
- Full-height layout для theory-view и theory-print (убраны лишние padding/margin)
- 7 новых CSS-файлов для компонентов теории
- Все компоненты используют staggered-анимации, HTML5 Drag API, современные карточки

---

## [3.2.0] - 2026-02-13

### Добавлено
- **Поле `student_class`** — учителя могут указывать класс ученика в панели прогресса
- **Обновление учеников учителем** — учитель может редактировать данные учеников (класс, имя)
- **Миграция правил доступа** — students.updateRule разрешает обновление учителем/админом

### Изменено
- Исправлена утечка авторизации в StudentProgressDashboard (учитель автоматически выходит из студенческого аккаунта при загрузке App)
- Исправлен краш TestWorkGenerator при генерации
- Добавлено перемещение задач между вариантами в TestWorkGenerator

---

## [3.1.0] - 2026-02-12

### Добавлено
- **Система достижений** — 30 предустановленных достижений с анимированными иконками
  - Случайные бейджи (common, rare, legendary) — один за попытку
  - Условные достижения (score, speed, count, special) — автоматическая разблокировка
  - Галерея достижений для ученика
  - Ручная выдача достижений учителем
  - Включение/отключение на уровне сессии
  - Polling каждые 10 секунд для обновления наград от учителя
- **Авторизация учеников** — регистрация (имя + логин + пароль), авторизация через PocketBase Auth
  - Привязка гостевых попыток к аккаунту
  - Повторное прохождение тестов под контролем учителя
- **Панель прогресса учеников** (StudentProgressDashboard) — таблица всех зарегистрированных учеников с попытками, средним баллом, классом
- **Список выполненных работ ученика** с deep-link на редактор работ
- **Модальное окно достижений учителя** с просмотром уже выданных бейджей

### Изменено
- Улучшен UI студенческого приложения (StudentApp)
- Исправлена синхронизация потока ученика (attempt reuse, answers persistence)
- Исправлена работа парсера/вывода, ручных достижений учителя
- Усилен скрипт бэкапа
- Убран серый фон у achievement-lock-overlay и иконок достижений

---

## [3.0.0] - 2026-02-10

### Добавлено
- **Студенческий интерфейс** — полный поток тестирования
  - StudentAuthPage — авторизация/регистрация
  - StudentEntryPage — стартовый экран теста
  - StudentTestPage — прохождение теста с автосохранением ответов
  - StudentResultPage — просмотр результатов
  - Dual routing: `/student/{sessionId}` → StudentApp
- **Редактор работ** (WorkEditor + WorkEditorPage) — управление вариантами и задачами сохранённых работ
  - Добавление/удаление/перетаскивание задач
  - Замена и редактирование задач
  - Панель сессии (SessionPanel) — выдача, QR-код, ссылка
  - Результаты учеников (TeacherResultsDashboard)
  - Перемещение задач между вариантами
- **Менеджер работ** (WorkManager) — список работ с поиском, фильтрами, архивированием
- **Проверка ответов** (answerChecker.js) — числовое сравнение, дроби, LaTeX, альтернативы через `|`
- **Сессии тестирования** (work_sessions) — выдача работ ученикам с QR-кодом
- **Попытки** (attempts) — отслеживание статуса, оценок, длительности
- **Ответы по задачам** (attempt_answers) — хранение и проверка ответов
- **Коллекция students** — Auth-коллекция для учеников
- **useStudentSession** — хук для управления сессией ученика
- **ReferenceDataContext** — глобальный контекст справочных данных (topics, tags, subtopics, years, sources)
- **Парсинг sdamgia.ru** — эндпоинт POST /parse-sdamgia в PDF-сервисе
  - Конвертация HTML-таблиц в Markdown
  - Очистка LaTeX-формул из русскоязычных alt-текстов
- **Заголовок для учеников** (student_title) в сессии

### Изменено
- Рефакторинг TaskCatalogManager — декомпозиция на подкомпоненты (TopicTab, SubtopicTab, TagTab, SourceTab, OtherTab, DuplicateTab, MergeModal)
- Рефакторинг: prop drilling заменён на ReferenceDataContext
- Ленивая загрузка TheoryEditor для уменьшения начального бандла
- Исправлено экранирование строковых значений в фильтрах PocketBase
- Заменена biased сортировка на Fisher-Yates shuffle

---

## [2.2.0] - 2026-02-08

### Добавлено
- **TaskCatalogManager** — единый каталог справочников с CRUD для тем, подтем, тегов, источников
- **Обнаружение и объединение дублей** — по нормализованным названиям + дубли задач
- **Быстрый переход к задачам** из каталога по выбранному элементу
- **Скрипты обслуживания**: `fix_task2_tables.py`, `recolor_tags.py`

### Изменено
- Обновлена документация: CLAUDE.md, ARCHITECTURE.md, CHANGELOG.md

---

## [2.1.0] - 2026-02-07

### Добавлено
- **Массовые операции с задачами**: теги (добавить/заменить), сложность, источник, тема, подтемы, удаление
- **Редактирование сохранённых работ** (update work/variants) в генераторах
- **Автопрогрессия сложности** в генераторах
- **Дашборд статистики** по темам, подтемам, тегам, сложности и источникам
- **Облако тегов** со ссылкой на отфильтрованные задачи
- **Связь теория → задачи** по тегам статьи
- **Каталог справочников** с CRUD, объединением дублей и переходом к задачам
- **Скрипты обслуживания**: `fix_task2_tables.py`, `recolor_tags.py`

### Изменено
- **TaskFilters / TaskList**: поддержка внешнего применения фильтров (переход из статистики)
- **useWorksheetGeneration**: прогрессивная сложность как опция генерации
- **useWorksheetActions**: обновление сохранённых работ
- **TaskFilters**: фильтр по наличию изображения

---

## [2.0.0] - 2026-02-04

### Добавлено
- **TestWorkGenerator** — генератор контрольных работ с задачами из разных тем
  - Конструктор структуры работы с блоками задач
  - Три режима генерации: different / shuffled / same
  - Гибкие фильтры для каждого блока
  - Drag & Drop задач между вариантами
  - Замена и редактирование задач в варианте
  - Сохранение/загрузка работ в БД
  - Экспорт в PDF

#### Архитектурные улучшения
- **useWorksheetGeneration.js** — универсальная логика генерации вариантов
- **useTaskDragDrop.js** — drag & drop функционал
- **useWorksheetActions.js** — действия с работами (печать, PDF, сохранение)
- **FilterBlock.jsx** — универсальный блок фильтров
- **VariantSettings.jsx** — настройки генерации
- **FormatSettings.jsx** — настройки форматирования

### Изменено
- Добавлен пункт меню «Контрольные работы» в App.jsx
- Добавлена зависимость `uuid`

---

## [1.5.0] - 2026-01

### Добавлено
- **PDF экспорт через Puppeteer** — standalone микросервис на порту 3001
  - Высокое качество рендеринга KaTeX
  - Векторный текст, 2-3x меньший размер файлов
  - Автоматический fallback на html2pdf.js
- **usePuppeteerPDF** — хук для PDF экспорта
- **Единый скрипт запуска** (start.sh) — PocketBase + PDF Service + Frontend
- **Модуль теории** — TheoryBrowser, TheoryEditor (Monaco), TheoryArticleView, TheoryCategoryManager, TheoryPrintBuilder
- **Создание задач через UI** — полная форма с метаданными
- **Автоматическое резервное копирование** (macOS LaunchAgent)

---

## [1.0.0] - 2026-01

### Начальный релиз
- Генератор карточек (A5/A6/A4)
- Генератор «Устный счёт» с тегами и Drag & Drop
- Управление задачами (CRUD, фильтрация)
- Редактирование задач с LaTeX предпросмотром
- Поддержка LaTeX (KaTeX) и Markdown (GFM)
- Парсеры: pb_parser.py, pb_parser_mordkovich.py
- PocketBase backend с миграциями
- Фильтрация по темам, подтемам, тегам, сложности, источникам, годам
- Экспорт в PDF (html2pdf.js)
